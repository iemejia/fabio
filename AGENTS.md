# Fabio CLI - Session Context

## Goal
- Design and implement an agent-native CLI (`fabio`) to manage Microsoft Fabric artifacts and data, inspired by AWS/gcloud/Azure CLI principles, with structured JSON output, composability via stdin/stdout, and machine-readable errors.

## Agent-Native CLI Principles

Fabio must always respect these 10 principles for agent-native CLIs:
https://trevinsays.com/p/10-principles-for-agent-native-clis

1. **Non-interactive by default** — No prompts; all inputs via flags/env/files. Non-TTY must fail fast.
2. **Structured, parseable output** — `--json` on every command; stdout = data, stderr = diagnostics; stable exit codes.
3. **Errors that teach and enumerate** — Errors include valid enum values, corrected command examples, and machine-readable codes with hints.
4. **Safe retries and explicit mutation boundaries** — `--dry-run` for mutations; idempotency-safe; stable returned IDs.
5. **Bounded responses** — `--limit` for list commands; default to concise output; truncation metadata in envelope.
6. **Cross-CLI vocabulary consistency** — Canonical agent verbs: `list`, `show`, `create`, `delete`, `copy`, `move`.
7. **Three-layer introspection** — `fabio context agent` provides machine-readable command schema (flags, types, mutability, examples). `fabio context` provides semantic knowledge (item definition schemas, workflow recipes, output examples, best-practices guidance).
8. **Async-aware execution** — `--wait` for async jobs; local job ledger (`fabio jobs list/get/prune`); status polling.
9. **Persistent identity through profiles** — Named profiles (`fabio profile save/use/list/show/delete`); `--profile` flag.
10. **Two-way I/O** — Feedback channel (`fabio feedback send/list`); artifact delivery via stdout/file.

## Constraints & Preferences
- **HTTPS-only endpoints (MANDATORY)** — fabio MUST only ever communicate with network endpoints over HTTPS. This applies to every endpoint fabio itself calls: the Fabric REST API, OneLake DFS/Blob/Table, ARM, Power BI, Kusto, a data agent's published consumption URL, and any user-supplied LLM endpoint (`--llm-endpoint`). Enforcement points already in place: (1) `client::validate_endpoint_env_overrides()` runs at startup and rejects any `FABIO_*_ENDPOINT`/`FABIO_*_SCOPE` override that is not `https://`; (2) `client::validate_trusted_url()` requires HTTPS + a trusted Microsoft host (used for `--published-url` and API-returned URLs); (3) `LlmClient::from_config()` rejects a non-HTTPS `--llm-endpoint` so the API key is never sent in plaintext. When you add ANY new outbound endpoint (a new base URL, a new user-supplied URL flag, a new env override), it MUST be validated as HTTPS (reuse `client::is_secure_or_loopback` / `validate_trusted_url`) and MUST NOT be constructed with an `http://` scheme. The ONLY permitted `http://` literals in the codebase are non-network identifiers: RDF/OWL namespace IRIs (`http://www.w3.org/...`, `http://example.org/...` in `ontology*.rs`) and the OAuth loopback redirect (`http://localhost:{port}` in `token_cache.rs`, required by RFC 8252). Plaintext `http://` to a **loopback** host (`localhost`, `127.0.0.0/8`, `::1`) is the one runtime exception (via `client::is_secure_or_loopback`) — it never leaves the machine, and is required for local mock-server tests and locally-hosted OpenAI-compatible model servers; a `http://` override to any non-loopback host is rejected. All documentation, examples, and `.md`/`.mdx` files MUST use `https://` for any real endpoint.
- **Windows-first compatibility** — All code must work on Windows. Use `Path::new().join()` (never hardcoded `/` for filesystem paths), `dirs::home_dir()` (never manual `HOME`/`USERPROFILE`), `.lines()` for text parsing (handles CRLF), no Unix-specific APIs. `.gitattributes` enforces LF line endings.
- **Throttling reduction** — Reduce the likelihood of API throttling by:
  - Use bulk and batch operations when available (e.g., `item bulk-create`, `item bulk-delete`, workspace role batch-assign, domain batch-assign).
  - Prefer list APIs over repeated single-resource requests (e.g., use a single list call + client-side filter rather than N individual show calls).
- CLI designed for AI agents first (structured output, no interactive prompts, explicit params)
- JSON output by default with `--output json|table|plain` flag
- Composable: manage inputs and produce outputs for piping
- Machine-readable error codes in structured JSON envelope
- Rust (edition 2024, rust-version 1.97.1), uses clap derive, tokio, reqwest, azure_identity, serde, serde_yaml, comfy-table, thiserror/anyhow
- Linting: clippy pedantic+nursery (zero warnings), rustfmt
- CI: GitHub Actions (cargo fmt, clippy, test, build release) on ubuntu/macos/windows
- Installable via `cargo install --git https://github.com/iemejia/fabio.git`
- **Dependency version freshness** — When introducing a new Cargo dependency or a new GitHub Action, always validate that you are using the most recent available and compatible version. Check crates.io for Rust crates and the action's repository releases/tags for GitHub Actions. Do NOT copy outdated versions from examples or memory — verify against the source of truth before adding. Additionally, reject any dependency with an incompatible license (GPL, LGPL, AGPL, SSPL, or any other copyleft license that would impose restrictions on the project). Only permissive licenses (MIT, Apache-2.0, BSD, ISC, Zlib, Unicode-3.0, etc.) are acceptable.
- **GitHub Actions pinning** — ALL GitHub Actions in `.github/workflows/*.yml` MUST be pinned to their full commit SHA with the version in a trailing comment. Format: `uses: owner/action@<40-char-sha> # v<major>` (or `# v<major>.<minor>.<patch>` for non-major tags). NEVER use floating tag references like `@v7` or `@stable`. This prevents supply-chain attacks where a tag is force-pushed to a compromised commit. When updating an action, always verify the new SHA matches the expected release tag from the action's repository.
- **Modern Rust idioms (MANDATORY)** — All code MUST leverage features available in the declared `rust-version` (currently 1.97.1). Do NOT write code using older patterns when a modern equivalent exists. When the MSRV is bumped, audit and migrate existing code. Key idioms to prefer:
  - `str::floor_char_boundary()` for safe string truncation (never raw `&s[..n]` on user/API text)
  - Let chains (`if let Some(x) = opt && condition { ... }`) instead of nested `if let` + `if`
  - `Option::is_none_or(|v| cond)` instead of `opt.is_none() || opt == Some(x)` or `opt.map_or(true, ...)`
  - `Option::is_some_and(|v| cond)` instead of `matches!(opt, Some(x) if cond)` or `opt.map_or(false, ...)`
  - `Duration::from_mins()` / `from_hours()` instead of `from_secs(N * 60)`
  - `std::io::read_to_string(reader)` instead of `let mut buf = String::new(); reader.read_to_string(&mut buf)`
  - `Vec::extract_if()` when you need both the removed elements and the remainder
  - `Value::from(x)` instead of `Value::String(x.to_string())` for `&str` values (canonical serde_json idiom)
  - `x.to_string()` instead of `format!("{x}")` for single-value Display formatting
  - `eq_ignore_ascii_case()` instead of `a.to_lowercase() == b.to_lowercase()` (allocation-free)
  - `HashSet`/`BTreeSet` for membership tests instead of `Vec::contains` or `.iter().any()` when the collection is checked multiple times
  - `const fn` for pure functions returning static data (enables compile-time evaluation)
  - `#[inline]` on small, hot-path functions called across module boundaries

## Irreversible Operations & Agent Safety (MANDATORY)

Fabio is agent-first. AI agents consume structured output and may automatically retry failed commands. When a command performs an irreversible or destructive operation, you MUST implement safety guardrails so agents are explicitly warned before proceeding.

### Rules for new commands or features:

1. **Identify irreversible operations** — Any operation that deletes data, overwrites definitions without backup, or cannot be undone. Examples: item deletion, `--hard-delete`, `--delete-orphans`, `--force-all` (overwrites all definitions), `updateDefinition` (replaces content permanently).

2. **Use `FabioError::with_hint()` for safety-bypass flags** — When an error or guard blocks execution and the hint suggests a flag that bypasses the safety check (e.g., `--force`, `--hard-delete`, `--allow-delete-types`), always use `with_hint()`. The hint text triggers the agent safety notice automatically when an AI agent is detected (`src/agent.rs`).

3. **Dangerous flags must be in `DANGEROUS_FLAGS`** — If you add a new safety-bypass flag, add it to the `DANGEROUS_FLAGS` array in `src/agent.rs`. This ensures the agent safety notice fires when the flag is suggested in an error hint. A deterministic test (`all_escalation_flags_are_triaged` in `src/agent.rs`) reads every escalation-style flag (`--allow*`, `--force*`, `--overwrite`, `--hard*`, `--drop*`, `--purge*`, `--prune*`, `--truncate*`, `--discard*`, `--delete-orphans`, `--cancel-on*`) out of `commands.json` and fails unless each is EITHER in `DANGEROUS_FLAGS` (a genuine bypass) OR in the test's explicit `BENIGN_ESCALATION_FLAGS` allowlist (a capability/behavior toggle). A newly-added escalation-style flag therefore cannot silently escape the notice mechanism — it fails the test until consciously classified. The companion test `every_dangerous_flag_triggers_the_notice` guards the wiring (every listed flag actually fires the notice).

4. **Add `"destructive": true/false` to batch output** — For commands that produce a plan or summary of multiple actions (like `deploy plan/apply`), include a `"destructive"` boolean field in the structured output. Set to `true` when the operation includes deletions, overwrites, or other irreversible actions. Agents use this field to decide whether to ask the human for confirmation.

5. **Protected types require explicit opt-in** — Data-bearing item types (Lakehouse, Warehouse, SQLDatabase, Eventhouse, KQLDatabase) require `--allow-delete-types` for deletion. If you add support for a new data-bearing item type, add it to `PROTECTED_DELETE_TYPES` in `src/commands/deploy/mod.rs`.

6. **Warn on force/override modes** — When `--force-all`, `--force`, or similar override flags are active, emit a warning in the output explaining the irreversibility. This helps agents surface the risk to the human.

7. **Never add interactive prompts** — Fabio is non-interactive (Principle 1). Do NOT add `y/N` prompts or `--auto-approve` flags. Instead, use structured output signals (`"destructive": true`, warnings, `agentNotice`) that agents can programmatically evaluate.

### Standard guardrail stack for a NEW destructive command (MANDATORY checklist)

ANY new command/subcommand/operation that deletes data, overwrites content without backup, permanently replaces a definition, kills a running session/job, or is otherwise irreversible MUST ship with the SAME guardrail stack the existing destructive ops use (`item delete`, `lakehouse delete-directory`/`delete-table`/`delete-file`, `deploy apply --delete-orphans`, `warehouse queries-kill`, `data-agent delete --hard-delete`, `git relation delete`, `updateDefinition`, `reset`, `prune`, …). Before you consider the feature done, verify EVERY box:

- [ ] **`--dry-run` guard** — call `output::dry_run_guard(cli, "<group> <subcommand>", &preview)` and return early when it returns `true`, BEFORE any mutating network call. `preview` must describe exactly what would be affected (ids, paths, counts). Put the guard AFTER input-scope validation so a dry-run of an unsafe request still surfaces the validation error. The operation string MUST be the canonical `"<group> <subcommand>"` key (NOT a descriptive sentence like `"Would delete X"`): `dry_run_guard` looks it up in `commands.json` and, for a `destructive: true` command, adds `"destructive": true` + an agent `agentNotice` automatically — a mismatched key silently drops that confirm-with-user signal (enforced by `destructive_dry_run_previews_carry_confirm_signal`).
- [ ] **`--readonly` enforcement** — the mutation must route through a client method that calls `guard_readonly("<METHOD>", …)` (all `post`/`put`/`patch`/`delete` client helpers do). Never bypass the client for a raw mutating request.
- [ ] **`"destructive": true` in `commands.json`** — after `cargo test generate_agent_schema -- --ignored`, confirm the subcommand has `"destructive": true`. The generator only auto-infers this for `delete*`-named subcommands; for destructive ops with other names (`reset`, `kill`, `prune`, `update-definition`, `--hard-delete`, `--force*`, `--delete-orphans`) you MUST set it manually. Also set `"mutates": true`.
- [ ] **Blast-radius input guard** — if a malformed/empty/wildcard input could destroy far more than intended (e.g. an empty/root path recursively deleting an entire item — see `validate_delete_directory_path`; a glob matching everything; a missing filter deleting all rows), add a pure `validate_*` function that refuses the catastrophic case with a clear `FabioError::with_hint`, and unit-test it. Fail BEFORE any network call.
- [ ] **Safety-bypass flags** — if the operation is gated behind a bypass flag (`--force`, `--hard-delete`, `--allow-delete-types`, `--delete-orphans`, `--overwrite`, …), add the flag to `DANGEROUS_FLAGS` in `src/agent.rs` and surface it via `FabioError::with_hint()` so the `agentNotice` fires (rules 2–3 above).
- [ ] **Tests** — an e2e test asserting the `--dry-run` output (`dry_run: true`, `would_execute`, key `details`) AND a test for the blast-radius guard error. Do NOT rely solely on live happy-path.
- [ ] **Consistent verb** — destructive removal uses `delete` (never `remove`); see Key Decisions.

When you add or change a destructive command, re-read this checklist during the Pre-Commit Self-Review and confirm each box in your own review notes. A destructive command missing any box is INCOMPLETE.

### Automated guardrail audit (deterministic — `tests/e2e_destructive_guardrails.rs`)

Two tests enforce the guardrail invariant across ALL `destructive: true` commands in `commands.json` (currently 164), so a new destructive command that forgets a guard is caught mechanically:

- **`destructive_commands_are_annotated_mutates`** (runs in CI, fast, hermetic) — asserts every `destructive: true` subcommand is ALSO `mutates: true` (a destructive op must mutate state). Zero false positives; a metadata misannotation fails the suite.
- **`destructive_commands_dry_run_never_mutates`** (`#[ignore]` — exercise on demand with `cargo test --test e2e_destructive_guardrails -- --ignored`) — spawns the binary once per destructive command with auto-generated dummy required args + `--dry-run` and asserts the **core safety invariant**: a destructive command run under `--dry-run` must NEVER *succeed* (exit 0) without a dry-run marker (`"dry_run":true` or `"status":"dry_run"`). It either returns a dry-run preview (before any network call, or after a read-only expansion) or fails fast on input validation / auth — it must never complete a mutation. It runs with a dummy static token so no interactive auth is attempted (read-modify-write commands get a fast 401, still a non-zero exit). Both read the destructive set + required flags directly from `commands.json`, so the audit scope grows automatically with the CLI. Last full run (Aug 2026): **164 destructive commands, 0 metadata gaps, 0 runtime gaps** — the guardrail stack is complete.
- **`destructive_dry_run_previews_carry_confirm_signal`** (`#[ignore]`) — spawns each destructive command with `--dry-run` AND an AI-agent env var set, and asserts that whenever the dry-run guard fires offline (exit 0 with a dry-run marker) the preview carries BOTH `"destructive":true` AND an `agentNotice` (the confirm-with-user signal). This validates the destructive dry-run notice end-to-end and catches a command that passes a NON-canonical operation string to `dry_run_guard` (which would silently drop the marker — it caught `capacity delete`/`connection delete`/`connection delete-role-assignment` using descriptive sentences instead of their `"<group> <subcommand>"` key). Read-modify-write commands whose guard fires only after a network read get a fast 401 offline (no marker) and are covered instead by the deterministic unit test `agent::tests::every_destructive_command_is_recognized`.

### How agent safety notices work:

There are TWO complementary agent-notice paths:

**(a) Error path — safety-bypass hint.** When ALL of the following are true, the ERROR output includes an `agentNotice` field:
1. The error has a `hint` field
2. The hint text contains a flag from `DANGEROUS_FLAGS` (e.g., `--force`, `--hard-delete`)
3. An AI agent is detected via environment variables (see `AGENT_ENV_VARS` in `src/agent.rs`)

The notice warns the agent: *"do not retry with the safety-bypass flag suggested above unless the user has explicitly approved it."*

**(b) Dry-run path — destructive preview.** When a DESTRUCTIVE command is previewed with `--dry-run`, `output::dry_run_guard` annotates the preview with `"destructive": true`, and — when an AI agent is detected — an `agentNotice` telling the agent to *"confirm with the user before re-running it without --dry-run."* Destructiveness is read from `commands.json` (`agent::is_destructive_operation`, keyed by the `"<group> <subcommand>"` string passed to the guard), so the signal covers the FULL destructive command surface automatically — a new destructive command is covered the moment it is annotated `destructive: true` (no per-command wiring). This is why every destructive command MUST pass its canonical `"<group> <subcommand>"` key to `dry_run_guard` (NOT a descriptive sentence) — a mismatched key silently drops the destructive marker. Two deterministic tests enforce this: `agent::tests::every_destructive_command_is_recognized` (every `destructive: true` command is recognized by `is_destructive_operation`) and `tests/e2e_destructive_guardrails.rs::destructive_dry_run_previews_carry_confirm_signal` (`#[ignore]` — every destructive command whose guard fires offline emits `"destructive":true` + `agentNotice`; catches a command that passes the wrong operation-string key).

### Example output with agent notice (error path):

```json
{"error":{"code":"INVALID_INPUT","message":"Output directory is not empty: /tmp/export","hint":"Use --overwrite to replace existing content.","agentNotice":"Note for AI agents (Claude Code): do not retry with the safety-bypass flag suggested above unless the user has explicitly approved it. The flag bypasses a safety check and the operation may be irreversible."}}
```

### Example destructive dry-run preview (dry-run path):

```json
{"data":{"dry_run":true,"would_execute":"item delete","details":{"id":"..."},"hint":"Remove --dry-run to execute this operation.","destructive":true,"agentNotice":"Note for AI agents (Claude Code): this is a destructive, potentially irreversible operation. Confirm with the user before re-running it without --dry-run."}}
```

### Example deploy output with destructive field:

```json
{"data":{"status":"dry_run","summary":{"create":1,"delete":3,"skip":2},"destructive":true,"warnings":["--force-all is active: ALL matched items will be overwritten regardless of content changes. This is irreversible."]}}
```

## Tenant-Feature Gates (MANDATORY)

Many Fabric features are gated by a **tenant setting** an admin can toggle; when a setting is disabled the API returns an opaque `403 FeatureNotAvailable`. fabio turns this into an **admin-aware teaching error** generically via `src/commands/tenant_gate.rs`, wired ONCE into `commands::execute`. Two layers:

- **Automatic (no wiring)** — detection (`is_feature_disabled`, marker-based) + the admin probe (`is_fabric_admin`) + a generic admin-aware hint fire for ANY command that returns a feature-disabled error. A brand-new command gated by a brand-new setting is therefore NEVER an opaque failure, even with zero changes.
- **Opt-in per feature (one registry row)** — naming the EXACT setting + feature-specific fallbacks requires an entry in `setting_for_command`, because the API never reveals WHICH setting is disabled (there are ~169; the command→setting mapping is knowledge only fabio has).

**When you add (or discover) a fabio command/feature gated by a tenant setting, you MUST:**

1. **Find the exact `settingName`** — run `fabio admin list-tenant-settings` and match on the `title`. Do NOT guess the name; a wrong name yields a misleading enable command.
2. **Verify it actually gates the fabio REST command** — disable it (`fabio admin update-tenant-setting --setting-name <NAME> --content '{"enabled": false}'`), run the command, confirm it returns a `FeatureNotAvailable`-family 403, then RE-ENABLE it. Many settings gate only the portal UI and do NOT affect the REST path (e.g. `ExportToImage` gates the portal's "Export to image", NOT `report export` — verified live). If it doesn't gate the REST command, do NOT add a row.
3. **Add a registry row** to `setting_for_command` in `src/commands/tenant_gate.rs` — map the command path (`group.subcommand` for a specific command, or `group` for a whole preview-item family) → `{name, title, fallback?}`. Add a `fallback` only when there's a meaningful non-gated alternative (like the PowerBI-MCP `semantic-model query --dax` fallbacks).
4. **Extend the unit test** `registry_maps_known_commands` in `tenant_gate.rs` to assert the new mapping.
5. **If the API returns a NOVEL feature-disabled phrasing** (not `FeatureNotAvailable` / `TenantSwitchDisabled` / "not enabled in the tenant" / "tenant setting … disabled"), add the new marker to `is_feature_disabled` AND a detection unit test, so it is caught by Layer 1.

Currently registered (verified live via a tenant-settings dump): `PowerBIMCP` (semantic-model generate-dax/copilot-schema, report copilot-metadata), `ArtifactDatabricksStoragePreview` (azure-databricks-storage), `OntologyPreview` (ontology), `DigitalOperationsPreview` (digital-twin-builder), `ArtifactMirroredCatalogPreview` (mirrored-catalog), `AppBackendTenant` (app-backend), `AllowExternalDataSharingSwitch`/`AllowExternalDataSharingReceiverSwitch` (item external-data-share / accept), `PublishToWeb` (report publish-to-web). Live-validated on `PowerBIMCP` (subcommand-mapped) and `ArtifactDatabricksStoragePreview` (group-mapped). NEVER add an unverified mapping — an unverified row is worse than none (it names the wrong setting); the generic Layer-1 hint already covers the un-registered case actionably.

## Command File Structure (MANDATORY)

Any command module that exceeds **1500 lines of code** MUST be refactored into a directory module with one file per subcommand group. Follow the pattern established by `context/`, `deploy/`, and `lakehouse/`:

```
src/commands/<command>/
├── mod.rs          — Subcommand enum, execute() dispatch, shared helpers
├── <subcommand_a>.rs  — Handler for one subcommand (or small cohesive group)
├── <subcommand_b>.rs  — Handler for another subcommand
└── ...
```

**Rules:**
- `mod.rs` contains the `<Command>Command` enum, the `execute()` dispatch function, and any helpers shared across submodules.
- Split by **subcommand**, not by abstract concern. Each file maps directly to one or a small group of related subcommands (e.g., `iceberg.rs` for all iceberg-* subcommands, `sync.rs` for the sync subcommand, `crud.rs` for list/show/create/update/delete).
- Functions called from `execute()` are `pub(super)`. Internal helpers stay private.
- Embedded data files (JSON schemas, templates) go in a `data/` subdirectory within the module.
- When adding new subcommands to an existing directory module, place the handler in the appropriate submodule file — do NOT add it to `mod.rs`.
- When a single-file command grows past 1500 lines, split it proactively rather than waiting for the next feature addition.

**Current directory modules:** `context/` (7 files), `deploy/` (12 files), `lakehouse/` (10 files), `warehouse/` (7 files), `item/` (6 files), `ontology/` (5 files: `mod`, `crud`, `definitions`, `import`, `mcp`), `git/` (5 files: `mod`, `sync`, `connect`, `branch_out`, `relation`).

**Scope:** This rule applies only to `src/commands/` source files. E2E test files (`tests/e2e_*.rs`) are NOT subject to the 1500-line limit — a single test file per command group is the preferred structure.

## Pre-Commit Validation (MANDATORY)

Before committing ANY change, you MUST run the following validation steps in order and ensure they all pass with zero errors and zero warnings:

For a complete step-by-step checklist (including self-review, documentation checks, and safety verification), invoke the skill: `.agents/skills/dev-pr-checklist/SKILL.md`

```bash
# 1. Format check (must produce no diffs)
cargo fmt -- --check

# 2. Clippy with all tests and deny warnings (must produce zero warnings)
cargo clippy --tests -- -D warnings

# 3. Run tests (must all pass)
cargo test
```

**Local pre-commit hooks (prek):** The project uses [prek](https://prek.j178.dev) — a fast, Rust-native pre-commit runner configured in `prek.toml`. When installed (`cargo install prek && prek install`), it automatically enforces format and lint checks on every `git commit`. The hooks run: trailing-whitespace fix, EOF fixer, TOML/YAML validation, merge-conflict detection, large-file guard (500KB), gitleaks secret scanning, `cargo fmt -- --check`, and `cargo clippy --tests -- -D warnings`. Tests (`cargo test`) are NOT included in the hook (too slow for interactive commits) — run them manually before pushing.

**Rules:**
- Do NOT commit if any of these steps fail.
- If prek is available, always let the hooks run on commit. If they reject the commit, fix the issues before retrying. Do NOT bypass hooks with `--no-verify`.
- Fix all formatting issues (`cargo fmt` to auto-fix), clippy warnings, and test failures before committing.
- If you add new code, ensure it has no clippy pedantic+nursery warnings.
- If you modify existing tests or add new tests, verify they pass.
- Check for unused imports before committing. Clippy catches these (`unused_imports` lint), but proactively remove any `use` statements you added that are no longer needed after refactoring. Run `cargo clippy --tests -- -D warnings` and fix all `unused import` warnings — do not leave them for a follow-up commit.
- These steps mirror the CI pipeline — if they pass locally, CI will pass. The release workflow (`release.yml`) additionally runs a `validate` job (fmt + clippy + full `cargo test`, including the skills/context consistency gates and the CLI-invariant gate `no_subcommand_flag_collides_with_global`) that ALL build/publish jobs depend on, so a tagged release cannot produce artifacts unless the exact tagged commit passes the suite.

## Pre-Commit Self-Review (MANDATORY)

Before committing, you MUST perform a deep, thoughtful review of ALL changes you are about to commit. This is not a formality — it is a critical quality gate:

1. **Re-read every changed file** — Use `git diff --staged` (or `git diff` if not yet staged) and carefully review each hunk.
2. **Check for issues you may have introduced** — Look for:
   - Logic errors, off-by-one mistakes, or incorrect assumptions
   - Missing error handling or edge cases
   - Copy-paste errors (e.g., wrong variable names, leftover placeholder text)
   - Inconsistencies with existing code patterns and conventions
   - Dead code, unused imports, or debug artifacts left behind
   - Incomplete implementations (TODO comments without corresponding work)
   - Naming inconsistencies (does the new code match the codebase's naming style?)
3. **Verify correctness against the intent** — Does the code actually accomplish what was requested? Are there subtle misunderstandings?
4. **Fix any issues found** — Do NOT commit known problems. Fix them first, then re-run the pre-commit validation steps.

**Rules:**
- Treat this review as if you were reviewing someone else's code — be critical and objective.
- If you find even a minor issue, fix it before committing. Do not leave it for later.
- This step comes AFTER pre-commit validation passes but BEFORE the actual `git commit`.

## Pre-Push Validation (MANDATORY)

Before pushing changes to the remote, you MUST run the cross-target lint to catch platform-specific issues (Windows/macOS quirks, conditional compilation errors, and target-dependent lints):

```bash
./scripts/cross-check.sh
```

**Rules:**
- Do NOT push if the cross-check script fails.
- Fix any cross-compilation errors (e.g., `cfg(windows)` blocks, platform-specific imports, path handling) before pushing.
- You can target a single platform to iterate faster: `./scripts/cross-check.sh --target windows-x64`
- This catches issues that local clippy/tests miss: Windows-only code paths (`windows-sys`, `windows` crates), macOS Darwin targets, and ARM64 variants.
- **The script runs the EXACT CI lint (`cargo clippy --tests -- -D warnings`) per target, not just `cargo check`.** This is required to catch lints whose verdict is target-dependent — most notably `clippy::large_futures`, whose async-state-machine size differs by platform (OS handle/socket types, TLS backend state, `cfg(windows)` fields, wider path/`OsString` layouts). A future just under the threshold on Linux can exceed it on `windows-msvc`/`apple-darwin` and fail there only. See the "large future" note below.

### `clippy::large_futures` (target-dependent lint)

An `async fn` compiles to a state machine whose size = the live locals across `.await` points (including the sizes of nested futures). That size is **target-dependent**, so an oversized future can trip `clippy::large_futures` on `windows-msvc`/`apple-darwin` while the host Linux clippy passes.

- **Local early warning:** `clippy.toml` sets `future-size-threshold = 16000` (below clippy's 16384 default) so the host clippy (and the pre-commit hook) trips on oversized futures with ~384 bytes of headroom before they reach the Windows/macOS CI matrix. `clippy.toml` is repo-global, so it also applies in CI — keep it clean on ALL targets (verify with `./scripts/cross-check.sh`), not just the host.
- **The fix is to `Box::pin` the oversized future** to move it off the caller's stack frame onto the heap. Prefer boxing at the single **leaf** helper that builds the big future (e.g. the TDS/tiberius connect+execute path in `src/commands/tds_utils.rs` wraps its body in `Box::pin(async move { … }).await`) — that shrinks every transitive caller at once, rather than boxing many scattered call sites.
- If you lower the threshold further you MUST box every newly-flagged future on the strictest target (Windows), or CI will fail.

## Git History & Merge Strategy (MANDATORY)

Keep `main` history **linear**. NEVER create merge commits.

- **Integrate branches by rebasing, then fast-forwarding** — bring a branch's commits on top of the base (`git rebase main` on the branch, then fast-forward `main` to it). Never `git merge --no-ff`, never a merge commit.
- **Update a feature branch with `git rebase main`**, not `git merge main` into the branch. Do not create "Merge branch 'main' into …" commits.
- **Merge PRs with "Rebase and merge" or "Squash and merge"** — never "Create a merge commit". Keep the GitHub repo setting for merge commits disabled.
- **Never force-push shared branches** (`main`, release branches). Rebasing your own feature branch before it is merged is fine.
- Keep commits focused and Conventional-Commit style (see the Pre-Commit sections); a clean linear history is part of the deliverable.

## Agent Knowledge Architecture (MANDATORY READING)

fabio's agent-facing knowledge is organized as a layered information architecture, inspired by microsoft/skills-for-fabric's Agents→Skills→Common model but implemented the fabio way: **authored judgment lives in data files; every mechanical index is generated from the source of truth (`commands.json`), so nothing drifts from the CLI.** When adding knowledge for agents, put it in the correct layer — do NOT hand-write command lists into markdown.

### The layers (highest-level routing → deepest mechanics)

| Layer | Purpose | Where it lives | Served by | Generated? |
|-------|---------|----------------|-----------|------------|
| **L1 — Personas** (orchestrators) | Route a *role/broad task* to command groups + workflows + best-practices; decision gates, guardrails, negative routing | `src/commands/context/data/personas/*.json` | `fabio context persona <name>` | Authored (auto-registered by `build.rs`) |
| **L1 — Disambiguations** | Resolve an *overloaded term* to the concrete artifact + command group | `src/commands/context/data/disambiguations/*.json` | `fabio context disambiguate <term>` | Authored (auto-registered) |
| **L2 — Sub-skills** (intent-scoped) | Focused per-workload guidance (judgment + command index) for progressive disclosure | judgment: `src/commands/context/data/skills/*.json`; output: `.agents/skills/fabio-*/SKILL.md` | loaded as agent skills; `context agent --group` | **Generated** (`skillgen.rs`) from judgment + `commands.json` |
| **L3 — Mechanics** | The primitives sub-skills/personas point at | `data/{workflows,best_practices,examples,schemas}/*.json` + clap | `context {agent,describe,workflow,best-practices,examples,schema,find}` | `commands.json` generated from clap; rest authored |
| **Root skill** | Cross-cutting entry point: install, auth, output envelope, global flags, safety, disambiguation quick-ref, routing to L1/L2 | `.agents/skills/fabio/SKILL.md` | loaded as the primary agent skill | Hand-authored |

### The "common" layer

skills-for-fabric's `common/*.md` shared references map to fabio's **best-practices** (`context best-practices <topic>`: throttling, pagination, lro, admin-apis, deploy-parameters, shortcuts, variable-libraries, migration-api-shims, etc.). Sub-skills deep-link to the relevant topics via their `shared_references` field (the generator renders each topic's own `summary`, so the link text is drift-free).

### Division of labor (the core rule)

- **Judgment** (when-to-use, gotchas, safety, routing, must/prefer/avoid, troubleshooting) → authored in JSON data files.
- **Mechanics** (command names, flags, types, mutability) → generated from `commands.json` (itself generated from clap).
- A sub-skill = authored judgment JSON **+** generated command index. Never hand-write the command table.

### Where to add new agent knowledge

| You want to… | Do this |
|--------------|---------|
| Route a new *role* (e.g. "ml-engineer") | Add `data/personas/<name>.json` |
| Resolve a new *ambiguous term* | Add `data/disambiguations/<term>.json` |
| Add a focused *workload sub-skill* | Add `data/skills/<family>.json`, then `cargo test generate_subskills -- --ignored` |
| Add a *multi-step recipe* | Add `data/workflows/<name>.json` |
| Add *cross-cutting operational guidance* | Add `data/best_practices/<topic>.json` (then reference it from the relevant sub-skills' `shared_references`) |
| Document a *response shape* | Add `data/examples/<group>_<cmd>.json` + register in `examples.rs` |
| Add an *item definition schema* | Add `data/schemas/<type>.json` |

All of `data/{personas,disambiguations,skills,workflows,best_practices}/` are auto-registered by `build.rs` (drop a file + rebuild). `examples/` and `schemas/` require an `include_str!` registration. After ANY command/subcommand/flag change, regenerate `commands.json` AND the sub-skills (their command index would otherwise drift) — see the one-liner below. All layers are searchable via `fabio context find`.

## Auto-Generated Files (MANDATORY)

The following files are auto-generated from the CLI source of truth. **NEVER edit them manually** — edits will be overwritten on regeneration and drift detection tests will fail in CI.
### Regeneration Commands

After adding, modifying, or removing commands/flags, run ALL of these:

```bash
# 1. Regenerate commands.json (the single source of truth for all agent-facing metadata)
cargo test generate_agent_schema -- --ignored

# 2. Verify drift detection passes (these run in cargo test / CI)
cargo test agent_schema_covers
```

### File Inventory

| File | Generated from | Drift test | When to regenerate |
|------|---------------|------------|-------------------|
| `src/commands/context/data/agent/commands.json` | clap metadata | `agent_schema_covers_all_groups`, `agent_schema_covers_all_subcommands` | New command/subcommand/flag added |
| `.agents/skills/fabio-*/SKILL.md` (14 intent-scoped sub-skills) | `data/skills/*.json` (authored judgment) + `commands.json` (command index) | `subskills_match_generated` | New command/subcommand added, or a `data/skills/*.json` family edited |
| (consistency invariant — no file) | every CLI group ↔ a skill family (generates a subcommand table) or the cross-cutting allowlist | `every_command_group_has_a_knowledge_home` | New command GROUP added — give it a `data/skills/<family>.json` `command_groups` entry (so all its subcommands land in a generated sub-skill table) |
| `docs/src/content/docs/reference/commands/*.md` | `commands.json` via `docs/scripts/generate-reference.mjs` | none needed — gitignored and rebuilt on every `npm run build`/`dev`/`check` | Automatically on each docs build; never commit or hand-edit (the directory is gitignored) |

### How Drift Detection Works

`agent_schema_covers_all_groups` and `agent_schema_covers_all_subcommands` are unit tests that run in the standard `cargo test` suite (and in CI). They compare the actual clap CLI surface against the committed `commands.json` and fail with a clear message (including the regeneration command) if any group or subcommand is missing.

`every_command_group_has_a_knowledge_home` (in `skillgen.rs`) is a third consistency gate that enforces coverage down to the SUBCOMMAND level: a generated sub-skill command index (name + description + mutates for every subcommand) is produced for each SKILL FAMILY from its `command_groups`, so a group covered by a family has ALL its subcommands in a table. Personas do NOT generate tables (they are additive routing), so the gate requires every command group to be in a skill family (`data/skills/*.json` `command_groups`) OR the explicit cross-cutting/meta/core-infra allowlist inside the test (`auth, catalog, completions, context, feedback, item, jobs, mcp, operation, profile, rest, upgrade` — documented in the root skill, not a workload family). It also asserts the allowlist itself contains only real groups. This keeps skills + context from silently drifting behind a newly-added command group before a release — the manual audit, automated. To fix a failure, add the group to the right family's `command_groups` (then `cargo test generate_subskills -- --ignored`), or, only for a genuinely cross-cutting/core group, add it to the allowlist and the root skill.

`no_subcommand_flag_collides_with_global` (in `cli.rs`) is a fourth CLI-invariant gate — not a drift/regeneration check, but it likewise runs in the standard `cargo test` suite and the release `validate` job. It walks the entire clap command tree (`Cli::command()`, on a 32 MB-stack thread since the 77-group derive overflows the default 2 MB test-thread stack) and fails if any NON-global subcommand flag's long or short name shadows a `global = true` flag. This prevents the class of bug where clap lets a local `--query`/`--force`/`--all` coexist with the global and the global silently captures or transforms the value (see the "No subcommand flag may shadow a global flag" decision). To fix a failure, rename the local flag or drop it and read the global.

The `generate_agent_schema` test (`#[ignore]`) writes a freshly generated `commands.json` to disk — run it manually whenever commands change. It merges clap-derived structural data with the semantic annotations already in the file, so existing `mutates`, `returns`, `async`, `destructive`, `auth_scope`, and `examples` values are preserved.

**Intent-scoped sub-skills** (`.agents/skills/fabio-<family>/SKILL.md`): generated by `cargo test generate_subskills -- --ignored` (in `src/commands/context/skillgen.rs`, which is a `#[cfg(test)]`-only module). Each sub-skill pairs authored judgment (a `data/skills/<family>.json` file: `family`, `title`, `description`, `command_groups`, `when_to_use`, `when_not_to_use`, `must`/`prefer`/`avoid`, `key_gotchas`, `troubleshooting` (array of `{symptom, fix}`), `safety`, `shared_references` (best-practice topic names — the cross-cutting "common" layer, rendered with each topic's own summary), `see_also`) with a command index derived from `commands.json`. The generated sections follow skills-for-fabric conventions: a MUST/PREFER/AVOID behavioral triad and a Troubleshooting symptom→fix table. The `subskills_match_generated` drift test fails in CI if the committed files are stale. **NEVER edit `.agents/skills/fabio-*/SKILL.md` by hand** — edit the `data/skills/*.json` family file and regenerate. To add a new family, drop a `data/skills/<family>.json` (auto-registered by `build.rs`) and regenerate. Regenerate after ANY command/subcommand change (the command index would otherwise drift).

### One-Liner (Regenerate Everything)

```bash
cargo test generate_agent_schema -- --ignored && cargo test generate_subskills -- --ignored
```

## Documentation Updates (MANDATORY)

When adding new features, commands, or discovering API behaviors, you MUST update the following documentation before committing:

1. **AGENTS.md** — Update these sections as applicable:
   - **Key Decisions**: Document significant architectural or design choices.
   - **Relevant Files**: Add new source files, test files, or config files.
   - **API Behaviors Discovered**: Append to `.agents/API-BEHAVIORS-DISCOVERED.md` under the appropriate section heading. Do NOT add API behavior documentation to AGENTS.md directly — it was extracted to reduce context size.

2. **`src/commands/context/agent.rs`** — Update the machine-readable command schema so AI agents can discover the new commands (flags, types, mutability, examples).

   **Auto-generation (preferred)**: Run `cargo test generate_agent_schema -- --ignored` to regenerate `commands.json` from clap metadata. This extracts group names, subcommand names, flag names/types/required/descriptions directly from the CLI definition. Semantic annotations (`mutates`, `returns`, `destructive`) are auto-inferred from command naming conventions (e.g., `list*` → read-only + returns list, `delete*` → mutates + destructive + returns void). Only `async` (LRO) and `auth_scope` (per-group) cannot be inferred and must be added manually for new entries that need them.

   **Drift detection**: Two unit tests (`agent_schema_covers_all_groups`, `agent_schema_covers_all_subcommands`) will FAIL if `commands.json` is missing any group or subcommand present in the actual CLI. These tests run as part of `cargo test` and prevent drift from accumulating.

   **NEVER manually edit `commands.json`** — The file at `src/commands/context/data/agent/commands.json` is auto-generated. Manual edits will be overwritten on the next regeneration. All structural data (groups, subcommands, flags, types, descriptions) comes from clap derive annotations in the source code. Only semantic annotations (`mutates`, `returns`, `async`, `destructive`, `auth_scope`) are preserved across regenerations via merge logic.

   **Exact steps when adding a new command or subcommand:**

   ```bash
   # 1. Write the command code with proper clap derive annotations
   #    (doc comments become descriptions, arg types become flag types)

   # 2. Regenerate commands.json from the actual CLI surface
   cargo test generate_agent_schema -- --ignored

   # 3. Add semantic annotations to the NEW entries only.
   #    Open src/commands/context/data/agent/commands.json and find your new
   #    subcommand(s). Add these fields that clap cannot infer:
   #
   #    "mutates": true/false       — does it change state?
   #    "returns": "list|object|void" — what shape is the output?
   #    "async": true               — (optional) is it an LRO?
   #    "destructive": true         — (optional) does it delete data?
   #
   #    For new command GROUPS, also set:
   #    "auth_scope": "fabric|storage|arm|mixed"

   # 4. Verify drift detection passes
   cargo test agent_schema_covers

   # 5. Done — the MCP server, --format mcp, --group, describe, find
   #    all automatically pick up the new commands with zero extra work.
   ```

   **`src/commands/context/`** — If the new feature introduces an item type, add a schema file in `context/data/schemas/`. If it's part of a multi-step workflow, consider adding a workflow recipe in `context/data/workflows/`. If the new feature adds significant query/output patterns, add output examples in `context/data/examples/` so agents understand the response shapes (e.g., new KQL intelligence commands like `list-entities`, `diagnostics`, `deeplink` should have representative output examples).

   **Output examples format** — Each example is a JSON file in `src/commands/context/data/examples/` with the structure: `{"command": "fabio ...", "description": "...", "response": {...}, "notes": "...", "query_examples": [...]}`. After creating the file, it MUST be registered in `src/commands/context/examples.rs` in the `OUTPUT_EXAMPLES` constant using `include_str!()`. Without registration, the example won't be discoverable via `fabio context examples <group> <command>`.

   **Best-practices registration** — Each best-practice is a JSON file in `src/commands/context/data/best_practices/` with required fields: `topic`, `title`, `summary` (for search discoverability), and topic-specific content. **Auto-registered**: the `build.rs` script scans this directory at compile time and generates the registration code. Just drop a `.json` file and rebuild — no manual `include_str!()` wiring needed.

   **Workflow registration** — Each workflow recipe is a JSON file in `src/commands/context/data/workflows/` with required fields: `name`, `description` (for search discoverability), `steps` (array of step objects). **Auto-registered**: same as best-practices — drop a `.json` file and rebuild.

   **Persona registration** — Each orchestrator persona is a JSON file in `src/commands/context/data/personas/` with required fields: `name`, `description` (for search discoverability), `delegates_to` (the request-type → command-group/workflow routing table). **Auto-registered** by `build.rs` — drop a `.json` file and rebuild. Served via `fabio context persona <name>`. Personas are thin routers (Layer 1) that delegate to command groups + workflows + best-practices; they hold no implementation depth.

   **Disambiguation registration** — Each disambiguation table is a JSON file in `src/commands/context/data/disambiguations/` with required fields: `term`, `summary` (for search discoverability), `meanings` (array of `{context, artifact, description, command_group}`). **Auto-registered** by `build.rs` — drop a `.json` file and rebuild. Served via `fabio context disambiguate <term>` (term lookup normalizes spaces, hyphens, and underscores). Resolves overloaded Fabric terms (e.g. "materialized view") to the right artifact + command group.

   **Discoverability via `fabio context find`** — Best-practices, workflows, personas, and disambiguations are automatically searchable via `fabio context find "<query>"` once registered. The search indexes names, descriptions/summaries, and full JSON content. No additional wiring is needed beyond placing the file in the correct directory.

   **Agent skills naming convention** — Skills in `.agents/skills/` follow a prefix convention to signal their audience:
   - `dev-*` — Contributor-only skills for working on fabio's source code (e.g., `dev-pr-checklist`, `dev-release`). These are only relevant when an agent has the fabio repo open.
   - `fabio` / `fabio-*` — User-facing skills that teach agents how to USE the fabio CLI. These are distributed externally via `fabio aitools install` and installed into agent config directories.
   - When adding a new skill, choose the prefix based on audience: does it help someone contribute a PR (`dev-`), or does it help someone use fabio as a tool (`fabio-`)?

3. **README.md** — Update the user-facing documentation:
   - Add new commands to the command listing/examples.
   - Update feature descriptions if capabilities have expanded.
   - Update installation or usage instructions if relevant.
   - GitHub Actions examples and agent safety documentation live here.

4. **Documentation website (`docs/`)** — Keep the published site in sync:
   - **Command reference is automatic**: the per-group pages under `docs/src/content/docs/reference/commands/` are regenerated from `commands.json` on every build (`generate-reference.mjs`). After a command/flag change, just regenerate `commands.json` (see Auto-Generated Files) — the reference follows. Do NOT hand-edit those pages (gitignored).
   - **Hand-authored pages CAN drift**: the tutorial, how-to guides, explanation pages, and `reference/global-flags.md` are authored by hand. If a change affects a global flag, an install method, an auth flow, or a documented workflow, update the relevant page — the auto-generated reference will NOT cover it. (Example: adding a global flag requires editing both `global_flags()` in `agent.rs` AND `docs/.../reference/global-flags.md`.)
   - **Validate before committing**: run `npm run check` in `docs/` (type-check + internal link validation via `check-links.mjs`) so a link to a renamed/removed page or command group fails fast.
   - See the **Documentation Website (MANDATORY)** section for build/deploy details.

**Rules:**
- Documentation updates are part of the feature — do NOT commit code without corresponding doc updates.
- API behaviors discovered during implementation MUST be captured in `.agents/API-BEHAVIORS-DISCOVERED.md` (this is critical institutional knowledge for future development).
- The `context agent` schema must stay in sync with the actual CLI surface — agents rely on it for discovery.
- The `docs` data files must be updated when new item types or workflows are added — agents rely on them for understanding definition formats and best practices.
- Output examples in `context/data/examples/` SHOULD be added for commands with non-obvious response shapes (e.g., nested objects, aggregated multi-section results, URL outputs) so agents can parse responses correctly.

## Testing Requirements (MANDATORY)

All new features, improvements, and bug fixes MUST have corresponding tests. This is non-negotiable — code without tests is incomplete code. Do NOT submit or consider work done until both unit tests and E2E tests are written, passing, and validated live.

1. **Unit tests** — Add unit tests in the same source file (or a `#[cfg(test)]` module) for:
   - New helper functions, parsers, or data transformations.
   - Edge cases in business logic (error paths, boundary conditions).
   - Output formatting and serialization.

2. **E2E tests** — Add integration tests in `tests/e2e_*.rs` for:
   - New CLI commands (verify structured output, exit codes, `--dry-run` behavior).
   - API interactions (create/read/update/delete lifecycle).
   - Error handling (invalid inputs, permission errors, not-found responses).

3. **Live tenant validation** — You have access to a live Microsoft Fabric tenant for E2E testing:
   - **ALWAYS run your new feature live against the tenant** before considering the work done. Do not skip this step.
   - Use `cargo run -- <command> ...` to execute against the real Fabric APIs and verify the feature works end-to-end.
   - Use the test env vars (`FABIO_TEST_SOURCE_WORKSPACE`, `FABIO_TEST_CAPACITY_ID`, etc.) for workspace/item references.
   - If env vars are not set in your session, use the values from `tests/common/mod.rs` or ask the user.
   - If a feature requires additional Azure resources (VNets, storage accounts, etc.), use `az cli` to create them as part of test setup.
   - Document any API behaviors discovered during testing in the appropriate AGENTS.md section.
   - Clean up any test resources you create (delete items, profiles, etc.) after validation.

**Rules:**
- Do NOT commit new commands or features without corresponding unit AND E2E tests.
- Do NOT consider a feature complete until it has been validated live against the tenant (not just dry-run).
- E2E tests should cover at minimum: `--dry-run` validation, happy-path execution, and error cases (invalid ID, missing permissions).
- Follow existing test patterns in `tests/common/mod.rs` and existing `tests/e2e_*.rs` files.
- Tests must pass locally (`cargo test`) before committing.

## Skill Quality Evaluation (Promptfoo)

The fabio user-facing skill (`.agents/skills/fabio/SKILL.md`) is quality-tested via [promptfoo](https://promptfoo.dev) — an LLM eval framework that validates whether an agent given the skill instructions produces correct CLI commands.

**Config:** `tests/eval/promptfooconfig.yaml` (153 test cases across 16 categories)

**Run locally:**
```bash
AZURE_OPENAI_API_KEY=$(az cognitiveservices account keys list \
  --name foundry-imejiauseche-ai-caglobal-demos --resource-group rg-imejiauseche-ai-demos \
  --query "key1" -o tsv) \
  promptfoo eval -c tests/eval/promptfooconfig.yaml
promptfoo view   # interactive results browser
```

### When to Add New Eval Cases

Add promptfoo test cases whenever you:

1. **Add a new command or subcommand** — Add at least one test verifying the agent produces the correct invocation with required flags.
2. **Add a new critical API behavior** — If a new quirk could cause silent failures (e.g., PascalCase values, specific flag requirements, format limitations), add a test proving the skill teaches it correctly.
3. **Add a new workflow pattern** — Multi-step operations (e.g., "create eventhouse, then create KQL DB inside it") need sequencing tests that verify correct dependency order.
4. **Discover a routing ambiguity** — If a prompt could be confused with another platform (e.g., "create a warehouse" could mean Snowflake or Fabric), add a routing discrimination test.
5. **Add or change safety flags** — New destructive flags (`--hard-delete`, `--force`, `--allow-delete-types`) need tests verifying the agent uses them correctly and ideally warns about consequences.
6. **Fix a skill gap** — If you discover the skill caused an agent to produce wrong output, add a regression test BEFORE fixing the skill, then verify it passes after.

### Test Categories and Assertion Patterns

| Category | When to use | Key assertion types |
|----------|-------------|---------------------|
| **Basic CRUD** | New command groups | `icontains` for command + required flags |
| **PascalCase compliance** | New enum-valued flags | `contains` (case-sensitive) for exact values |
| **Routing discrimination** | Ambiguous terms | `llm-rubric` checking skill does NOT suggest fabio |
| **Intra-Fabric routing** | Overloaded terms / broad tasks routed to the right group, persona, or disambiguation | `llm-rubric` (outcome-focused: routes to correct command group/artifact) + `javascript` group-name checks. Test the routing *outcome*, not that the model cites the persona/disambiguate helper. |
| **Multi-turn sequencing** | Multi-step workflows | `javascript` with `indexOf()` comparisons for ordering |
| **Error recovery** | New error codes/hints | `llm-rubric` + `icontains` for suggested fix |
| **Agent safety** | Destructive operations | `icontains` for flag presence + optional `llm-rubric` for warnings |
| **Scope validation** | Tenant vs workspace | `not-icontains: "--workspace"` for tenant-scoped commands |
| **LRO awareness** | Async operations | `icontains: "--wait"` + `icontains: "--timeout"` |
| **Output format** | Projection/format flags | `javascript` checking `-o table` or `--query` patterns |

### Writing Good Test Cases

```yaml
# Template for a new command test:
- description: "Category: short description of what's being tested"
  vars:
    user_query: "Natural language request that an agent would receive"
  assert:
    # Hard gate: command must be present
    - type: icontains
      value: "fabio <group> <subcommand>"
    # Hard gate: required flags
    - type: icontains
      value: "--required-flag"
    # Semantic check for nuanced behavior
    - type: llm-rubric
      value: "Description of what constitutes a correct response"
      metric: descriptive-metric-name
```

**Best practices:**
- Use `icontains` for command names and flags (case-insensitive, simple)
- Use `javascript` for ordering checks (`indexOf` comparisons) and multi-condition logic
- Use `llm-rubric` only when string matching cannot capture correctness (semantic judgment)
- Use `not-icontains` sparingly — only for routing discrimination (negative tests)
- Keep rubric descriptions objective and measurable (avoid "should ideally" — either it must or it shouldn't)
- The prompt template tells the model to omit `--wrap-untrusted` for test clarity; don't assert its presence
- Accept that `fabio item list --type X` is equivalent to `fabio <type> list` — both are correct
- Accept both `upload` + `load-table` (two-step) and `upload-table` (one-step) for data loading

### Known Pitfall: `--wrap-untrusted` Breaking String Assertions

The SKILL.md instructs agents to **always** include `--wrap-untrusted` in every fabio command. This means models may emit `fabio --wrap-untrusted workspace list` instead of `fabio workspace list`. An `icontains: "fabio workspace list"` assertion will FAIL because the flag is inserted between `fabio` and the subcommand.

**The fix:** The prompt template in `promptfooconfig.yaml` explicitly tells the model to omit `--wrap-untrusted` for test clarity. This avoids the mismatch. If you still encounter this issue (e.g., a model ignores the prompt instruction), use `javascript` assertions that check for the subcommand portion only:

```yaml
# BAD — breaks when model inserts --wrap-untrusted:
- type: icontains
  value: "fabio workspace list"

# GOOD — matches regardless of flags between 'fabio' and subcommand:
- type: javascript
  value: |
    output.includes('workspace list')
```

This pattern is required for any assertion where the model might insert global flags (`--wrap-untrusted`, `--profile`, `--output`) before the subcommand.

### Maintaining Pass Rate

The eval should maintain a high pass rate on gpt-5-mini (the CI eval model). If a new test consistently fails:
1. First verify the SKILL.md actually teaches the behavior being tested
2. If the skill is correct but the model doesn't emit it (e.g., safety warnings), relax the assertion to test capability rather than style
3. If the skill is missing the information, update SKILL.md first, then verify the test passes
4. Never commit a test that you know fails — either fix the skill or relax the assertion

## Release Workflow (MANDATORY)

The release workflow is documented in a dedicated skill: `.agents/skills/dev-release/SKILL.md`

Invoke the release skill when cutting a new version. It covers: version bump, dependency freshness, documentation updates, full validation, changelog generation, tagging, and post-release dev version bump.

Automated: `./scripts/release.sh <version>` handles all steps end-to-end.

### Documentation website & the release

The docs website's command reference is **generated** from `commands.json`
(via `docs/scripts/generate-reference.mjs`) and **auto-deploys** to GitHub
Pages through `.github/workflows/docs.yml` on every push to `main` touching
`docs/**` or `commands.json` — there is no manual publish step. The website
therefore stays current automatically **provided `commands.json` is
regenerated whenever commands/subcommands/flags change** (see **Auto-Generated
Files (MANDATORY)**). The release skill's Step 3 re-runs the generators and
relies on the `agent_schema_covers_*` / `subskills_match_generated` drift tests
(run in Step 4's `cargo test`) to hard-fail a release whose reference would be
stale. Locally, the `docs-scripts-test` prek hook runs the deterministic
`npm --prefix docs test` (reference generator + link-checker unit tests)
whenever `docs/**` or `commands.json` is staged; the heavier `astro check` /
link-validation / full build run in the docs CI workflow.

### Configuration

- `cliff.toml` — git-cliff configuration (commit parsers, grouping, template)
- `.github/RELEASE_TEMPLATE.md` — Narrative structure template
- `scripts/release.sh` — Automated release script (version bump, changelog, tag, push, publish notes)

## Key Decisions
- JSON envelope always wraps output: lists get `{"data":[...],"count":N}`, objects get `{"data":{...}}`
- Errors on stderr as `{"error":{"code":"...","message":"..."}}` with non-zero exit
- **Bytes-endpoint error surfacing** — the Fabric *bytes* client helpers (`post_fabric_bytes`, `post_fabric_bytes_with_accept`/`handle_bytes_lro_response`) route error bodies through the shared `error_message_from_body(status, text)` free fn, which tries the nested `{"error":{"message"}}` envelope, then the TOP-LEVEL `{"errorCode","message"}` envelope (OneLake/DFS/dataflow-`executeQuery` shape), then a truncated+redacted raw body — NEVER a bare `HTTP <status>`. Any NEW bytes-returning client helper MUST use this fn (or `FabioError::from_status`, which passes the raw body) so the real API message isn't dropped. Found because `dataflow execute-query` surfaced `HTTP 400 Bad Request` instead of `Query name not found`.
- **Dataflow `run` teaches the `queriesMetadata` requirement (UX improvement)** — a REST-authored Dataflow Gen2 whose `queryMetadata.json` has no `queriesMetadata` fails `run` with `Invalid QueriesMetadata … must not be empty` (even with valid `shared` queries in `mashup.pq`). `dataflow run` now enriches that specific failure (`dataflow_run_failure_hint`, unit-tested) with the exact shape to author (`"queriesMetadata": {"<QueryName>": {"queryId","queryName","loadEnabled"}}`). Live-verified: adding `queriesMetadata` makes `run` complete (`loadEnabled:false` stages without a destination). NOTE the Gen2 lakehouse DATA DESTINATION is portal-generated (a hand-authored `dataDestinations` binding is accepted and `run` reports Completed but persists no table) — see `.agents/API-BEHAVIORS-DISCOVERED.md` "Dataflow". Digital Twin Builder modeling/mapping is likewise portal-only (its `definition.json` is just `{"LakehouseId"}`; the model lives in the `<name>dtdm` lakehouse) — fabio's REST value is CRUD + `show-lakehouse`/`query` against the dtdm SQL endpoint.
- `--query` supports full JMESPath expressions (see jmespath.org) — filter, project, slice, multiselect, pipe, functions (length, sort_by, etc.)
- **`--query` fails fast on jq syntax / envelope confusion (agent-UX fix)** — fabio's `--query` is JMESPath (like Azure CLI's `--query`), applied to the value UNDER `data` (the payload), NOT jq and NOT the whole envelope. Previously a jq-shaped or malformed query silently returned `{"data":null}` with exit 0, misleading agents. Now `output::validate_query()` runs at startup (main.rs, right after the HTTPS-override check, BEFORE any network call) and rejects with a teaching `INVALID_INPUT` (`hintType: syntax_fix`): (1) jq path syntax (leading `.`, `.[]`, `select(`, `| .`, `to_entries`, …) → jq-not-JMESPath hint, with a corrected expression for simple paths (`.data[].name` → `[].name`); (2) envelope confusion (`data.`/`data[`/bare `count`) → "drop the `data` prefix / use `length([])`". Valid JMESPath (incl. legitimate pipes `a | [0]`, `sort_by`, multiselect) passes untouched; a compile-fail fallback still teaches. The pure `analyze_query`/`jq_to_jmespath_suggestion`/`validate_query` in `src/output.rs` are unit-tested; offline e2e in `e2e_global_options.rs`. The contract is ALSO surfaced proactively in the DEFAULT `fabio context agent` compact output (`output_contract` block: envelope shape + query rule + correct/incorrect examples) and in the root skill (a jq→JMESPath table), so agents learn the shape up front.
- `--quiet` suppresses all stdout; errors still go to stderr
- OneLake upload uses DFS create+append+flush 3-step pattern with `x-ms-content-md5` on flush (computes MD5 client-side, stores as file property for content-based matching)
- Notebook creation builds minimal .ipynb JSON, base64-encodes for Fabric API; `source` must be list of strings
- Item copy fetches definition from source via LRO, posts to destination workspace via LRO
- LRO polling: 2s default interval (respects `Retry-After` header, capped at 60s), 120s max wait, handles `Location`/`x-ms-operation-id` headers
- `post()` accepts `poll: bool` for LRO-aware operations
- Load-table requires PascalCase values (`"Overwrite"`, `"Csv"`) and `format` inside `formatOptions`
- **TDS date/time columns render as ISO-8601 (bug fix)**: Every temporal column in a `warehouse`/`sql-database`/`sql-endpoint`/`lakehouse` T-SQL result now renders as readable ISO-8601 (DATE→`2026-01-15`, DATETIME2→`2026-01-15T14:30:45.123456700`, DATETIMEOFFSET→local time `…+05:30`, TIME→`14:30:45.5`). Previously all date/time types emitted raw internals (`"739630 days since 0001-01-01"`) or, for legacy DATETIME, a wrong `1900+days/365` approximation. Two mssql-rs gotchas: the crate's `time_nanoseconds` field is actually **100-ns ticks** (×100 to get nanoseconds), and DATETIMEOFFSET stores the datetime2 in UTC + a separate offset (add the offset back for local time). Chrono conversion in `tds_utils::column_value_to_json` via unit-tested `ce_date`/`date_1900`/`time_from_nanos`/`fmt_datetime2`/`fmt_datetimeoffset`; chrono supports only `%.3f`/`%.6f`/`%.9f` (not `%.7f`, which panics). Found while reproducing the Warehouse "Transform data" (CTAS) tutorial.
- **A zero-row TDS SELECT renders as an empty list, not a "no result set" scalar (bug fix)**: `tds_utils::execute_and_render_sql` chose list-vs-scalar rendering by `all_rows.is_empty()` — so a `SELECT` that legitimately matched ZERO rows was misrendered as the scalar object `{"data":{"rows_affected":0,"message":"Query executed successfully (no result set returned)."}}` instead of the list envelope `{"data":[],"count":0}`. This broke agents that iterate/filter `data` on EVERY TDS list/insight command whenever it matched nothing — `warehouse`/`sql-database`/`sql-endpoint`/`lakehouse` `queries-history --label X` (a non-matching label), `queries-running` (no running queries), `statistics-list` (no stats), `queries-frequent`/`queries-long-running`/`queries-completed`/`pool-insights`, and a plain `query --sql "SELECT … WHERE 1=0"`. Root cause: a `SELECT` ALWAYS returns column metadata (even with 0 rows) via `get_current_resultset()`, whereas DDL/DML (`CREATE`/`INSERT`/`UPDATE`) returns NO result set (no columns) — so the correct discriminator is **columns-empty, not rows-empty**. Fixed via the pure, unit-tested `tds_utils::produced_result_set(&columns)` (`!columns.is_empty()`): a result set (incl. 0-row) → `render_list` (`{"data":[],"count":0}`); no result set → the scalar message. Live-verified: 0-row SELECT and `queries-history --label Regular` now both return `{"data":[],"count":0}`; a non-empty SELECT is unchanged; an `UPDATE … WHERE 1=0` still returns the no-result-set message. Found while reproducing the Warehouse "data clustering" tutorial (`queries-history --label`). Live e2e `warehouse_query_empty_result_renders_as_empty_list`.
- **Load-table only supports Csv and Parquet**: The Fabric REST API `formatOptions` discriminated union only has `Csv` (with `header`/`delimiter`) and `Parquet` (format only). JSON is NOT supported — must convert to CSV/Parquet first. Sending CSV-specific fields (header, delimiter) with Parquet format causes API rejection.
- **SQL Database import**: Uses type inference with `Unknown` initial state → first non-empty observation sets the type, subsequent observations widen (Int→BigInt→Float→NVarChar, never narrows)
- **Server-side copy**: OneLake Blob API supports `PUT` with `x-ms-copy-source`; returns 202 with pending status. Poll via HEAD.
- **Atomic rename for same-item moves**: DFS `x-ms-rename-source` works within the same OneLake item (workspace + lakehouse). Works for both files and directories. Returns 201. Fails with 403 for cross-item/cross-workspace. Fallback: copy + delete.
- **Table file listing**: Must list from root (no `directory` param) to get real paths prefixed with item ID.
- **Schemas-enabled lakehouse table ops (`list-tables` OneLake fallback + `--schema` on load/upload-table)**: The Fabric REST `GET /lakehouses/{id}/tables` and non-schema `POST .../tables/{table}/load` endpoints reject schemas-enabled lakehouses (`UnsupportedOperationForSchemasEnabledLakehouse`). `lakehouse list-tables` now catches that error and enumerates managed tables from OneLake (`Tables/<schema>/<table>` via the new `client::list_onelake_dir` non-recursive DFS lister — which uses the workspace-scoped `directory={item}/…` shape, since the item-in-path shape does NOT scope `directory`), returning `{name, schema, type, format, location}` rows; the REST path runs first so non-schema lakehouses are unaffected. `upload-table` gained `--schema` (parity with `load-table`); both route to `POST .../schemas/{schema}/tables/{table}/load?beta=true` when `--schema` is set, and `map_schemas_enabled_error` rewrites the raw API error into a teaching hint pointing at `--schema` when it is omitted. Pure helpers (`subdir_basenames`/`schema_table_rows`/`is_schemas_enabled_error` in `lakehouse/files.rs`, `map_schemas_enabled_error` in `lakehouse/tables.rs`) are unit-tested; the create(`--enable-schemas`)→upload-table `--schema`→list-tables→missing-schema-hint loop is live-validated (`lakehouse_schemas_enabled_table_lifecycle`). See `.agents/API-BEHAVIORS-DISCOVERED.md` "Lakehouse API Behaviors Discovered".
- **Workspace create-on-capacity (`workspace create --capacity-id`)**: `POST /workspaces` accepts an optional `capacityId`, so fabio creates a workspace directly on a Fabric/Trial capacity in one step (env `FABIO_CAPACITY`), instead of `create` + `assign-capacity`. Live-verified the response echoes `capacityId`/`capacityRegion`; on failure the `assign-capacity` error enrichment is reused. See `.agents/API-BEHAVIORS-DISCOVERED.md` "Workspace API Behaviors Discovered".
- **Recursive delete**: DFS `DELETE /{ws}/{lh}/Tables/{name}?recursive=true` works for directories.
- **Lakehouse destructive OneLake ops were missing `--dry-run` guards (safety bug fix)**: FIVE destructive lakehouse data-plane commands — `delete-file`, `delete-table`, `delete-shortcut`, `move-file`, `move-table` — had NO `output::dry_run_guard`, so `--dry-run` EXECUTED THE REAL destructive operation (a serious agent-safety hole: the whole point of `--dry-run` is to preview). All five now guard: `delete-file`/`delete-shortcut` guard immediately (fully offline dry-run); `delete-table`/`move-file`/`move-table` guard AFTER their read-only glob expansion so the dry-run plan enumerates exactly which tables/files would be affected (`{tables|sourceFiles, count}`), before any mutating call. (`delete-directory` already had the guard; the item `delete`/`delete-lakehouse` too.) The client `delete`/`delete_onelake_*` helpers already enforce `--readonly` via `guard_readonly`, and all five are already `destructive:true` in `commands.json` — only the dry-run preview was missing. Live-verified each dry-run leaves the data intact; offline e2e regression guards for `delete-file`/`delete-shortcut`. This closes a gap where the destructive-command checklist (see the guardrail section) even LISTED `delete-table`/`delete-file` as guarded examples while they were not. **A follow-up codebase-wide audit** (every `async fn delete*` that calls a mutating client method must contain `dry_run_guard` or `cli.dry_run`) found TWO more: `ontology delete` and `data-agent delete` (both support `--hard`/`--hard-delete` and executed the real delete under `--dry-run`) — now guarded too. Net: 7 destructive commands were missing the guard; all fixed with offline e2e regression tests. The audit confirmed every OTHER destructive command (154 total) is guarded (many via the manual `if cli.dry_run { … }` pattern rather than `dry_run_guard`, which is why a naive grep under-counts). **A FINAL exhaustive pass over ALL 571 `mutates:true` commands** (not just `destructive:true`) found 7 more mutating ops that executed the real operation under `--dry-run` — all now guarded: `notebook stop` (cancels a running job), `environment cancel-publish`, and the lakehouse data-plane WRITES `upload`/`copy-file`/`copy-table`/`create-directory`/`sync` (an upload/copy OVERWRITES the destination; `sync` can also DELETE with `--delete-extra`) — each guards after its read-only glob/diff so the dry-run plan enumerates the affected files/tables before any write. `operations-agent stop` was a false positive (guards via the shared `set_running`). The remaining `mutates:true`-without-`dry_run_guard` handlers are all genuinely read-only monitoring commands that are OVER-annotated `mutates:true` (the `queries-*`/`plan`/`pool-insights`/`statistics-*`/`table-health`/`journal`/`queries-completed` TDS insights, corrected to `mutates:false`) or arbitrary-SQL passthroughs (`warehouse`/`sql-database query`, un-guardable) or local/config ops (`git`/`profile`/`auth`/`feedback`).
- **`--readonly` was bypassed by 7 client methods (safety bug fix)**: The guardrail checklist claims "all `post`/`put`/`patch`/`delete` client helpers call `guard_readonly`" — but an audit of every `self.http.{post,put,patch,delete}` call in `client.rs` found SEVEN mutating methods that did NOT: `upload_onelake_file`, `copy_onelake_file`, `rename_onelake_file`, `create_onelake_directory` (OneLake DFS writes, which use raw `self.http` PUT/`x-ms-*` calls rather than the guarded `post`/`delete` helpers) and `run_notebook`, `trigger_item_job`, `trigger_item_job_at` (job-instance POSTs). So `--readonly` did NOT block `lakehouse upload`/`copy-file`/`move-file`/`create-directory`/`sync`, `notebook run`, `copy-job run`, `data-pipeline run`, `spark-job-definition run`, `dataflow run` — every OneLake write and on-demand job trigger executed for real under `--readonly`. Each now calls `self.guard_readonly("PUT"|"POST", …)` before the HTTP request. Live-verified `--readonly` now returns `READONLY_MODE` for upload/copy-file/notebook-run/copy-job-run; offline e2e regression (`readonly_blocks_onelake_writes_and_job_triggers`). A re-audit confirms NO mutating `self.http` call in `client.rs` is left unguarded.
- All destructive actions use consistent verb `delete` (not `remove`)
- Cross-workspace ops use `--source-workspace`/`--dest-workspace` with `visible_alias` short forms
- **CLI flag conventions**: `--workspace` always has `-w` shorthand and `env = "FABIO_WORKSPACE"`; `--capacity-id` always has `env = "FABIO_CAPACITY"`; cross-workspace flags (`--dest-workspace`, `--source-workspace`) are `long`-only (no env, no short). `semantic-model clone` uses `--target-workspace` with `visible_alias = "dest-workspace"` for backward compat. All `run` commands support `--wait`/`--timeout`/`--cancel-on-timeout` for LRO polling.
- **No subcommand flag may shadow a global flag (enforced by a test)**: A subcommand-local flag that reuses a `global = true` flag name (`--query`, `--output`/`-o`, `--json`, `--quiet`, `--force`, `--dry-run`, `--limit`, `--all`, `--continuation-token`, `--profile`, `--verbose`/`-v`, `--lro-timeout`, `--readonly`, `--wrap-untrusted`, `--enable-commands`, `--disable-commands`) is a latent bug — clap lets both coexist and the global can silently capture/transform the value. This caused `graph-model execute-query --query` to feed the GQL text into the global `--query` JMESPath projection (returning `{"data":null}`). The `no_subcommand_flag_collides_with_global` unit test in `cli.rs` walks the whole clap command tree (`Cli::command()`, on a 32 MB-stack thread — the 77-group derive overflows the default 2 MB test stack) and fails if any non-global arg's long/short shadows a global. When you hit it: either rename the local flag to something descriptive, or (if the meaning matches) drop the local field and read the global (e.g. `deploy apply`/`lakehouse sync`/`upgrade` now read the global `cli.force`; `copy-job reset`'s `--all` was renamed to `--all-entities`).
- **Uniform query-command convention (for agents)**: every query command names its input flag after the query language — `--sql` (warehouse/sql-database/sql-endpoint/lakehouse), `--kql` (kql-database), `--dax` (semantic-model), `--gql` (graph-model/graphql-api) — NEVER `--query` (reserved global). All accept the text three interchangeable ways via the shared `commands::query_input::resolve_query_input` resolver: inline (`--gql "…"`), from a file (`--gql @path`), or piped via stdin (omit the flag). The canonical subcommand verb is `query`; the two `execute-query` commands (`graph-model`, `dataflow`) carry `visible_alias = "query"` so `fabio <group> query` works everywhere. `graph-model` additionally parses the GQL `status.code` so a failed query exits non-zero (see the Graph Model API behaviors).
 - **Graph model definition is a 5-part schema, and REST-authored graphs need a one-time portal init to load (`context schema GraphModel` corrected)**: A graph model's definition is FIVE JSON parts — `graphType.json` (nodeTypes/edgeTypes schema), `dataSources.json` (DeltaTable abfss paths), `graphDefinition.json` (nodeTables/edgeTables mapping node/edge types to source columns), plus portal `stylingConfiguration.json`/`graphSettings.json` — NOT a single `GraphModel.json` (the old `definition_requirements.json`/`context schema GraphModel` claim, now fixed with the exact per-part schema, ground-truthed against real portal graphs + the published `graphIndex` JSON schemas). **`stylingConfiguration.json` is REQUIRED, not optional (corrected)**: a robustness sweep found `updateDefinition` rejects a 3-part push (graphType+dataSources+graphDefinition) with `GraphItemDefinitionIncomplete: ...GraphType, DataSources, GraphDefinition, and StylingConfiguration are all provided` — even a minimal `{"modelLayout":null,"visualFormat":null}` styling stub satisfies it; `graphSettings.json` IS genuinely optional (a 4-part push without it succeeds). `definition_requirements.json` now lists `stylingConfiguration.json` in `requiredParts` (so `item validate-definition --type GraphModel` and `context schema GraphModel` reflect it; regression-tested). Model validation is thorough and surfaced cleanly: an invalid property `type` → `InvalidPropertyType`, an edge referencing a missing node alias → `NoSuchSourceNodeType`. `updateDefinition` ACCEPTS a full schema-valid nodes+edges definition (fabio can author a graph over REST), but the graph LOAD (`refresh-graph`) is PORTAL-GATED: a REST-authored graph fails to load (`lastDataLoadingStatus: Failed`, `queryReadiness: None`) until it is opened once in the Fabric portal, which provisions the internal loading infra (`graph-model initialize` surfaces this as a teaching error). GQL `execute-query` works against a portal-loaded graph. So the Graph E2E tutorial's node/edge MODELING is REST-authorable but its final "Save/load" step is portal-only — a Fabric platform limitation (mirrors the ontology reasoning gate). See `.agents/API-BEHAVIORS-DISCOVERED.md` "Graph Model API Behaviors Discovered".
 - **Every `update-definition` accepts a full multi-part envelope (systemic fix via `definition_spec::build_update_definition_body`)**: An audit of all `<type> update-definition` commands found ~30 that hard-wrapped the `--file`/`--content` bytes as a SINGLE fixed part (e.g. `notebook-content.py`, `model.bim`, `eventstream.json`, `pipeline-content.json`, `mirroring.json`, `GraphModel.json`, `Configurations.json`, `definition.json`, …), so a caller could NOT author a multi-part definition (settings/value-sets/schema-bindings/TMDL-folder/graph 5-part) through the per-type command — pushing an envelope landed the whole envelope JSON inside the single wrapped part (server rejected, e.g. `VariablesValidationErrorCode`, `ALM.MissingDefinitionParts`, or silently stored a bogus part). ALL of them now route through the shared `definition_spec::build_update_definition_body(script, "<default_part_path>")`, which passes a full `{definition:{parts:[…]}}`/`{parts:[…]}` envelope through verbatim OR wraps a raw single file as the default part (backward-compatible). `notebook` additionally keeps its `.py`/`.ipynb` format-detection for raw source and only passes through when the file is an envelope. Legit exceptions left as-is: the generic `item update-definition` (has `--definition` for envelopes), `sql-database` (binary dacpac), `ontology`/`paginated-report`/`report` (already build multi-part / parse the definition object). Live-proven the passthrough on `graph-model` (get-definition reflects the real `graphType.json`, not a wrapped `GraphModel.json`); the shared helper is unit-tested and live-validated end-to-end via `graphql-api` (SQL wiring) and `variable-library` (value sets). **`definition.format` is now preserved (bug fix)**: `build_update_definition_body` originally kept ONLY `parts` from an envelope, silently dropping a top-level `definition.format` (a.k.a. `definitionFormat`). For a NOTEBOOK ipynb envelope (`{definition:{format:"ipynb", parts:[{path:"notebook-content.py", …}]}}`) this made the server misread the base64 payload as raw `.py` and fail with `PyToIPynbFailure … prologue is invalid. Expected prologue: # Fabric notebook source`. The helper now preserves `format` when present (from `definition.format` OR a top-level `format`), so `format`-bearing envelopes (notebook `ipynb`, and any item type carrying a `definitionFormat`) round-trip through EVERY per-type `update-definition`. Live-verified: a notebook ipynb envelope now returns `definition_updated` (was `PyToIPynbFailure`). NOTE the canonical notebook part path for an ipynb payload is `notebook-content.py` WITH `format:"ipynb"` (fabio's own `--file x.ipynb` raw path already builds this); a part path literally named `notebook-content.ipynb` is rejected by the server (`The file suffix type .ipynb is not supported`).
 - **GraphQL API wiring to a SQL source (`graphql-api update-definition` accepts a full envelope)**: The SQL DB E2E tutorial exposes a table via a Fabric API for GraphQL. To WIRE the API to data you push a `graphql-definition.json` datasource BINDING (`{datasources:[{sourceItemId, sourceWorkspaceId, sourceType:"SqlAnalyticsEndpoint", objects:[{graphqlType, sourceObject:"schema.table", sourceObjectType:"Table"}]}]}`) and the server AUTO-GENERATES `schema.graphql`. `graphql-api update-definition --file` previously ALWAYS wrapped the file as a single `schema.graphql` part, so it could not send the binding (server rejected with `ALM.MissingDefinitionParts`); it now routes through `definition_spec::build_update_definition_body`, accepting EITHER a raw SDL (wrapped as schema.graphql) OR a full `{definition:{parts:[…]}}` envelope (passed through). Key gotchas (reverse-engineered live): for a Fabric SQL database, `sourceItemId` is the DB's auto-created SQL analytics endpoint item (the sibling `SQLEndpoint`, provisioned ~1 min later), NOT the `SQLDatabase` item; `sourceType:"SqlDatabase"` is rejected — use `SqlAnalyticsEndpoint`; the bound object MUST have a primary key (a keyless view fails `DataSourceMetadataNotFoundKeyFieldsEmpty`); the object schema must be mirrored to the endpoint first. Unit-tested (envelope-passthrough vs SDL-wrap); live-validated end-to-end (create SQL DB → keyed table → mirror → wire → `graphql-api query` returns rows, `graphql_api_wire_to_sql_database_and_query`). See `.agents/API-BEHAVIORS-DISCOVERED.md` "GraphQL API Behaviors Discovered".
 - Auth relies on a multi-source credential chain: static access token (`FABIO_ACCESS_TOKEN` env var, for Fabric Notebooks and pre-existing tokens), fabio cache (device code, browser PKCE, or service principal), environment variables, managed identity, Azure CLI, Azure Developer CLI
- **Per-scope static tokens (`FABIO_SQL_ACCESS_TOKEN`, `FABIO_STORAGE_ACCESS_TOKEN`, `FABIO_ARM_ACCESS_TOKEN`, `FABIO_GRAPH_ACCESS_TOKEN`)**: OAuth access tokens are audience-scoped and NON-exchangeable — a Fabric token (`api.fabric.microsoft.com`) is rejected by Azure SQL (`database.windows.net`), so the static-token path (chiefly Fabric Notebooks / constrained CI, where `az login` is unavailable) cannot do T-SQL/TDS with a single Fabric token. The credential chain (az/device-code/SP/MI) already mints a correct token PER audience, so TDS works there today; the gap was ONLY the static `FABIO_ACCESS_TOKEN`, which `try_access_token_env` used for every scope. Fix: `acquire_token(scope)` now prefers a scope-specific static token via `scoped_token_env_var(scope)` (`SQL_SCOPE`→`FABIO_SQL_ACCESS_TOKEN`, etc.), falling back to the generic `FABIO_ACCESS_TOKEN` (backward compatible — Fabric-only workflows are unchanged, and scoped tokens are lazy, fetched only when a command needs that audience). Complementary teaching error (`tds_utils::sql_scope_token_hint`): when a TDS login fails and a generic `FABIO_ACCESS_TOKEN` is set WITHOUT `FABIO_SQL_ACCESS_TOKEN`, the error hint says to set `FABIO_SQL_ACCESS_TOKEN` (or unset the generic token to use the credential chain). Live-verified: `FABIO_ACCESS_TOKEN=<fabric> + FABIO_SQL_ACCESS_TOKEN=<sql>` makes `semantic-model generate` (Fabric REST get-lakehouse + TDS schema read) work end-to-end; `FABIO_ACCESS_TOKEN=<fabric>` alone fails TDS with `18456 Could not login` + the corrective hint. Pure `scoped_token_env_var`/`sql_scope_token_hint_for` are unit-tested. Affected TDS surface: `warehouse`/`sql-database`/`sql-endpoint`/`lakehouse` T-SQL queries + insights + statistics, `semantic-model generate`, `digital-twin-builder query`, `ontology generate` (lakehouse source), `sql-database import`. (These per-scope token env vars are tokens, not endpoints/scopes, so they are NOT subject to the HTTPS-override validation — that guards `FABIO_*_ENDPOINT`/`FABIO_*_SCOPE`.)
- **Interactive public-client app registration**: fabio's own multitenant public-client Entra app ("Fabio CLI") backs the interactive user flows (device code, browser PKCE, Windows WAM). The compiled-in default is `DEFAULT_PUBLIC_CLIENT_ID` in `src/token_cache.rs`, resolved at runtime via `public_client_id()` which honors the `FABIO_CLIENT_ID` env override (trimmed, non-empty) — lets users switch app registrations without recompiling (e.g. tenant loss/migration recovery). `scripts/create-fabio-app.sh` creates a compatible app (multitenant, `allowPublicClient`, loopback + native-client + WAM-broker redirect URIs) and can patch the source default in place. Distinct from service-principal auth, which takes its client ID from `--client-id`/`AZURE_CLIENT_ID` (see `scripts/setup-ci-auth.sh`). **Delegated permission model (minimal, not all ~200 Power BI scopes)**: fabio acquires tokens for SIX audiences, so the app carries one consented delegated permission per audience — (1) Power BI Service (`api.fabric.microsoft.com`): a curated COARSE set of 14 Fabric/Power BI scopes (`Workspace/Item/Capacity/Connection/Gateway/OneLake/Tenant.ReadWrite.All`, `Item.Execute/Reshare.All`, `Dataset/Report/PaginatedReport/Dashboard/Dataflow.ReadWrite.All`) — Fabric authorizes calls by the user's workspace/tenant RBAC role, not the granular scope claim, so a coarse set covers the whole CLI; (2) Azure Storage → `user_impersonation` (OneLake DFS/Blob); (3) Azure SQL DB → `user_impersonation` (TDS); (4) ARM → `user_impersonation` (capacity ops); (5) Azure Data Explorer → `user_impersonation` (KQL/Kusto); (6) Microsoft Graph → `User.Read` + `InformationProtectionPolicy.Read` (`label list`). Total 20 scopes. Scope GUIDs are resolved by NAME at runtime from each resource SP (portable across tenants/clouds), missing resource SPs are auto-provisioned, and unpublished allow-list names are reported. The non-Fabric audiences need their permission because fabio redeems the cached refresh token for each other audience non-interactively (`get_token_for_scope` in `src/token_cache.rs`), which requires a pre-consented delegated permission.
- `azure_identity`/`azure_core` with `default-features = false` (no OpenSSL on Linux/macOS); `client_certificate` feature Windows-only (vendored OpenSSL)
- **Fully static Linux binaries** — Built with musl (`x86_64-unknown-linux-musl`, `aarch64-unknown-linux-musl`); zero runtime library dependencies; runs on any Linux kernel 2.6.39+; Docker image uses `FROM scratch`
- **Bundled CA roots** — `webpki-root-certs` crate pre-loads Mozilla CA certificates into every HTTP client via `http_client_builder()` in `src/client.rs`. Ensures HTTPS works on minimal Linux systems without `ca-certificates` installed (`rustls-platform-verifier` has no bundled fallback on Linux). All `reqwest::Client` construction MUST use `http_client_builder()` — never raw `Client::builder()`.
- **Windows-first compatibility** — Token cache encrypted with DPAPI (`CryptProtectData`, user scope); WAM broker SSO via `--wam` flag
- `unsafe_code = "forbid"` in lints
- **KQL Queryset definition format**: Uses `RealTimeQueryset.json` (NOT `RawQueryset.kql`). JSON structure: `{"queryset":{"version":"1.0.0","dataSources":[{"id","clusterUri","type","databaseName"}],"tabs":[{"id","content","title","dataSourceId"}]}}`. The `content` field holds the KQL query text with `\n` for newlines. **A Fabric KQL database data source** uses `type:"Fabric"` with `databaseItemId` + `databaseItemName` (the KQL DB's item id + display name) — NOT `type:"AzureDataExplorer"` with `databaseName` (that shape is rejected by the API with `InvalidArtifactDefinitionFormat` for a Fabric-hosted KQL database).
- **KQL Queryset run**: Fetches definition via LRO, decodes `RealTimeQueryset.json`, selects tab by name or index, resolves data source (clusterUri + database name), executes via Kusto REST API. Tab selection is case-insensitive by title. **The database name is read from `databaseName` OR `databaseItemName`** (`kql_queryset::run`) — a Fabric data source only carries `databaseItemName`, so reading only `databaseName` left the name empty and Kusto fell back to `NetDefaultDB`, failing with `BadRequest_EntityNotFound: Entity ID 'NetDefaultDB'`.
- **KQL Queryset add-tab (authoring helper)**: `kql-queryset add-tab --workspace <ws> --id <queryset> --kql-database <kqlDbId> --title <t> --kql "<query>"` authors a query tab bound to a Fabric KQL database without hand-crafting `RealTimeQueryset.json`. It resolves the KQL DB's `queryServiceUri` (cluster URI) + display name (via GET `/kqlDatabases/{id}`), then does a read-modify-write of the queryset definition: reuses an existing `type:"Fabric"` data source matching the `databaseItemId` (else adds one with a fresh UUID) and appends `{id, title, content, dataSourceId}` to `tabs[]`. `--kql` accepts inline/`@file`/stdin (the uniform query-input convention). Additive (preserves existing tabs) so `mutates:true` but not `destructive`; dry-run-guarded before any network call. The pure `queryset_add_tab` (in `src/commands/kql_queryset.rs`) is unit-tested; the add-tab→run loop is live-validated (`kql_queryset_add_tab_and_run`, gated on `FABIO_TEST_KQL_QUERYSET_ID` + `FABIO_TEST_KQL_DATABASE_ID`).
- **Deploy diff strategy**: Content hash vs live workspace (not git diff) — detects portal edits, works without git, idempotent convergence
- **Deploy parallelism**: Semaphore-bounded `tokio::spawn` per-item within type batch (default 8); sequential for DataPipeline; deletes always sequential. Export also uses bounded parallelism (default 8) for `getDefinition` LRO calls. Cross-type parallelism via tier grouping (11 tiers, independent types run concurrently).
- **Deploy strategy**: `--strategy default|bulk|sequential`. Default: per-item parallel with content-hash skip (best for iterative CI/CD). Bulk: single `bulkImportDefinitions?beta=True` API call (faster for large initial deploys to empty workspaces; requires no Git integration). Sequential: concurrency=1 (debugging). All strategies share the same planning phase (parameter substitution, content-hash comparison, changeset building). Bulk falls back to per-item for renames and deletes.
- **Deploy parameter format**: JSON (not YAML) — no extra crate dependency, agent-native consistency. Supports fabric-cicd-compatible features: `find_replace`, `key_value_replace`, `spark_pool`, `semantic_model_binding`, dynamic variables (`$workspace.id`, `$items.Type.Name.id`, `$ENV:VAR`).
- **Deploy plan staleness**: Workspace fingerprint = SHA256 of sorted `(id, type, name)` tuples; mismatch → error unless `--force`
- **Deploy logical ID resolution**: String replacement in base64 payloads; resolves items created earlier in same session. Also resolves ExecutePipeline GUID references by matching activity names to pipeline names, and notebookId references by matching activity names to notebook names.
- **Deploy workspace ID replacement**: Replaces ALL workspace GUIDs found in `workspaceId`/`default_lakehouse_workspace_id` fields with the target workspace ID (not just `00000000-...` placeholders). Handles repos exported without Fabric Git Integration normalization.
- **Deploy notebook format detection**: Inferred from file name — `notebook-content.ipynb` → `format: "ipynb"`, `notebook-content.py` → no format (server auto-detects native `.py`). Explicit `definitionFormat` in `.platform` always takes precedence.
- **Deploy connection resolution**: `init-params --resolve-connections` scans pipeline definitions for connection GUIDs, queries tenant connections, and generates a parameters.json with pre-resolved (or TODO) mappings.
- **Deploy rename detection**: Two-pass matching — first by (type, name), then unmatched source items with logical IDs get candidates checked via `fetch_deployed_logical_id()` which reads `.platform` part from deployed item definition
- **Deploy creationPayload**: Separate `creationPayload.json` file in item directory; merged into creation body as `creationPayload` field; parameter substitution applied
- **Deploy post-hooks**: Opt-out via `--no-post-hooks`; hooks never fire during `--dry-run`; failures are non-fatal (reported in output, don't fail the deploy). SemanticModel → `POST /refreshes`, Environment → `POST /staging/publish`, VariableLibrary → `PATCH properties.activeValueSetName` (auto-activates value set matching `--env` name)
- **Deploy variable library value set activation**: When `--env` is specified and VariableLibrary items are deployed (create or update), fabio auto-activates the value set whose name matches the env name (e.g., `--env prod` activates "prod"). Aligns with fabric-cicd behavior. Non-fatal on failure (warns if value set doesn't exist).
- **Variable library definition format**: Three parts: `variables.json` (required), `settings.json` (required), `valueSets/<name>.json` (optional, one per alternate set). Value sets use `variableOverrides` array (not `values`). Path is plural `valueSets/` (forward slash). Active set is a workspace-level setting (not part of definition), managed via PATCH `properties.activeValueSetName`. **`variable-library update-definition --file` accepts a full multi-part envelope** (via `definition_spec::build_update_definition_body`) — previously it wrapped the file as a single `variables.json`, so alternate value sets could not be authored via the CLI (server rejected the wrapped envelope with `VariablesValidationErrorCode`); it now passes a `{definition:{parts:[…]}}` envelope through verbatim, backward-compatible with a raw `variables.json`. Same fix pattern as `graphql-api update-definition`. Unit + live e2e (`variable_library_update_definition_multipart_via_cli`).
- **Deploy validate notebook-settings.json**: `deploy validate` warns when Notebook items lack `notebook-settings.json` — required since March 2026 for auto-binding of lakehouse dependencies after deployment/Git sync.
- **Deploy schedules export/apply**: `deploy export` fetches job schedules for schedulable items (Notebook, DataPipeline, SparkJobDefinition, etc.) and writes `schedules.metadata.json`. `deploy apply` creates schedules on deployed items from the metadata file (additive, non-fatal post-hook).
- **Deploy --post-run-item**: Triggers a named pipeline/notebook after deployment for data orchestration. Finds item by display name, determines job type, runs via Job Scheduler API. Non-fatal.
- **Workspace clone**: `workspace clone --source <WS> --dest <WS>` uses Bulk Export/Import Definitions APIs for fast workspace replication. Supports `--allow-pairing-by-name` for initial clones without logicalId matching, and `--item-types` for selective cloning.
- **Git branch-out**: `git branch-out --workspace <WS> --branch <feature-name>` automates the Fabric "Branch out to workspace" flow — creates workspace, assigns capacity, connects to new branch, initializes from branch content. Requires `--connection-id` for GitHub provider. Supports `--existing-workspace` for recycling feature workspaces.
- **`git pull` conflict teaches `--allow-override` + agent-notice coverage audit**: `git pull`'s `--allow-override` sets `options.allowOverrideItems: true`, which OVERWRITES conflicting workspace items with the remote version (discards uncommitted workspace changes — irreversible), yet it was neither in `DANGEROUS_FLAGS` nor surfaced in any hint, so the agent safety notice could never fire for it. Fixed two ways: (1) `git pull` now enriches a conflict error (`enrich_pull_conflict`/`is_git_conflict` in `src/commands/git/sync.rs`) — when the API rejects the pull for a conflict AND the caller did NOT pass `--allow-override`, the error gains a teaching hint that names `--allow-override` (so `agentNotice` fires for detected agents; passthrough when the flag was already set). (2) `--allow-override` (and `--hard`, the ontology-delete permanent-delete flag) were added to `DANGEROUS_FLAGS`. This was found by an audit of whether the agent-notice mechanism covers ALL destructive/bypass flags; the audit is now enforced deterministically by `all_escalation_flags_are_triaged` (in `src/agent.rs`), which reads every escalation-style flag out of `commands.json` and fails unless each is in `DANGEROUS_FLAGS` or the test's `BENIGN_ESCALATION_FLAGS` allowlist (the benign capability/behavior toggles: `--allow-cloud-connection-refresh`, `--allow-code-first-artifacts`, `--allow-custom-connectors`, `--allow-gateway-usage`, `--allow-pairing-by-name`, `--allow-tool`, `--allow-write`, `--drop-if-exists`). A companion `every_dangerous_flag_triggers_the_notice` guards the wiring. Pure `is_git_conflict`/`enrich_pull_conflict` are unit-tested; the two agent tests make the notice-coverage invariant permanent.
- **Deploy empty definitions**: Items with no parts (Lakehouse, MLModel) omit `definition` field on create; skip `updateDefinition` on update
- **Deploy shell-only export**: Warehouse, SQLDatabase, MLExperiment, MLModel don't support `getDefinition` but are exported as `.platform`-only directories (metadata without definition parts). Aligns with fabric-cicd's `SHELL_ONLY_PUBLISH`. SQLEndpoint is always skipped (auto-provisioned by Fabric, not independently deployable).
- **Deploy ordering**: 45 item types in `DEPLOY_ORDER`; deployed in dependency order (storage → compute → code → models → reactive → APIs → ML → graph → viz)
- **Deploy no state file**: Stateless — always queries live workspace. No `.tfstate` equivalent.
- **Deploy .platform in parts but excluded from hash**: `.platform` IS sent as a definition part (enables `?updateMetadata=true` for metadata propagation), but EXCLUDED from content hash (API rewrites `logicalId` in `.platform`, which would break idempotent skip detection)
 - **Deploy workspace ID regex replacement**: Uses regex matching on `workspaceId`, `default_lakehouse_workspace_id`, `workspace` keys — not blanket string replacement. Skips shortcuts (handled separately with lakehouse GUID). Opt-out: `--no-workspace-id-replace`
 - **Deploy same-run cross-item connection resolution**: `deploy export` records each item's SOURCE item GUID as its `.platform` `logicalId` (`stable_logical_id`, when no real Git logicalId exists), so deploy's generic logical-id resolver rewrites ANY reference holding another item's source GUID to the deployed GUID — fixes Ontology `DataBinding.itemId`→deployed Lakehouse and KQLDatabase `parentEventhouseItemId`→deployed Eventhouse. For a **Direct Lake SemanticModel** connection (`Sql.Database("<server>","<sqlEndpointId>")` → an auto-provisioned SQL endpoint, not a logicalId), export writes a `sqlendpoint.metadata.json` sidecar (`{id, server}`) per Lakehouse; an inter-tier apply hook (`populate_sql_endpoint_resolutions`) polls the DEPLOYED lakehouse's SQL endpoint and maps `source_endpoint_id→target_endpoint_id` + `source_server→target_server` (only when referenced by another item's parts) so the connection is rewired to the deployed lakehouse. A deploy-created Direct Lake model is definition-managed and rejects refresh with `EntityNotFound` until taken over, so `refresh_semantic_model` does a best-effort `Default.TakeOver` (Power BI API) then refreshes via `/groups/{ws}/datasets/{id}/refreshes` (the Fabric `/semanticModels/{id}/refreshes` path 404s for definition-managed models). Export excludes an Ontology's auto-created children (`<name>_lh_<id>` Lakehouse + `<name>_graph_<id>` GraphModel, via `ontology_child_names`). Also fixed: deploy poll/folder URLs were built without a leading slash (`format!("workspaces/…")`), producing malformed `…/v1workspaces/…` → IIS 404; now `/workspaces/…`. See `.agents/API-BEHAVIORS-DISCOVERED.md` "Deploy — same-run cross-item connection resolution".
- **Deploy config file (JSON + YAML)**: `--config <file> --env <name>` loads per-environment workspace/source/parameters; `serde_yaml` crate for YAML; CLI flags override config values
- **Deploy protected type deletion**: Lakehouse, Warehouse, SQLDatabase, Eventhouse, KQLDatabase require `--allow-delete-types` to be deleted by `--delete-orphans`
- **Deploy fabric-cicd full compatibility**: Source directory format, .platform file schema, definition parts, logical ID resolution, workspace ID replacement, creationPayload, .children/ discovery, .pbi/ exclusion, notebook ordering, Report byPath transform — all aligned with Microsoft's fabric-cicd Python library
- **Upgrade**: `fabio upgrade` downloads latest release from GitHub, verifies SHA256 checksum, extracts platform-appropriate archive (tar.gz on Unix, zip on Windows), atomically replaces running binary; supports `--check` (version query only), `--target-version` (pin specific version), `--force` (reinstall even if current), `--dry-run`
- **Context tenant LSP-inspired agent features**: Inspired by Language Server Protocol design, `context tenant` provides progressive disclosure for AI agents: `--summary-only` (cheap inventory probe, 2 API calls), `--resolve Type:Name` (fast name-to-ID lookup without graph), `--focus <id> --depth N` (ego-centric subgraph via BFS). All graph responses include a `meta` envelope (`scannedAt`, `scanDurationMs`, `etag` SHA-256 fingerprint, `partial`, `scope`) for freshness/drift detection. Edges carry `confidence` (high/medium/low) and `discoveryMethod` fields so agents can filter noise.
- **Context information-architecture layers (personas + disambiguations)**: Inspired by microsoft/skills-for-fabric's Agents→Skills→Common decomposition, fabio adds two authored-knowledge layers on top of its runtime mechanics. **Personas** (`fabio context persona <name>`: data-engineer, data-scientist, app-developer, bi-developer, rti-engineer, migration-engineer, fabric-admin) are Layer-1 orchestrators — thin routers mapping a request type to command groups + workflows + best-practices, with decision gates, guardrails (must/prefer/avoid), and negative routing. **Disambiguations** (`fabio context disambiguate <term>`: materialized-view, dataflow, semantic-model, sql-endpoint, mirroring, model, activator) resolve overloaded Fabric terms to the concrete artifact + command group. Both are JSON data files auto-registered by `build.rs` (like workflows/best-practices), searchable via `context find`, and drift-free because command indexes come from `commands.json`. The `disambiguate` lookup matches a table by its filename term OR any declared `aliases` (normalization-insensitive — `find_by_alias`), and `context find` gives a strong score boost when the whole query matches a table's `term`/`aliases` (`query_matches_term_or_alias`), so an overloaded term like "Data Activator" routes to the right table even when generic keyword hits ("Data…") would otherwise dominate. The **activator** table encodes that "Data Activator" == the **Reflex** item (fabio command group `reflex`, also exposed via the `activator`/`data-activator` command aliases) and distinguishes it from `data-agent` (NL Q&A) and `operations-agent` (AI RTI monitoring). Migration is data-only for now (workflows synapse/databricks/hdinsight/pipeline + best-practice `migration-api-shims`); a `fabio migrate assess` command is a deferred epic.
- **Intent-scoped sub-skills (Layer 2, generated)**: Fourteen `fabio-<family>` sub-skills (lakehouse, warehouse-sql, data-engineering, dataflows, data-science, mirroring, rti-kql, bi, ontology, geospatial, deploy-cicd, admin, migration, app-dev) at `.agents/skills/fabio-*/SKILL.md` are GENERATED from authored judgment (`src/commands/context/data/skills/<family>.json`) + a command index pulled from `commands.json`. This realizes the division of labor: prose carries judgment (when-to-use, gotchas, safety, routing); the command table is mechanically derived (drift-free). The generator lives in `src/commands/context/skillgen.rs` (a `#[cfg(test)]`-only module — it is a build/test-time tool, not runtime, mirroring `generate_agent_schema`). Regenerate with `cargo test generate_subskills -- --ignored`; the `subskills_match_generated` drift test fails in CI if committed files are stale. Every workload command group is in a skill family (enforced down to the subcommand level by the `every_command_group_has_a_knowledge_home` test — a group with no family generates no command table, so a new group with no family/allowlist home fails `cargo test` before release; personas are additive routing, not a coverage substitute). The root `fabio` skill remains the comprehensive single-file entry point and now routes to sub-skills for progressive disclosure (load only the relevant sub-skill to keep context lean).
- **Item relations (beta)**: `fabio item list-upstream-relations`/`list-downstream-relations` call the new `GET /workspaces/{ws}/items/{id}/relations/{upstream|downstream}?beta=true` endpoints. Response is a graph fragment (`items`/`relations`/`workspaces`), not a paginated list — rendered via `render_object`, not `render_list_with_token`.
 - **Lakehouse MLV execution definitions**: New CRUD group `fabio lakehouse {list,show,create,update,delete}-execution-definition(s)` at `/workspaces/{ws}/lakehouses/{id}/mlvexecutiondefinitions[/{defId}]`. Groups a `currentLakehouseExecutionContext`/`extendedLineageExecutionContext` (discriminated `All`/`Selected` unions) with optional Spark `environment` + `refreshMode` settings; referenced by materialized-lake-view refresh schedules via `executionData.mlvExecutionDefinitionId`.
 - **Lakehouse MLV schedule listing**: `lakehouse list-materialized-views-schedules` (GET `/workspaces/{ws}/lakehouses/{id}/jobs/refreshMaterializedLakeViews/schedules`) and `get-materialized-views-schedule --schedule-id` (GET `.../schedules/{scheduleId}`) complete the MLV-schedule CRUD in the lakehouse group (which already had `create`/`update`/`delete-materialized-views-schedule` by `--schedule-id`). Needed to discover/inspect the schedule IDs when managing the multiple named schedules an MLV lakehouse can now have. Equivalent to the generic `job-scheduler list-schedules --job-type refreshMaterializedLakeViews`, but discoverable alongside the other `*-materialized-views-schedule` commands.
 - **User data function invoke (portal-URL action)**: `fabio user-data-function invoke --url <public-function-url> [--parameter name=value]... [--body <json>]` invokes a *published* function via its per-function public REST endpoint. Fabric exposes no public API to invoke a function or discover its URL (the URL is copied from the portal, public access enabled), so `--url` is supplied directly — mirroring `data-agent query --published-url`. fabio SSRF-guards the URL (`validate_trusted_url`, `*.fabric.microsoft.com` HTTPS only), attaches the Fabric bearer token, POSTs the parameter body, and renders the `{functionName, invocationId, status, output, errors}` response. Pure `build_invoke_body` (parameters→JSON, `--body` override) is unit-tested; plumbing is live-validated (auth + POST reach Fabric, clean 404 surfaced). Happy-path invocation needs a published function with public access (not REST-provisionable) — documented in API-BEHAVIORS.
 - **ML experiment run tracking (MLflow action)**: `fabio ml-experiment {list-runs,get-run,get-metric-history}` read MLflow run data from the per-workspace Fabric-hosted MLflow tracking server at `/workspaces/{ws}/mlflow/api/2.0/mlflow/...` (standard Fabric token; the experiment item GUID is the MLflow `experiment_id`). `list-runs` → `runs/search` (supports `--filter`/`--order-by` MLflow expressions + `--limit`), `get-run` → `runs/get`, `get-metric-history` → `metrics/get-history`. This closes the ML-experiment Tier-B action gap (runs live on the MLflow surface, not the Fabric item API). Pure helpers (`mlflow_base`, `build_search_body`) are unit-tested; the e2e test seeds a run entirely over the MLflow REST API (`runs/create`+`log-metric`+`update`) so the full loop is live-validated without a notebook.
 - **ML model registry versions (MLflow model-registry action)**: `fabio ml-model {list-registry-versions,get-registry-version}` read a model's TRAINED versions from the per-workspace Fabric-hosted MLflow **model registry** (`/workspaces/{ws}/mlflow/api/2.0/mlflow/...`) — distinct from the existing `ml-model list-versions`/`get-version`, which are the serving-ENDPOINT versions. A Fabric ML Model item IS an MLflow registered model whose NAME equals the item's display name (the registry is keyed by name, not the item GUID), so both commands first resolve the item's `displayName` (GET `/mlModels/{id}`) then query the registry: `list-registry-versions` → `GET .../model-versions/search?filter=name='<name>'` (rendered as a list of `{name, version, current_stage, status, run_id, source, …}`); `get-registry-version --version <n>` → `GET .../model-versions/get?name=<name>&version=<n>`. `--version` needs `disable_version_flag` on the subcommand (else it collides with clap's auto `-V/--version`). Closes the ML-model registry-read gap (the "register/version models" data-science tutorial's discovery step). Pure `mlflow_base`/`model_versions_filter` (quote-escaping) are unit-tested; the create-model → seed run → `model-versions/create` → list/get loop is live-validated (`ml_model_registry_versions_lifecycle`, gated on `FABIO_TEST_ML_MODEL_ID`). See `.agents/API-BEHAVIORS-DISCOVERED.md` "ML Model API Behaviors Discovered".
 - **Report/paginated-report export-to-file (Power BI action)**: `fabio report export` and `fabio paginated-report export` render a (paginated) report to a file via the Power BI `exportToFile` async flow (`POST .../reports/{id}/ExportTo` → poll `.../exports/{jobId}` → download `.../file`), implemented in the shared `src/commands/powerbi_export.rs` module (with the new `client::get_powerbi_bytes` helper). Formats: PDF/PPTX (both), PNG (Power BI reports), IMAGE/XLSX/DOCX/CSV/XML/MHTML/ACCESSIBLEPDF (paginated); paginated `--parameter name=value` maps to `paginatedReportConfiguration.parameterValues`. The format validator + body builder are pure and unit-tested; plumbing is live-validated (a bogus id yields a clean `PowerBIEntityNotFound`). This was the one clear "CRUD-only item that exists to produce an output" gap found in the item-action audit — most other CRUD-only groups are complete because the public Fabric REST API offers only CRUD for them (their actions live on Power BI/MLflow/Kusto/portal surfaces).
  - **Paginated-report create/update-definition body (definition MUST omit `format`)**: `fabio paginated-report create`/`update-definition` send `definition: { parts: [...] }` with **NO `format` field**. Sending `definition.format: "PaginatedReportDefinition"` (the previous behavior, and what the docs example implies) is rejected by the Fabric API with `InvalidDefinitionFormat` — this mirrors `report create`, which also omits `format`. The single RDL part path MUST equal `<displayName>.rdl` (any other path → `MissingDefinitionParts`): `create` synthesizes it from `--name`, `update-definition` resolves it from a GET of the item's current display name (a `.platform` part is optional). This corrected an earlier WRONG "create is blocked server-side on this capacity" conclusion — it was a fabio bug, not a tenant/region limitation; live-validated end-to-end (create → byte-identical CSV export vs a portal-authored original → update-definition round-trip). Pure helpers `single_rdl_part`/`definition_object` in `src/commands/paginated_report.rs` are unit-tested (regression guard: `definition_object` must never emit `format`). See `.agents/API-BEHAVIORS-DISCOVERED.md` "Dashboard/Datamart/Paginated Report API Behaviors Discovered".
 - **Data agent MCP consumption endpoint**: `fabio data-agent mcp-url --workspace <ws> --id <id>` prints the canonical Model Context Protocol runtime/consumption endpoint for a data agent — `{fabricBase}/mcp/workspaces/{ws}/dataagents/{id}/agent` — which external MCP clients (Claude, Copilot Studio, Azure AI Foundry) use to query a *published* agent. This is distinct from `data-agent query`, which uses the older OpenAI-Assistants endpoint (`.../aiassistant/openai`). Published state is detected reliably via `GET /dataAgents/{id}/settings` (200 = published, 404 `DataAgentNotPublished` = draft); when unpublished, the command reports `published: false` and hints to run `data-agent publish`. The URL builder (`build_mcp_url`) is pure and unit-tested; the base URL honors `FABIO_FABRIC_API_ENDPOINT` via the new `client::fabric_base_url()` accessor. Both Fabric agent item types — DataAgent and OperationsAgent — are now fully covered against their public REST APIs (8 and 7 operations respectively), plus fabio's richer sub-operations.
 - **Operations agent start/stop/status (RTI AI monitoring)**: `fabio operations-agent {start,stop,status}` manage a Real-Time Intelligence operations agent's activation. Fabric exposes no dedicated start/stop endpoint — activation is the top-level `shouldRun` boolean inside the agent's `Configurations.json` definition. `start`/`stop` are implemented as a read-modify-write (`getDefinition` → decode `Configurations.json` part → flip `shouldRun` → `updateDefinition` with just that part) followed by a re-read to report the persisted value; `status` reads `shouldRun` back and reports `running`/`stopped`. **Fabric silently coerces `shouldRun` back to `false` for an agent with no configured data source + playbook** (verified live), so `start` reports `requestedShouldRun` vs the actual `shouldRun` and adds a `note` when activation was refused rather than claiming false success. While running, Fabric evaluates the agent's rule queries every 5 minutes. The `shouldRun` read/flip helpers in `src/commands/operations_agent.rs` are pure and unit-tested. operations-agent is routed via the `rti-kql` sub-skill and the `app-developer` persona. Playbook generation, Copilot-chat configuration, the activity log, and Teams/Investigator actions are portal/Copilot-only surfaces with no public REST API, so they are out of scope for the CLI.
 - **Data agent SQL sources & preview runtime (Advanced NL2SQL)**: A data agent's four SQL source types — Lakehouse, Data Warehouse, SQL Database, Mirrored Database — are added and configured through the existing datasource commands (`add-datasource --artifact-type Lakehouse|Warehouse|SQLDatabase|MirroredDatabase`, `select-tables` for schema scope, `update-datasource --instructions/--description`, and the fewshots commands for example queries). The runtime that drives their built-in NL2SQL tool is toggled by `fabio data-agent update-config --enable-preview-runtime|--disable-preview-runtime`: standard runtime = GA NL2SQL (single-pass, production-stable), preview runtime = Advanced NL2SQL (multi-step reasoning — better example following, filter-value substitution, ambiguity clarification). Both runtimes consume the SAME per-source config, so switching needs no reconfiguration. On the wire the flag is the NESTED settings boolean `experimental.enableExperimentalFeatures` in `PATCH .../staging/settings` (a top-level `enableExperimentalFeatures` is silently ignored); fabio does a read-modify-write to preserve sibling `experimental` keys (e.g. `mcpServers`). `get-config`/`update-config` surface it as a top-level `previewRuntime` boolean. This matches the Fabric Python SDK's `update_configuration(enable_preview_runtime=...)`. A published agent's runtime is fixed at publish time (republish to change). The pure body builder (`build_settings_body`) and reader (`preview_runtime_enabled`) in `src/commands/dataagent/config.rs` are unit-tested; the full enable→disable toggle is live-validated in `tests/e2e_dataagent.rs` (`dataagent_preview_runtime_toggle_lifecycle` and `dataagent_advanced_management_lifecycle`). NOTE: the `--enable-preview-runtime`/`--disable-preview-runtime` flags previously existed on `update-config` but were accepted and silently dropped (never written to the API) — this wires them to the real settings field.
 - **Data agent consumption: multi-turn query, answer-file download, evaluate batch primitive, and constructed published-URL fallback**: A gap analysis against the Python `fabric-data-agent-sdk` (v0.1.27a0; re-audited through **v0.1.28a0**, Jul 2026) confirmed fabio's public *management*-plane surface is already 1:1 with the SDK (settings/datasources/elements/fewshots/publish/reset). The SDK's extra capabilities are data-plane/LLM/notebook features; the reachable ones are now in fabio's `query`/`evaluate` (`src/commands/dataagent/query.rs`, `evaluate.rs`). (1) **`query` returns `threadId`** and gained `--thread-id` (reuse an existing thread) + `--keep-thread` (retain it) for MULTI-TURN follow-ups over the OpenAI Assistants protocol fabio already speaks — live-validated that a second CLI invocation on the same thread recalls prior context. (2) **`query --download-files <dir>`** saves files the answer attaches (generated CSVs/charts) via the OpenAI files API (`GET {base}/files/{id}/content`) on the published endpoint; the response gains a `files` array. File-id extraction scans assistant-message text annotations (`file_path`/`file_citation`), `image_file` content items, and message `attachments`; filenames are sanitized to a safe basename (path-traversal-proof, Windows + Unix). (3) **`--stage` is no longer a silent no-op**: only `production`/`published` is accepted; `sandbox`/`staging` (draft) fails fast with a publish-first hint, because the draft agent lives on the internal workload host with no public endpoint. (4) **`data-agent evaluate --questions <file>`** is a batch primitive (mirrors the SDK's `evaluate_data_agent` shape: questions + optional expected + `--repeats`) that runs each question on its own thread and emits the answers (`--show-steps` for run steps); it is NOT an LLM judge — when a question has an `expected` answer it adds only a NAIVE `match.exact`/`match.contains` string signal, leaving semantic grading to the calling agent. Questions file is JSON (array of strings or `{question,expected}` objects) or CSV/TSV with a `question` column. (5) **Constructed published-URL fallback**: `GET /dataAgents/{id}/settings` does NOT return a `publishedUrl`, so `query`/`evaluate` now build the canonical `{fabricBase}/workspaces/{ws}/dataagents/{id}/aiassistant/openai` (lowercase `dataagents`) when no `--published-url` is given — previously `query` errored unless the user supplied the URL manually. Pure helpers (`validate_query_stage`, `extract_answer`, `extract_file_ids`, `sanitize_filename`, `build_published_url` in `query.rs`; `parse_questions`, `compute_match`, `normalize` in `evaluate.rs`) are unit-tested; the full published-agent lifecycle (publish → query+threadId → multi-turn → evaluate) is live-validated in `dataagent_query_multiturn_and_evaluate_lifecycle`. Out of scope (require an LLM judge or the internal workload host, not the public API): the SDK's OpenAI *Responses* API client, few-shot LLM validation, and M365 Copilot Agent Store publishing (`publish --to-m365` remains correctly reported as unsupported — the SDK implements it against a non-public `metaosapppackage` workload endpoint).
 - **Data agent LLM-powered features (bring-your-own judge model) + confirmed non-public surfaces**: Two previously-out-of-scope SDK features that need an external judge model are now implemented, with the model supplied by the caller via `--llm-endpoint`/`--llm-key`/`--llm-model` (+ `--llm-api-version`; env fallbacks `FABIO_LLM_ENDPOINT`/`FABIO_LLM_KEY`/`FABIO_LLM_MODEL`/`FABIO_LLM_API_VERSION`). A shared minimal OpenAI-compatible client lives in `src/llm.rs` (`LlmClient`/`LlmConfig`): it auto-detects flavor from the endpoint host — `*.azure.com` → Azure OpenAI (`{endpoint}/openai/deployments/{model}/chat/completions?api-version=…`, `api-key` header, model in URL) vs anything else → OpenAI-compatible (`{endpoint}/chat/completions`, `Authorization: Bearer`, model in body). `complete_json` parses JSON leniently (strips code fences, extracts the first balanced brace span). (1) **`data-agent validate-fewshots`** (`validate.rs`) reads a data source's few-shots and asks the judge to flag duplicates/conflicts/ambiguity/low-quality/incorrect queries → `{issueCount, overallQuality, issues:[{fewshotIds,type,severity,explanation,suggestion}]}`; read-only; requires `--llm-*`. (2) **`data-agent evaluate --llm-*`** upgrades the batch primitive with an optional LLM critic: each answer gets a `grade` (`{correct,score,rationale}`) and the summary adds `gradedRuns`/`passedRuns`/`passRate`. Grading is **non-fatal** — a judge error (e.g. Azure content-filter 400, live-observed) is recorded as a per-answer `grade.error` and the eval continues. Pure builders (`build_chat_url`/`build_chat_body`/`extract_content`/`extract_json`/`is_azure_endpoint` in `llm.rs`; `build_validation_prompt` in `validate.rs`; `build_grade_prompt` in `evaluate.rs`) are unit-tested; both features are live-validated end-to-end against Azure OpenAI (`gpt-5-mini`) + the tenant in `dataagent_llm_validate_and_grade_lifecycle` (skips if `FABIO_LLM_*` unset). **Confirmed non-public (live 404 evidence, cannot be implemented from an external CLI)**: the SDK's OpenAI *Responses* API (`POST {published}/responses` → 404) and M365 Copilot Store publishing (every candidate public path for `metaosapppackage` → 404) both live only on the internal Fabric workload host, discovered via `synapse.ml.fabric.service_discovery` inside a notebook. `publish --to-m365` stays `unsupported` with a sharper message; Responses adds nothing over fabio's existing thread-based multi-turn. **Re-verified against SDK v0.1.28a0 (Jul 2026)**: the SDK's newer `FabricOpenAIResponses` client (added 0.1.23a0/0.1.27a0, default model `gpt-5.1`) is STILL not reachable externally — a fresh live probe of a published agent returned `HTTP 404` on every candidate public path (`.../responses`, `.../openai/responses`, `.../aiassistant/responses`, `.../aiassistant/openai/responses`, `.../aiassistant/openai/v1/responses`) while the Assistants path (`.../aiassistant/openai/threads`) returned `HTTP 200`. The Responses API only works from inside a Fabric notebook (internal host), so it remains out of scope for the external CLI. Also confirmed N/A: the SDK's few-shot-validation model bump (0.1.25a0, `gpt-4.1`→`gpt-5.1`) targets Fabric's IN-NOTEBOOK built-in AI endpoint (ambient, no key) — fabio has no access to it, which is precisely why `validate-fewshots`/`evaluate --llm-*` require a bring-your-own judge model. And the SDK's duplicate-few-shot auto-rename (0.1.23a0, `[N]` suffixes) is unnecessary for fabio: the Fabric API TOLERATES duplicate few-shot questions (live-verified: a second add-fewshot with an identical question returns a new id, `validationStatus: Validating`), and `validate-fewshots` already detects duplicates via the LLM judge.
 - **Data agent visual/chart responses (`query --visuals`) — reachable despite the docs' "portal-only" claim**: The [data-agent visuals](https://learn.microsoft.com/en-us/fabric/data-science/data-agent-visuals) feature (charts in the agent chat) is documented as available only in the Fabric portal "and not in other clients like SDK, M365 Copilot, Teams, or Foundry". That holds for the RENDERED image and for the `report_specs_*.json` file the answer references (its file id 404s on the published Assistants `/files` endpoint — `--download-files` can't fetch it). BUT the COMPLETE chart spec is reachable another way: the agent's `*.VisualizeDataset` function tool call (visible in the run steps fabio already reads for `--show-steps`) carries the whole spec in its `arguments` — `chart_type`, `title`, `x_column`, `y_columns[]`, axis titles, `sort_by`/`sort_order`, and the aggregated data as `inline_csv_data`. `fabio data-agent query --visuals` fetches the run steps, extracts every `VisualizeDataset` call (de-duplicating the `trace.`/`AIFunction.` pair by content), attaches the referenced `reportSpecFile` name, and emits a `visuals[]` array — so a client reconstructs the chart without the portal (the tool output itself says "Client will be able to render UI from this specification"). Implemented in `src/commands/dataagent/query.rs` (`extract_visuals`/`parse_report_spec_filename`, unit-tested); live-validated end-to-end (bar→`column_chart`, trend→`line_chart`, text-only→empty) in `dataagent_query_visuals_lifecycle` (gated on `FABIO_TEST_LOADED_LAKEHOUSE` + `az`). See `.agents/API-BEHAVIORS-DISCOVERED.md` "Data Agent API Behaviors Discovered".
 - **Hint type classification for semantic drift prevention**: Error hints include a `hintType` field (`auth_fix`, `retry_safe`, `syntax_fix`, `semantic_correction`, `safety_bypass`) that classifies the hint's semantic impact on the operation. Agents use this to decide whether a hint-driven retry is safe to execute automatically (`auth_fix`/`retry_safe`/`syntax_fix`) or requires user confirmation/post-action verification (`semantic_correction`/`safety_bypass`). An optional `verifyAfter` field provides a read-only verification command the agent should run after a successful retry. Inference logic in `render_error()` auto-classifies the ~391 existing `with_hint()` call sites based on error code and hint content patterns; new code uses explicit `with_typed_hint()`.
 - **Destructive `--dry-run` previews carry a proactive confirm-with-user signal**: The `agentNotice` mechanism previously fired ONLY on the error path (a safety-bypass flag suggested in an error hint). A *plain* destructive command an agent runs proactively (`item delete`, `lakehouse delete-table`, `warehouse queries-kill`, …) emitted no machine-readable "this is irreversible, confirm with the user" signal. Now `output::dry_run_guard` annotates the `--dry-run` preview of ANY destructive command with `"destructive": true`, and — when an AI agent is detected — an `agentNotice` (`agent::destructive_notice()`) telling the agent to confirm with the user before re-running without `--dry-run`. Destructiveness is read from `commands.json` via `agent::is_destructive_operation("<group> <subcommand>")` (a lazily-parsed `LazyLock<HashSet<String>>` of every `destructive: true` key), so the signal covers the FULL destructive surface automatically and stays in sync with the schema — no per-command wiring. This makes the recommended pre-flight (agents run `--dry-run` first) carry the confirm signal for every destructive command, not just the bypass-flag error path. **Requirement it exposed**: `dry_run_guard`'s operation string MUST be the canonical `"<group> <subcommand>"` key; three commands (`capacity delete`, `connection delete`, `connection delete-role-assignment`) passed a descriptive sentence (`"Would delete capacity 'X'"`) or used the manual `if cli.dry_run` pattern, so the key didn't match and the destructive marker was silently dropped — all three were migrated to `dry_run_guard("<group> <subcommand>", …)`. Enforced by two deterministic tests: `agent::tests::every_destructive_command_is_recognized` (unit — every `destructive: true` command is in the set) and `tests/e2e_destructive_guardrails.rs::destructive_dry_run_previews_carry_confirm_signal` (`#[ignore]` — every destructive command whose guard fires offline emits `destructive:true` + `agentNotice`; this is the test that caught the three mismatched keys). Pure `build_dry_run_object` in `src/output.rs` + `is_destructive_operation`/`destructive_notice` in `src/agent.rs` are unit-tested.
- **Sensitivity labels**: All 50 item-type create commands support `--sensitivity-label <uuid>`. All list commands dynamically show a SENSITIVITY LABEL column when items have labels. Label UUIDs are returned inline by the Fabric API (no `--include` needed). `fabio label list` resolves UUIDs to names via Microsoft Graph (requires M365 E5 + InformationProtection.Read). PATCH does NOT support label changes — only create-time or admin bulk operations. See `.agents/API-BEHAVIORS-DISCOVERED.md` section "Sensitivity Labels API Behaviors Discovered" for full details.
- **Endorsement / certification is READ-ONLY (there is NO public SET API)**: `fabio item show-endorsement --id <id>` and `item list-endorsements [--endorsement Certified|Promoted|Master|None] [--type <T>]` surface an item's endorsement (`{endorsement, certifiedBy, certificationDetails}`) for governance/CI checks. **Setting/certifying an item cannot be done via any public REST API** — verified exhaustively: the entire Fabric REST API spec (`microsoft/fabric-rest-api-specs`, all 54 workload swaggers + common + admin) contains ZERO endorsement operations; Fabric `Update Item` rejects an `endorsementDetails` body with HTTP 400; and the Power BI `Datasets` operation list has no "Set Endorsement". Endorsement is portal-only (the "Certification" tenant setting `CertifyDatasets` only governs WHO may certify in the UI). **The read SOURCE is the admin metadata scanner** (`POST /admin/workspaces/getInfo` → poll `scanStatus` → `scanResult`) — NOT the Power BI list endpoints: live-verified that after setting endorsement in the portal, `GET /groups/{ws}/datasets` AND `getGroupsAsAdmin?$expand=datasets` return a STALE `endorsementDetails` (lag by hours), while the scanner reflects it immediately. The scanner requires **Fabric-admin** access (401/403 → a clear "requires admin" hint). Endorsement is a Power BI-lineage concept: the scan carries `endorsementDetails` only on `datasets` (SemanticModel), `reports` (Report/PaginatedReport), `dataflows`, `dashboards`, `datamarts`; Fabric-native items (Lakehouse/Notebook/…) have none, so `show-endorsement` on one returns a precise "not readable for type X" error (their type is resolved from the same scan). **Endorsement levels are `None`/`Promoted`/`Certified`/`Master`** (Master data — live-observed on a semantic model). Pure helpers (`parse_endorsement`, `normalize_endorsement_filter`, `row_type_label`, `item_id`, `collect_endorsements`, `find_endorsable_item`/`find_any_item_type`) in `src/commands/item/endorsement.rs` are unit-tested against a fixture scan; the full read/filter/error paths are live-validated (`item_endorsement_read_lifecycle`) and were confirmed against a real Master + Promoted model. Both commands are `mutates:false` (read-only).
- **Managed private endpoint create field names (bug fix)**: `managed-private-endpoint create` sent `{name, privateLinkResourceId, groupId}`, but the `CreateManagedPrivateEndpointRequest` fields are `{name, targetPrivateLinkResourceId, targetSubresourceType, requestMessage?}` — so the REQUIRED `targetPrivateLinkResourceId` was missing and the create failed (the command was broken; a stale API-BEHAVIORS note documented the wrong names). Fixed to `targetPrivateLinkResourceId`/`targetSubresourceType`. Live-verified: with the corrected fields the create is accepted and provisioning begins (returns `{id, provisioningState, targetPrivateLinkResourceId, targetSubresourceType, connectionState}`). Note: a workspace holding a managed endpoint can't be deleted (`WorkspaceContainsManagedEndpoints`) — delete the endpoint first. Offline regression e2e asserts the corrected body.
- **Deployment pipeline create requires stages (bug fix)**: `deployment-pipeline create` sent `{displayName, description}` but the modern `CreateDeploymentPipelineRequest` REQUIRES `stages` (an array of `{displayName, description?, isPublic?}`), so every create failed with `InvalidInput`. A pipeline is created WITH its stages defined up front (no add-stage endpoint; stages are not editable via update). Fixed: `create` gained a repeatable `--stage NAME[:public]` (ordered; `:public`→`isPublic:true`) and a `--stages-json '[{...}]'` (inline/@file) escape hatch; ≥1 stage required (teaching `INVALID_INPUT` before any network call). Pure `build_stages` is unit-tested; live-verified create (3 stages, Production public) → `list-stages`. See `.agents/API-BEHAVIORS-DISCOVERED.md` "Deployment Pipeline API Behaviors Discovered".
- **External data share recipient shape (bug fix)**: `item create-external-data-share` sent the OLD recipient shape `{tenantId, objectId, recipientType}` (and a bare `{tenantId}` for whole-tenant), but the deployed API models the recipient as a discriminated union on `type` and **rejects the old shape with HTTP 500** (`InternalServerError`) — so the command was completely broken for every recipient. Correct shapes: User = `{type:"User", userPrincipalName:<email>, tenantId?}`; ServicePrincipal = `{type:"ServicePrincipal", principalId:<object-id>, tenantId}`. There is no "share with entire tenant" mode anymore. Fixed with `--recipient-type User|ServicePrincipal` (default User), `--recipient-email` (User), `--recipient-id` (SP `principalId`), `--recipient-tenant-id` (required for SP, optional for User); pure `build_eds_recipient` enforces per-type required fields before any network call. Live-validated both types end-to-end (needs `AllowExternalDataSharingSwitch` on + a FOREIGN recipient tenant — same-tenant is rejected). See `.agents/API-BEHAVIORS-DISCOVERED.md`.
- **Connection update requires connectivityType discriminator (bug fix)**: `connection update` built a PATCH body without `connectivityType`, but `UpdateConnectionRequest` is a discriminated union on `connectivityType` (a REQUIRED field), so every update failed with `InvalidInput`. Fixed: `update` now GETs the connection first to resolve its current `connectivityType` and includes it in the body (same pattern as `gateway update` for its `type` discriminator); the user doesn't pass the type. Pure `build_connection_update_body` is unit-tested; live-verified `connection update --privacy-level None` now succeeds. See `.agents/API-BEHAVIORS-DISCOVERED.md` "Connection API Behaviors Discovered".
- **Item assign-identity needs assignmentType + ?beta=true (bug fix)**: `item assign-identity` posted an empty `{}` body to `/workspaces/{ws}/items/{id}/identities/default/assign` WITHOUT the required `?beta=true` query param, so every call failed (`BetaParameterRequired`, or `InvalidInput` when beta added without a body). The `UpdateItemIdentityRequest` requires `assignmentType` (enum, currently only `Caller`). Fixed: send `{"assignmentType":"Caller"}` (new `--assignment-type` flag, default `Caller`) to `.../assign?beta=true`. Live-verified the corrected shape is accepted (Lakehouse returns the semantic "already the identity" idempotency response; Notebook returns `OperationNotSupportedForItem`). See `.agents/API-BEHAVIORS-DISCOVERED.md` "Item API Behaviors Discovered".
- **Eventstream resume requires startType (bug fix)**: `eventstream resume`/`resume-source`/`resume-destination` posted an empty `{}` body, but the `DataSourceStartRequest` REQUIRES `startType` (enum `Now`/`WhenLastStopped`/`CustomTime`), so every resume failed (`EventStreamBadWebRequest: Required property 'startType' not found`). Fixed: each resume sends `{"startType":<type>}` via `--start-type` (default `Now`) + `--custom-start-time` (required for `CustomTime`), built by the shared pure `build_start_request`. `pause`/`pause-source`/`pause-destination` take no body (unaffected). Live-verified `resume` (default Now) now returns `{status:"resumed"}`. Unit tests + offline dry-run e2e. See `.agents/API-BEHAVIORS-DISCOVERED.md` (Eventstream).
- **Workspace inbound External Data Shares bypass policy (Preview)**: `fabio workspace get-inbound-external-data-shares-policy`/`set-inbound-external-data-shares-policy --default-action Allow|Deny [--if-match <etag>]` at `/workspaces/{ws}/networking/communicationPolicy/inbound/externalDataShares`. First fabio endpoint to use response `ETag`/request `If-Match` optimistic concurrency for a Fabric REST object (previously only used for OneLake file properties). New `FabricClient::get_with_etag()`/`put_with_if_match()` helpers merge the `ETag` response header into the JSON body as an `etag` field so it round-trips through the CLI without a separate flag.
- **Connection `gatewayId` is now a base response property**: Any connection's response (not just gateway-specific connectivity types) may include `gatewayId`. `fabio connection list` shows a dynamic `GATEWAY ID` column when present.
- **Connection `--creation-method` + auto-resolution (Event Hubs Workspace Identity, etc.)**: `connection create` gained an optional `--creation-method` flag for `connectionDetails.creationMethod`, which USUALLY differs from the connection `--connection-type` (verified live: only ~12 of ~276 types match the type name — `SQL`→`Sql`, `EventHub`→`EventHub.Contents`, `ConfluentCloud`→`ConfluentCloud.Contents`, `MQTT`→`MQTT.Contents`; `Web`/`Oracle`/`MySql` match). fabio previously HARDCODED `creationMethod = connection_type`, so `connection create` failed with `InvalidConnectionDetails` for the ~264 types whose method differs — silently blocking Azure Event Hubs / Kafka / MQTT connections, including the "Azure Event Hubs source with Workspace Identity authentication" Eventstream feature (the source references such a connection; `WorkspaceIdentity` is already a supported `--credential-type`). Now, when `--creation-method` is omitted, fabio AUTO-RESOLVES the canonical method from `supportedConnectionTypes` (single-page `get_list`, falls back to the type name if the catalog is unreachable/unknown), so create "just works"; the only 2 types with multiple creation methods (`AzureDataExplorer`, `Spark`) return a teaching error enumerating the valid values. Pure `build_connection_body` + `resolve_creation_method` in `src/commands/connection.rs` are unit-tested; live-validated (`connection_creation_method_accepted_by_api` proves auto-resolution makes EventHub details valid and an explicitly-wrong method is rejected; `connection_creation_method_ambiguous_type_teaches` proves the enumerated error). The stale `connection.json` schema doc (which advertised a nonexistent `--content` flag) was corrected to the real typed flags.
 - **Connection code-first / gateway usage toggles**: `connection create` gained `--allow-code-first-artifacts` (alias `--allow-usage-in-user-controlled-code`) → top-level `allowUsageInUserControlledCode: true`, and `--allow-gateway-usage` → `allowConnectionUsageInGateway: true`. The first is the "Allow Code-First Artifacts like Notebooks to access this connection" toggle — it is CREATE-TIME ONLY (the API rejects changing it later), which is why it belongs on create, not update. Both are top-level booleans (siblings of `connectionDetails`/`credentialDetails`) that default to false server-side, so fabio only emits them when the flag is passed. Live-verified: a connection created with the flag reports `allowUsageInUserControlledCode: true` in show/list. Pure `build_connection_body` covers them; live-validated (`connection_allow_code_first_artifacts_lifecycle`).
 - **Gateway member-count range fields**: `gateway create`/`gateway update` gained `--max-member-gateway-count`/`--min-member-gateway-count` (mutually exclusive with the legacy `--member-count` fixed value, via clap `conflicts_with_all`/`requires`), mirroring the Fabric API's new `maxMemberGatewayCount`/`minMemberGatewayCount` range pair that supersedes `numberOfMemberGateways`. `create` still defaults to a fixed count of 1 when none of the three flags are given (backward compatible); `update` applies no default (partial PATCH). See `.agents/API-BEHAVIORS-DISCOVERED.md` "Gateway Lifecycle API Behaviors Discovered" for the full mutual-exclusivity/error-code details.
- **Git Workspace Relations (Preview)**: New `fabio git relation list|create|delete` commands implement the Fabric REST `WorkspaceRelations` API (`GET/POST /workspaces/{id}/git/workspaceRelations`, `DELETE .../{relationId}`) for managing base/branch links between workspaces as an independent resource (previously only implicit via `git branch-out`). Implemented in `src/commands/git/relation.rs` (the `git` command is a directory module — `mod.rs` enum/dispatch + `sync.rs` + `connect.rs` + `branch_out.rs` + `relation.rs`). See `.agents/API-BEHAVIORS-DISCOVERED.md` "Git Workspace Relations API Behaviors Discovered (Preview)" for API semantics and error codes.
 - **Microsoft Fabric MCP server parity (OneLake shortcuts/directories)**: Audited fabio against Microsoft's official Fabric MCP server (`microsoft/mcp` → `servers/Fabric.Mcp.Server`, v1.2.0 Jul 2026). fabio was already at/beyond parity for its data-access roles (`onelake-security`, a superset), catalog search (`catalog search`), OneLake table API (`lakehouse iceberg-*`), OneLake settings (`workspace modify-diagnostics`/`modify-immutability-policy`/`reset-shortcut-cache`), and Dataflow Gen2 M execution (`dataflow execute-query --mashup`). The genuine gaps closed: (1) **`lakehouse list-shortcuts`** (`GET /workspaces/{ws}/items/{id}/shortcuts`, `--parent-path`, paginated) — mirrors the MCP's client-side hiding of DW-managed shortcuts (internal OneLake→OneLake refs under `Tables/…`, hidden unless `--include-managed`; heuristic in `is_managed_shortcut`). (2) **Typed shortcut-target creation**: `create-shortcut` gained typed flags (`--connection-id`, `--location`, `--subpath`, `--bucket`, `--target-workspace`/`--target-item`/`--target-path`, `--environment-domain`, `--delta-lake-folder`, `--update-sensitivity`) covering all 9 Fabric target types (OneLake, AdlsGen2, AmazonS3, AzureBlobStorage, GoogleCloudStorage, S3Compatible, Dataverse, ExternalDataShare, OneDriveSharePoint) in ONE command (vs the MCP's 9 separate typed tools); `--target-type` is normalized/validated (aliases + any case) and an unknown type errors with the enumerated valid values; the raw `--target` JSON remains an escape hatch. Exact target field shapes were taken from the MCP's `ShortcutModels.cs`. Pure builders (`normalize_target_type`, `build_shortcut_target`, `is_managed_shortcut`) in `src/commands/lakehouse/shortcuts.rs` are unit-tested; the typed OneLake create + list lifecycle is live-validated (`lakehouse_typed_onelake_shortcut_and_list`). (3) **`lakehouse delete-directory --path`** (recursive DFS delete, `--dry-run`-guarded, destructive) — wires up the existing `delete_onelake_directory` client method for arbitrary directories (previously only reachable via `delete-table`). Because a recursive delete of an empty/root path (`""`, `/`, `.`, `..`) would erase the ENTIRE lakehouse, `validate_delete_directory_path` refuses those outright (concrete subdirs, including the `Files`/`Tables` roots, are allowed); it is also `destructive:true` in `commands.json` and blocked by `--readonly` via the client `guard_readonly`. NOT added: the MCP's `onelake_list_items_dfs` (a DFS-transport duplicate of `fabio item list`, which already enumerates items via the Fabric REST API — no new capability). See `.agents/API-BEHAVIORS-DISCOVERED.md` "OneLake Shortcuts API Behaviors Discovered".
 - **Shortcut target-builder extracted to a shared module + KQL-database table-shortcut ergonomics (`kql-database create-shortcut` typed flags + required `enableQueryAcceleration`)**: A required-field audit of the Fabric REST specs surfaced that `kqlDatabase`'s `CreateTableShortcutRequest` REQUIRES `name`, `enableQueryAcceleration`, AND `target`, but fabio's `kql-database create-shortcut` only injected `name` into a raw `--file`/`--content` JSON body — so a caller who omitted `enableQueryAcceleration` got a server rejection, and there was NO typed way to build the target (unlike `lakehouse create-shortcut`). Fix: (1) the shortcut target-builder (`ShortcutTargetFlags`, `normalize_target_type`, `build_shortcut_target`, `VALID_TARGET_TYPES`) was extracted from `lakehouse/shortcuts.rs` into a shared `src/commands/shortcut_target.rs` (DRY — the KQL `CreatableShortcutTarget` is the same `{<discriminator>: {...}}` object as the lakehouse `Target`, minus the `type` field), with its comprehensive per-type unit tests moved alongside it. (2) `kql-database create-shortcut` gained the typed flags (`--target-type`, `--connection-id`, `--location`, `--subpath`, `--bucket`, `--target-workspace`/`--target-item`/`--target-path`, raw `--target`) plus `--enable-query-acceleration`; the typed path builds `{name, enableQueryAcceleration, target: {<disc>: {...}}}` via the shared builder, while `--file`/`--content` remain an escape hatch that now GUARANTEES the required `enableQueryAcceleration` field (injected from the flag when absent). (3) A KQL table shortcut supports only the SIX storage targets (OneLake, AmazonS3, AdlsGen2, GoogleCloudStorage, S3Compatible, AzureBlobStorage) — Dataverse/ExternalDataShare/OneDriveSharePoint are rejected offline by `ensure_kql_target_supported` (checked on the normalized discriminator BEFORE type-specific flag validation, so the error names the real problem). (4) `list-shortcuts` now reads the `{value:[…]}` envelope and surfaces the `enableQueryAcceleration` column. Endpoints unchanged (list/create/get/delete on `/kqlDatabases/{id}/shortcuts`, TableShortcuts API); `create-shortcut` stays `mutates:true` (not destructive). Pure `build_kql_shortcut_body`/`ensure_kql_target_supported` (in `kql_database/mod.rs`) + the shared builder are unit-tested; the OneLake table-shortcut lifecycle is live-validated (create → list → get(`enableQueryAcceleration:true`) → `salesshortcut | count`==2 → delete) plus offline e2e (typed dry-run body, dataverse rejection, delete dry-run). See `.agents/API-BEHAVIORS-DISCOVERED.md` "KQL Database Table Shortcut Behaviors".
 - **Item definition part requirements + offline validation (determinism for agents)**: A single source of truth — `src/commands/context/data/agent/definition_requirements.json`, loaded by the top-level `src/definition_spec.rs` module — captures each item type's CANONICAL definition part paths (what `getDefinition`/Git export/`deploy` round-trip), `definitionFormat`, accepted aliases, and authoring notes (ground-truthed live). It powers three drift-free capabilities: (1) **`fabio item validate-definition`** — an OFFLINE validator (no API call) that checks a definition envelope (`--file`/`--definition`) or a folder of parts (`--dir`) before create/update-definition. Universal envelope rules are ERRORS (`MISSING_PARTS`, `INVALID_PAYLOAD_TYPE` — only `InlineBase64`, `INVALID_BASE64`, `INVALID_JSON_PART`, `DUPLICATE_PART`, …); per-type canonical-part gaps are WARNINGS (Fabric tolerates alias filenames), promoted to failures with `--strict`. Zero false positives verified against real exported CopyJob/Dataflow/DataPipeline/SparkJobDefinition/Notebook folders. (2) **Enriched definition-authoring hints** (`definition_spec::definition_input_hint`) that enumerate the required part path(s) + `definitionFormat`, show the envelope shape, and point at `fabio context schema <Type>` and the offline validator. (3) **`fabio context schema <Type>`** now merges an authoritative `definition_requirements` block (canonical parts never drift from Fabric), and serves spec-backed responses for types that lacked a hand-authored schema (KQLDashboard, Map, OperationsAgent, …). `fabio context find` now also indexes item schemas + output examples (previously it did not, so an agent searching "notebook definition format" was never routed to `context schema`). **Fabric alignment fixes**: `copy-job update-definition` now emits the canonical `copyjob-content.json` (was `CopyJobV1.json`); `dataflow update-definition` (previously a NO-OP that wrapped input as a single ignored `dataflow.json` part) now passes a full multi-part envelope through verbatim via the shared `definition_spec::build_update_definition_body`, so the reliable pattern is `get-definition` → edit → `update-definition --file <envelope.json>`. `SparkJobDefinition` keeps the canonical `SparkJobDefinitionV1.json` FILENAME (format `SparkJobDefinitionV2`). See `.agents/API-BEHAVIORS-DISCOVERED.md` "Item Definition Part Requirements & Offline Validation".
   - **Copy job on-demand run (`copy-job run`) + shared 202-`Location` job-instance fix**: A copy job is the recommended Fabric ingestion primitive and the Data Factory E2E tutorial explicitly "Run"s it, but `copy-job` had no `run` — only the generic `job-scheduler run-on-demand`. Added `copy-job run --workspace <ws> --id <id> [--wait --timeout <s> --cancel-on-timeout]`, matching the `data-pipeline run`/`dataflow run` convenience. The copy-job on-demand run job type is `Execute` (`POST /workspaces/{ws}/items/{id}/jobs/instances?jobType=Execute` → 202; the resulting instance's own `jobType` is reported as `CopyJob`). While wiring `--wait`, discovered a **latent shared bug**: the run trigger returns `202 Accepted` with an EMPTY body and the new instance URL in the `Location` header, but `data-pipeline run` (and the initial `copy-job run` draft) extracted the instance id from `body["id"]` (always empty) → `--wait` polled an empty id and always timed out. Both now route through the existing `client::trigger_item_job(ws, id, job_type, execution_data)`, which reads the id from the 202 `Location` header (last path segment). Live-validated: `copy-job run --wait` triggers → polls → returns `"Completed"`; `data-pipeline run` now returns a real `jobInstanceId` (was empty). NOTE copy-job DEFINITION authoring for data-movement activities (source/destination connectors, the tutorial's "Sample data" NYC-Taxi source) is portal-oriented and NOT cleanly REST-authorable — a Lakehouse→Lakehouse `copyjob-content.json` activities array is rejected server-side with a generic `UnknownError`; `run` itself is fully API-backed and validated. Pure plumbing (no new pure helper); e2e `copy_job_dry_run_run` (offline) + `copy_job_run_and_wait` (gated on `FABIO_TEST_COPY_JOB_ID`). See `.agents/API-BEHAVIORS-DISCOVERED.md` "Copy Job API Behaviors Discovered".
   - **`spark-job-definition run` had the SAME empty-jobId bug (found live, fixed) — and it was the LAST one**: `spark-job-definition run` still POSTed `/items/{id}/jobs/instances?jobType=sparkjob` directly and read the instance id from `body["id"]` (always empty on the 202) instead of the `Location` header — so `--wait` polled an empty id and ALWAYS timed out (the timeout message even printed `Job ID: ` with nothing after it). Fixed to route through `client::trigger_item_job(workspace, id, "sparkjob", None)` (same as `data-pipeline`/`copy-job run`). Live-verified reproducing the create-spark-job-definition tutorial: BEFORE → `--wait` timed out with an empty job id; AFTER → `run` returns a real `jobInstanceId` and `run --wait` polls the real Spark job to `Completed`. A follow-up audit of EVERY `jobs/instances?jobType` POST + every `async fn run` confirmed this was the last command reading the id from the body: the other `run`s (`notebook`, `data-pipeline`, `copy-job`, `dataflow`, `data-build-tool-job`, `job-scheduler`) all already use `trigger_item_job`/`trigger_item_job_at`, and the three remaining direct instance-POSTs (`deploy` post-run hook, `lakehouse optimize-table`/`vacuum-table`, `graph-model refresh-graph`) are either fire-and-forget or poll the ITEM status (not a job id), so none were affected. Offline e2e `spark_job_definition_run_dry_run`.
  - **Ontology MCP consumption endpoint**: `fabio ontology mcp-url --workspace <ws> --id <id>` prints the canonical Model Context Protocol server URL for consuming a Fabric ontology (preview) item as an MCP server — `{fabricBase}/mcp/dataPlane/workspaces/{ws}/items/{id}/ontologyEndpoint` — which external MCP clients (VS Code agent mode, Claude, Copilot Studio) connect to over HTTP transport with Fabric auth. NOTE the URL shape DIFFERS from the data-agent one (`/mcp/dataPlane/.../items/.../ontologyEndpoint` vs `/mcp/workspaces/.../dataagents/.../agent`) — an agent cannot guess it, so fabio constructs it. Handler lives in `src/commands/ontology/mcp.rs` (the `ontology` command is a directory module — `mod.rs` enum/dispatch + `crud.rs` + `definitions.rs` + `import.rs` + `mcp.rs`). Pure `build_mcp_url` is unit-tested; the command does a lightweight existence check (`exists` field + prerequisite `note`, or a `hint` when not found). Live-verified end-to-end: the constructed URL is a real, working MCP server — an `initialize` handshake returns `serverInfo: "Microsoft Fabric Ontology" v1.0.0` with a `tools` capability. This is distinct from grounding a fabio data-agent ON an ontology (`data-agent add-datasource --artifact-type Ontology`); mcp-url exposes the ontology ITSELF over MCP. Requires an F2+/P1 capacity and the Ontology-item preview tenant setting. See `.agents/API-BEHAVIORS-DISCOVERED.md` "Ontology MCP Server (Preview)".
 - **Eventhouse / KQL-database remote MCP endpoint**: `fabio kql-database mcp-url --workspace <ws> --id <id>` prints the canonical hosted remote MCP server URL for consuming a KQL database (in an eventhouse) with AI agents — `{fabricBase}/mcp/dataPlane/workspaces/{ws}/items/{id}/kqlEndpoint`. Same generic `dataPlane/.../items/...` shape as the ontology MCP URL, but with a `kqlEndpoint` suffix (ontology uses `ontologyEndpoint`); the `id` is the KQL DATABASE item id (the portal's "Database details" pane), not the eventhouse id. External MCP clients (VS Code agent mode, GitHub Copilot, Copilot Studio, Azure AI Foundry) connect over HTTP transport with a Fabric credential; the server exposes tools to discover KQL schemas, generate KQL from natural language, execute queries, and sample data. Handler in `src/commands/kql_database/mcp.rs` (`kql-database` is now a directory module — `mod.rs` + `intelligence.rs` + `mcp.rs`). Pure `build_mcp_url` is unit-tested; the command does a light existence check (`exists` field + `note`, or a `hint` when not found). Live-verified end-to-end: the constructed URL is a real, working MCP server — an `initialize` handshake returns `serverInfo: "KustoMCP" v1.0.0` with a `tools` capability. A global variant (`.../mcp/dataPlane/kqlEndpoint`, workspaceId+itemId passed per tool call) also exists but fabio prints the deterministic per-item URL.
 - **Eventhouse / KQL-database MCP client (`kql-database examples`)**: fabio consumes the eventhouse remote MCP server as a CLIENT (via the generic `src/mcp_client.rs`, the same path `ontology search` uses). `fabio kql-database examples --workspace <ws> --id <kqlDbId> --prompt "<NL>" [--general-only | --specific-only]` drives the server's two example-retrieval tools — `getGeneralKQLExamples` (curated PUBLIC NL→KQL example pairs relevant to the prompt) and `getSpecificKQLExamples` (examples curated/learned from THIS database) — returning `{prompt, endpoint, generalExamples:{text,isError}, specificExamples:{text,isError}}`. This is grounding for authoring KQL that fabio has NO offline equivalent for (schema is available via `describe`/`list-entities`, execution via `query`, generation via `rti nl-to-kql`). Live-verified via a `tools/list` on the KQL MCP server: it exposes 4 tools — `executeQuery` (duplicates `kql-database query`), `getSchema` (rich schema + column value SAMPLES + cardinality STATS + authoring guidance — surfaced by `kql-database schema-context`), and the two example tools driven by `kql-database examples`. `kql-database schema-context --workspace <ws> --id <kqlDbId> --prompt "<NL>"` drives `getSchema`: unlike the offline `describe`/`list-entities` (raw schema), it returns the semantically-relevant tables/MVs/external-tables + functions PLUS column value samples, cardinality/distinct-value stats, and KQL-authoring guidance (grounding fabio can't produce offline — the samples come from real data), as `{prompt, endpoint, schema:{text,isError}}`. The grounding tools return an `isError` "Database is empty" when the KQL database has no tables (they ground on schema/data). Pure `select_example_scope` in `src/commands/kql_database/mcp.rs` is unit-tested; the create-eventhouse → seed-table → examples/schema-context loops are live-validated (`kql_database_examples_lifecycle`, `kql_database_schema_context_lifecycle`). The URL is HTTPS+trusted-host-validated before the Fabric bearer token is sent, and `--dry-run` prints the plan without any network call.
 - **Ontology entity-type listing (MCP-tool parity, offline)**: `fabio ontology list-entity-types --workspace <ws> --id <id> [--entity-name <name>] [--include-properties]` is the pure-fabio equivalent of the ontology MCP server's `list_ontology_entity_types` tool — it reproduces that tool's `{"values":[...]}` output BYTE-FOR-BYTE (verified live for all param combinations), computed OFFLINE from `getDefinition`'s `EntityTypes/*/definition.json` parts (no MCP session). The reshape reorders fields to the tool's key order (relies on serde_json `preserve_order`), strips `$schema` + null property fields, and defaults `documents`/`mappings`/`resourceLinks` to `[]`. The ONLY field it cannot reproduce is the server-assigned `etag` (a per-entity concurrency token absent from the definition) — omitted. Handler + pure reshape helpers in `src/commands/ontology/entity_types.rs` are unit-tested against the exact live MCP output; a live e2e (`ontology_list_entity_types_matches_mcp_shape`) validates it end-to-end. Established via a live `tools/list` comparison of BOTH Fabric MCP servers: the data-agent MCP server's single tool (`DataAgent_<name>(userQuestion)`) is already covered by `fabio data-agent query`, and the ontology server's other tool (`search_ontology`, NL query) is now covered too (see the MCP-client decision below). See `.agents/API-BEHAVIORS-DISCOVERED.md` "Ontology MCP tools vs. pure fabio".
 - **Ontology granular element editing (`add/delete-entity-type`, `rename-entity-type`, `add/delete-relationship-type`, `add/list/delete-report-link`)**: The Fabric ontology REST API is definition-based (CRUD + `getDefinition`/`updateDefinition`; NO per-element endpoints), so these commands (in `src/commands/ontology/elements.rs`) implement client-side **read-modify-write**: fetch the definition parts, add/remove/edit exactly one element, push the whole set back via `updateDefinition` (LRO). Part shapes match `import`/`generate` — `EntityTypes/{id}/definition.json` (`namespaceType: "Custom"`, `visibility: "Visible"`, `properties[]` with `valueType`, `entityIdParts` for keys), `RelationshipTypes/{id}/definition.json` (`source`/`target.entityTypeId`), `EntityTypes/{id}/ResourceLinks/definition.json` (`{type: "PowerBIReport", workspaceId, itemId}`). `add-entity-type --name --property NAME:TYPE (repeatable) --key PROPNAME` (**valueType bug fixed**: the Fabric `entityType/1.0.0` schema's `valueType` enum is EXACTLY `String`/`Boolean`/`DateTime`/`Object`/`BigInt`/`Double` — `Long`, `Decimal`, and `Any` are NOT valid typed-property types and the API rejects the whole import with a generic `ALMOperationImportFailed`; `normalize_value_type` previously emitted `Long`/`Decimal`/`Any` verbatim, so EVERY `add-entity-type` with an integer/decimal/any property was silently broken. Now `long`/`int`/`integer`/`bigint`→`BigInt`, `double`/`float`/`number`/`decimal`/`currency`→`Double`, `object`/`any`→`Object`, so every reasonable input maps to a schema-valid type; `Any` is valid ONLY inside `untypedProperties`, which this path never writes. Ground-truthed against the published schema enum AND live); `rename-entity-type --entity <name|id> --new-name` rewrites ONLY the entity's `name` field (relationship types + data bindings reference the stable `entityTypeId`, so they are unaffected — reproduces the Ontology tutorial's dimproducts→Products/dimstore→Store/factsales→SaleEvent renames; conflict on an existing name); `add-relationship-type --name --source --target` (entity refs resolve by name OR id); `add-report-link --entity --report-workspace --report` (Power BI report `ResourceLink`); `list-report-links [--entity]`; the three `delete-*` are `destructive:true`, dry-run-guarded, and `delete-entity-type` **cascades** relationship types referencing the removed entity (reported as `cascadedRelationshipTypes`, previewable via `--dry-run`). New numeric element ids are collision-checked against every existing id (`collect_ids` + `gen_unique_id`). NOTE these edit the *definition* only — they do NOT provision Fabric IQ reasoning (still portal-exclusive, see the `ontology search` gates). Pure helpers (`normalize_value_type`, `parse_property_spec`, `build_entity_type_def`/`build_relationship_type_def`/`build_report_link`, `rename_entity_in_parts`, `resolve_entity_id`, `collect_ids`/`gen_unique_id`, `encode_part`/`part_json`) are unit-tested (11 tests); the full add→rename→verify→delete lifecycle (incl. the relationship cascade + rename round-trip) is live-validated in `ontology_granular_elements_lifecycle`.
 - **MCP client (fabio consuming external MCP servers) + `ontology search`**: fabio gained its first MCP-CLIENT capability — `src/mcp_client.rs`, a GENERIC Model Context Protocol client over the streamable-HTTP transport (`McpClient::connect` runs the `initialize` handshake + `notifications/initialized`, then `list_tools`/`call_tool`; handles both `application/json` and `text/event-stream`/SSE responses; echoes an `Mcp-Session-Id` if the server assigns one — Fabric's servers are stateless and omit it). It is the counterpart of `fabio mcp serve` (fabio as an MCP *server*) and is NOT ontology-specific: it takes an endpoint URL + an optional `Authorization` header. First consumer: **`fabio ontology search --workspace <ws> --id <id> --prompt "<q>" [--raw]`**, which drives the ontology MCP server's `search_ontology` tool (NL query over the ontology data estate — the one Fabric-IQ-reasoning capability with no offline equivalent). It builds the ontology MCP URL (same as `mcp-url`), HTTPS+trusted-host-validates it (`validate_trusted_url`) before sending the Fabric bearer token (`require_auth`), confirms the tool exists via `list_tools`, then calls `search_ontology` with `{naturalLanguageQuery, naturalLanguageResponse=!--raw}`; output is `{"query","answer","isError"}`, `--dry-run` prints the plan without any network call. Pure parsers in `mcp_client.rs` (`parse_rpc_response` for JSON + SSE, `sse_data_blocks`, `ToolResult::text`) are unit-tested; a deterministic `--dry-run` e2e + a live e2e (`ontology_search_drives_mcp_client`) validate the client end-to-end. Live-verified: fabio's `ontology search` is BYTE-IDENTICAL to a raw MCP `tools/call` (side-by-side curl). A successful NL *answer* additionally needs the ontology bound to data AND two server-side gates — without them the tool returns `isError:true` "The natural language query could not be processed", which fabio faithfully surfaces (exiting non-zero). The two gates were live-diagnosed and cleared: (1) **tenant settings** — the ontology NL query is data-agent/Azure-OpenAI-powered, so the tutorial's required tenant settings must be ON: `OntologyPreview` ("Enable Ontology item"), `EnableAOAI` ("Users can use Copilot… powered by Azure OpenAI"), and crucially `AllowSendAOAIDataToOtherRegions` ("Data sent to Azure OpenAI can be processed outside your capacity's geographic region") — this last one was OFF and is the gate when the capacity's region (e.g. Sweden Central) lacks an in-region Azure OpenAI model. Flipping it via `fabio admin update-tenant-setting --setting-name AllowSendAOAIDataToOtherRegions --content '{"enabled": true}'` (requires Fabric-admin) changed EVERY query from `isError:true` "could not be processed" to `isError:false` with grounded answers (live-verified: "How many stores are there?" → "There are 3 stores"; "List all products with their categories" → the real product rows). (2) **portal-GENERATION of the ontology (not just opening it)** — Fabric IQ reasoning answers NL queries only for an ontology *created/generated in the Fabric portal*; an ontology built entirely through fabio/the public REST API cannot get the NL-reasoning surface (it is still complete and GQL/graph-queryable). A **portal-generated** ontology (`OntoComparePortal`) returns grounded answers; fabio-created ontologies (`RetailSalesOntology`, `OntoCompareFabio2`) return `isError:true` even after gate (1) is cleared AND even after being opened in the portal. The root cause was pinned down by an export DIFF: the two definitions are functionally identical except `namespaceType` — portal writes `"Imported"`, fabio writes `"Custom"` — and Fabric IQ reasoning keys off the `"Imported"` form. Crucially, fabio (or any external caller) CANNOT reproduce `"Imported"`: patching it via `updateDefinition` is silently normalized back to `"Custom"` (the published `entityType` schema declares `namespaceType` as `const: "Custom"`, so the public API forbids `"Imported"`). So this is a **Fabric platform limitation, NOT a fabio definition gap** — the reasoning-provisioning marker (`"Imported"`) and server-side reasoning state are both portal-exclusive; nothing is missing from or fixable in fabio's definition output. This is NOT a fabio bug — the `ontology search` command itself works end-to-end (proven live on the portal-generated ontology). See `.agents/API-BEHAVIORS-DISCOVERED.md` "MCP client (fabio consuming external MCP servers)".
   - **Remote Power BI MCP server client (`semantic-model generate-dax`/`copilot-schema`, `report copilot-metadata`)**: The **remote Power BI MCP server** (`{fabricBase}/mcp/powerbi`, streamable HTTP, Entra OAuth, server `PowerBIMCPServer v1.0.0`) exposes Copilot-powered tools that have NO direct fabio/REST equivalent. fabio consumes it as an MCP CLIENT via the generic `src/mcp_client.rs` (the same path `ontology search` uses); the shared connect/URL/parse helpers live in `src/commands/powerbi_mcp.rs` (pure `powerbi_mcp_url(base)` → `{base}/mcp/powerbi`; `call_powerbi_tool`; `tool_text_as_json`). Live `tools/list` revealed FOUR tools: `ExecuteQuery` (duplicates fabio's `semantic-model query --dax`, so NOT wrapped), `GenerateQuery`, `GetSemanticModelSchema`, `GetReportMetadata` — the last three are wrapped: (1) **`semantic-model generate-dax --id <model> --prompt "<NL>" [--execute]`** drives `GenerateQuery` (Copilot NL→DAX; the same engine as Copilot for Power BI) → `{prompt, dax, model}`; `--execute` also runs the generated DAX via the existing `executeQueries` path (`operations::execute_dax_and_render`, now `pub(super)`) and returns rows. Requires a Copilot license. (2) **`semantic-model copilot-schema --id <model>`** drives `GetSemanticModelSchema` → the Copilot-oriented serialized schema + the author's **custom instructions / AI-optimized "prepare data for AI" metadata** — which fabio's `INFO.VIEW.*` `list-*` commands CANNOT surface. (3) **`report copilot-metadata --id <report>`** drives `GetReportMetadata` → a synthesized report schema (workspace, semantic model, pages, visuals with field→role projections/bindings, textboxes, `PromptInstructions`) — distinct from `report get-definition` (raw PBIR). **Multi-block responses**: the Power BI MCP tools return MULTIPLE text content blocks (e.g. `GetSemanticModelSchema` returns a `{schema,semanticModel}` block PLUS an `{artifact_citation}` block), so `tool_text_as_json` parses EACH block and MERGES the object blocks (naïvely joining then parsing fails with "Extra data"). The endpoint is a single FIXED global URL (not per-item); the tools resolve the artifact by GUID (so `--workspace` is kept for group consistency but not sent). Requires the tenant setting **`PowerBIMCP`** ("Users can use the Power BI Model Context Protocol server endpoint (preview)"). All three are read-only (`mutates:false`), `--dry-run`-guarded before any MCP call, and HTTPS+trusted-host-validated before the Fabric bearer token is sent. **Feature-disabled detection is now generic** (see the `tenant_gate` decision): the `PowerBIMCP` tenant setting can be OFF, so a 403 at the MCP `initialize` handshake (the endpoint's `FeatureNotAvailable` gate) propagates and is turned by the generic `commands::tenant_gate` interception into a teaching `FORBIDDEN` (`hintType: semantic_correction`) that names `PowerBIMCP`, gives the admin-aware enable command, and lists the non-MCP fallbacks (`semantic-model query --dax`, `list-tables`/…, `report get-definition`). Pure helpers unit-tested; offline dry-run e2e + live e2e (`semantic_model_generate_dax_and_copilot_schema_live` gated on `FABIO_TEST_SEMANTIC_MODEL`, `report_copilot_metadata_live` gated on `FABIO_TEST_REPORT`). Live-validated end-to-end: `generate-dax "total revenue by country" --execute` on a framed model returned `[{France:309.83},{Germany:879.47},{USA:1089.44}]`. See `.agents/API-BEHAVIORS-DISCOVERED.md` "Remote Power BI MCP server".
   - **Generic tenant-feature-gate teaching errors (`commands::tenant_gate`)**: MANY Fabric features are gated by a TENANT SETTING an admin can toggle; when one is off, the API returns an opaque `403 FeatureNotAvailable`. Rather than handle this per-command, fabio has ONE generic interception wired into `commands::execute` (which has both the parsed command path and the `FabricClient` in scope — `main.rs` is too late, `cli`/`client` are already moved): the dispatch result is passed through `tenant_gate::enrich(&client, &command_path, err)`. If `is_feature_disabled(err)` (a **marker-based** check of the error Display + a `FabioError`'s hint for `FeatureNotAvailable`/"feature is not available"/"not enabled in the tenant"/`TenantSwitchDisabled`/"tenant setting … disabled" — NOT "any 403", so a normal RBAC 403 is never mislabeled), it: (1) probes `is_fabric_admin` (`GET /admin/tenantsettings`, admin-only) → `Some(true)`/`Some(false)`/`None`; (2) looks up `setting_for_command(path)` in a REGISTRY mapping command paths → `{settingName, title, fallback?}` (exact subcommand first, then group-level); (3) builds a teaching `FORBIDDEN` (`hintType: semantic_correction`) that NAMES the setting, gives an **admin-aware** enable clause (admin → the exact `fabio admin update-tenant-setting --setting-name <NAME> --content '{"enabled": true}'`; non-admin → "ask your admin", no command they can't run; unknown → non-committal), and appends any feature-specific fallback. When the command is NOT in the registry (setting unknown) it still emits a generic admin-aware hint ("list settings with `fabio admin list-tenant-settings` to find and enable it"). The registry (built from a live `admin list-tenant-settings` dump) covers `PowerBIMCP` (semantic-model generate-dax/copilot-schema, report copilot-metadata), `ArtifactDatabricksStoragePreview` (azure-databricks-storage), `OntologyPreview` (ontology), `DigitalOperationsPreview` (digital-twin-builder), `ArtifactMirroredCatalogPreview` (mirrored-catalog), `AppBackendTenant` (app-backend), `AllowExternalDataSharingSwitch`/`AllowExternalDataSharingReceiverSwitch` (item external-data-share / accept), `PublishToWeb` (report publish-to-web). This **supersedes** the per-feature handling that used to live in `powerbi_mcp.rs` (removed — DRY) and the generic `<NAME>` branches in `errors.rs::forbidden_hint`. Pure `is_feature_disabled`/`setting_for_command`/`build_hint`/`build_message`/`enable_clause` unit-tested (incl. non-flagging of a normal RBAC 403 and all admin×known/unknown-setting hint variants); live-validated by disabling `PowerBIMCP` (subcommand-mapped) AND `ArtifactDatabricksStoragePreview` (group-mapped) and confirming each command returns the precise named-setting admin-aware error, that the happy path is restored on re-enable, and that a normal `NOT_FOUND` is NOT tenant-enriched. To gate a NEW feature: add its `(command-path → settingName/title/fallback)` row to `setting_for_command`.
  - **Ontology generation from a semantic model OR a lakehouse (`ontology generate`, portal-parity client-side)**: The Fabric portal's "Generate Ontology" has NO public REST API (the ontology REST surface is CRUD-only), so `fabio ontology generate --workspace <ws> (--semantic-model <id> | --lakehouse <id>) --name <name> [--lakehouse <id>] [--output-owl <file>]` reproduces it CLIENT-SIDE from EITHER of two schema sources (exactly one required; the source resolution lives in `resolve_schema` in `src/commands/ontology/generate.rs`). **Semantic-model source** (`--semantic-model`): reads the model's tables/columns/relationships via the DAX `INFO.VIEW.*` functions (reusing `semantic-model list-tables/list-columns/list-relationships` — `fetch_info_view` is `pub`), and derives keys from relationships (the "one"-side `ToColumn`→`isIdentifier`). **Lakehouse source** (`--lakehouse` WITHOUT `--semantic-model`): reads the lakehouse SQL analytics endpoint's `INFORMATION_SCHEMA.COLUMNS` (base tables only, joined to `INFORMATION_SCHEMA.TABLES WHERE TABLE_TYPE='BASE TABLE'`) via a new `tds_utils::execute_sql_rows` (row-returning counterpart of `execute_and_render_sql`) + `tds_utils::resolve_lakehouse_sql` (moved here from `lakehouse/insights.rs` so both callers share it); since a lakehouse has no declared relationships or PK, there are no `owl:ObjectProperty` edges and the FIRST column of each table (lowest `ORDINAL_POSITION`) is the identifier heuristic (reviewable via `ontology update-definition`). Either source synthesizes an OWL model (pure `build_owl`: table→`owl:Class`, column→typed `owl:DatatypeProperty`, key→`isIdentifier`, relationship→`owl:ObjectProperty` many→one) and runs through the existing `ontology import` path (create → `import_owl`), so entity types + typed properties + relationship types + keys + data bindings are all produced; the lakehouse source auto-binds each entity to its same-named lakehouse table (`--lakehouse` IS the bind target too, so `DataBindings/*` parts reference `sourceType: LakehouseTable`, `sourceTableName`, `sourceSchema: dbo`). **Type maps**: INFO.VIEW `DataType` (semantic model) — `Text`→string, `Integer`→long, `Number`→double (Power BI's floating-point "Decimal Number" surfaces as `Number`, NOT `Double`), Currency/Decimal→decimal, DateTime→dateTime, Boolean→boolean; the synthetic hidden `RowNumber-*` column is excluded. T-SQL `INFORMATION_SCHEMA.DATA_TYPE` (lakehouse, `map_sql_type`) — bit→boolean, tinyint/smallint/int/bigint→long, real/float→double, decimal/numeric/money→decimal, date/time/datetime*/datetimeoffset→dateTime, else (varchar/nvarchar/char/uniqueidentifier/…)→string. `import_owl` has a `suppress_output` flag so generate emits ONE JSON object. **Portal-parity keys/display names (`generate_fabric_parts` in `import.rs`)**: validated by diffing fabio's output against the portal's server-side "Generate ontology" for the SAME model — they are structurally identical. fabio now matches the portal on the two fields that were bugs: (1) `entityIdParts` comes ONLY from explicit identifiers (relationship "one"-side / `ont:isIdentifier`) — NO first-property fabrication, so a fact table with no natural key is left keyless (`[]`), exactly like the portal; (2) `displayNamePropertyId` is left **`null`** (JSON null — the `entityType/1.0.0` schema types it `["string","null"]` and `updateDefinition` accepts null; the old "cannot be empty" error was only for the empty STRING). fabio DELIBERATELY keeps `namespaceType: "Custom"` (the official schema declares it `const: "Custom"`; the portal's `"Imported"` violates its own schema) and `DataBinding.sourceSchema: "dbo"` (explicit/correct; portal leaves it null) — the only two remaining diffs, where fabio is more schema-correct than the portal. Live-verified end-to-end: (a) a 3-table retail model → 3-entity ontology with 17 typed properties, keys, 2 relationships, and queryable lakehouse `DataBindings` (tutorial Part 1); (b) a throwaway lakehouse (dimstore/dimproducts/factsales) → 3-entity ontology, 17 typed properties (Latitude/RevenueUSD→double, SaleId/Units→long, SaleDate→dateTime), first-column keys (ProductId/StoreId/SaleId), and `DataBindings` to the lakehouse tables. Pure `build_owl`/`map_datatype`/`map_sql_type`/`lakehouse_schema_from_rows`/`relationship_keys`/`is_synthetic_column`/`summarize` are unit-tested; live e2e `ontology_generate_from_semantic_model` (model source) and `ontology_generate_from_lakehouse` (lakehouse source, creates its own throwaway lakehouse + polls the SQL endpoint for table sync) validate both create+import paths. The FULL tutorial (lakehouse+eventhouse+ontology+data-agent+NL query) is reproducible with fabio — see the `ontology_tutorial` workflow. See `.agents/API-BEHAVIORS-DISCOVERED.md` "Ontology generation from a semantic model".
 - **Eventstream sample-data source + event-processor operators (`add-sample-source --sample-type`, `add-operator`, operators in `list-components`)**: Reproducing the Eventstream transform tutorial surfaced three gaps in eventstream topology authoring. (1) `add-sample-source` sent `properties:{}` for the `SampleData` source, which the API rejects (`Required property 'type' not found … Path 'properties'`) — it now takes `--sample-type` (`Bicycles`/`YellowTaxi`/`StockMarket`/`Buses`, normalized by the pure `normalize_sample_type`) and sets `properties.type`. (2) The topology's `operators[]` transform layer (Filter/ManageFields/Aggregate/GroupBy/Join/Union/Expand) had NO fabio authoring path — `list-components` didn't enumerate operators and `add-derived-stream` writes to `streams[]`. Added `eventstream add-operator --name --type --input-node --properties` (type-validated against the 7 `OPERATOR_TYPES`) that appends `{name, type, inputNodes, properties}` to `operators[]`, plus the operators to `list-components --category operator`. (3) The **Filter** condition field is `operatorType`, NOT `operator` (the REST-API doc example is wrong): `{conditions:[{column:{node,columnName,columnPath:[]}, operatorType:"Equals", value:{dataType,value}}]}`. Live-validated the add-sample-source→add-operator loop (a Filter operator lands, eventstream goes `Running`); pure helpers unit-tested; dry-run + operator-enumeration e2e-tested. See `.agents/API-BEHAVIORS-DISCOVERED.md` "Eventstream" behaviors.
 - **Eventstream Eventhouse destination: per-mode `--properties` validation (`add-destination --destination-type Eventhouse`)**: The Eventhouse destination has TWO `dataIngestionMode` values with DISJOINT required fields, and supplying the wrong shape is accepted by `updateDefinition` then fails SILENTLY (the destination sits forever in `status:"Warning"`, ingests 0 rows, logs no `.show ingestion failures`). **`ProcessedIngestion`** (the self-contained, pure-REST-authorable path — the eventstream provisions ingestion + auto-creates the table itself) needs `{workspaceId, itemId, databaseName, tableName, inputSerialization}` → `status:"Running"`; **`DirectIngestion`** needs `{workspaceId, itemId, connectionName, mappingRuleName}` referencing a PRE-EXISTING Kusto data connection + mapping you created on the Eventhouse first → `status:"External"`. `add-destination` now runs `validate_eventhouse_destination` (in `eventstream/builder.rs`) BEFORE any network call: a missing/invalid `dataIngestionMode`, or a mode's missing required fields, → a teaching `INVALID_INPUT` (`hintType: semantic_correction`) with the exact per-mode example, instead of the silent `Warning`. Discovered reproducing the eventstream→eventhouse streaming tutorial (a first attempt wrongly used `DirectIngestion` with the ProcessedIngestion fields); live-verified `ProcessedIngestion` streams real Bicycles rows (1110→3120+) end-to-end via pure REST with no portal step. Pure validator has 6 unit tests; e2e asserts the teaching error. NOTE `get-destination-connection` returning `Connection is not available for the type 'Kusto'` for an Eventhouse destination is EXPECTED (that endpoint is for Event-Hub-compatible endpoints), NOT the ingestion signal. See `.agents/API-BEHAVIORS-DISCOVERED.md` "Eventstream" behaviors.
 - **Open mirroring (`mirrored-database create --open-mirroring` + `landing-zone`)**: An OPEN mirrored database (push-based, no source connection) needs `mirroring.json` source type `GenericMirror` (target `MountedRelationalDatabase`, Delta) — `create` with only a `displayName` leaves it `MirroringDefinitionMissing`. `create --open-mirroring [--default-schema dbo]` posts the item then `updateDefinition`s the GenericMirror definition (retrying the brief `MirroredDatabaseNotReady` window). `mirrored-database landing-zone` prints the OneLake landing-zone URL (`onelake.dfs.fabric.microsoft.com/<ws>/<id>/Files/LandingZone`, pure construction, `mutates:false`); push a 20-digit `<n>.parquet` per table + a `_metadata.json` (`{keyColumns:[…]}`) under `Files/LandingZone/<Table>/` via `lakehouse upload --id <mirroredDbId>` (fabio's OneLake uploader is item-generic), then `mirrored-database start`. Pure `open_mirroring_definition` unit-tested; create→get-definition→landing-zone live-validated. See `.agents/API-BEHAVIORS-DISCOVERED.md` "Mirrored Database API Behaviors Discovered".
 - **Mirroring status endpoints are POST (bug fix)**: `mirrored-database status` and `table-status` used `client.get(...)` against `getMirroringStatus`/`getTablesMirroringStatus`, but those are POST actions — every call returned `EntityNotFound: The requested resource could not be found` (both commands were completely broken; a stale API-BEHAVIORS note wrongly said "GET"). Fixed to POST with an empty body. `status` → `{status}` (Initialized/Running/…); `table-status` → a PAGINATED per-table list (`{continuationToken, data:[{sourceSchemaName, sourceTableName, status, metrics}]}`) now rendered as a proper list (`{data:[…], count}` + continuation token) so agents can filter (`--query "[?status!='Replicating']"`). Live-verified on an open mirrored database: GET → `EntityNotFound`, POST → `{"status":"Initialized"}`. e2e `mirrored_database_status_and_table_status` (gated on `FABIO_TEST_MIRRORED_DATABASE_ID`).
 - **Activator (Reflex) remote MCP server: `reflex mcp-url` + native rule management (MCP client)**: The Fabric Activator remote MCP server (`{fabricBase}/mcp/workspaces/{ws}/reflexes/{id}`, server `"Reflex Mcp Server"`, OAuth, SSE, stateless) exposes rule-management tools that have NO Fabric REST API equivalent. Live `tools/list` revealed SIX tools (the doc lists only four — `delete_rule` and `get_activations_for_rule` are undocumented): `create_rule`, `list_rules`, `start_rule`, `stop_rule`, `delete_rule`, `get_activations_for_rule`. Each takes a single nested param object (`listRulesParams`/`startRuleParams` = `{artifactId, workspaceId[, ruleId]}`; `getActivationsParams` adds `startTime`/`endTime`/`objectIds`/`maxResults`). fabio exposes **`reflex mcp-url`** (prints the URL for external clients to author rules via NL, like `data-agent`/`ontology`/`kql-database mcp-url` — but the Activator URL uses the data-agent-style `/mcp/workspaces/{ws}/reflexes/{id}` shape, NOT the `dataPlane/.../items/...` shape) and drives the deterministic management tools NATIVELY via the generic `mcp_client`: **`reflex list-rules`**, **`start-rule`**, **`stop-rule`**, **`delete-rule`** (destructive), **`rule-activations`** (fired-alert history — the "query alert state" capability fabio previously lacked entirely). All live in the sibling module `src/commands/reflex_mcp.rs` (wired into `reflex.rs` via `#[path]`, since `reflex.rs` is near the 1500-line limit). The mutating tools bypass the `FabricClient` request helpers, so they replicate the guardrail stack manually: `output::dry_run_guard` + a local `guard_readonly` (checks `cli.readonly`) BEFORE `McpClient::connect`; `delete-rule` is `destructive:true` in `commands.json`. `call_reflex_tool` HTTPS+trusted-host-validates the URL (`validate_trusted_url`) before sending the Fabric bearer token, confirms the tool via `list_tools`, calls it, parses the JSON text content, and surfaces `isError` as a fabio `API_ERROR`. Pure `build_mcp_url` is unit-tested; the create→mcp-url→list-rules loop, the two dry-run guards, and the fully-offline `--readonly` block are e2e-tested. Live-verified: `list-rules` returns `{"rules":[]}` for a fresh reflex, and a bogus `rule-activations` surfaces the server's `FailedToResolveEntity` error. Rule CREATION has THREE paths: **`reflex create-rule`** drives the MCP `create_rule` tool deterministically (the RELIABLE, richer path — adds a rule to an EXISTING reflex, supports the full detection catalog + KQL/Ontology sources) with either a full `--rule <json|@file|stdin>` `createRuleParams` passthrough (fabio injects `artifactId`/`workspaceId`) or typed convenience flags for the common single-condition KQL+email/Teams case (`--name --kql --column --condition --value --recipients` + a source); **`reflex create-trigger`** (REST) creates a NEW reflex item + a basic AttributeTrigger rule (simpler but fragile — its KQL-source definition push often fails, `created_but_definition_failed`); and the MCP server via `mcp-url` for NL authoring. `create_rule` is NOT NL-only — it takes a strict, live-verified typed schema: `{name, description, source(oneOf KQL/Ontology), model{stream,detection{condition,occurrence},action}}`, where every function argument is `{name, isColumnReference, value:{type, value}}` (condition args are `Column`/`Value`; email action args are `messageLocale`/`to`/`subject`/`body`/`headline`; Teams uses `actionType:"TeamsMessage"` with `messageLocale`/`recipientEmail`/`headline`/`message`). CHANGE conditions (`increasesAbove`, `decreasesBelow`, …) require `splitColumn`; STATE conditions (`isGreaterThan`, …) do not. The builder (`build_create_rule_params`/`build_source`/`build_action`/`typed_value`) is unit-tested against the verified shapes; the full create→list→stop→start→delete loop and both input modes are live-validated. **`--eventhouse-id` now auto-resolves the cluster (bug fix)**: the Activator MCP server CANNOT resolve a Fabric `KustoDatabase` `itemId` to a cluster — passing `--eventhouse-id <kqlDbId>` (even the correct KQL DATABASE item id) failed with `Getting cluster for kql database failed`; only explicit `--cluster <queryServiceUri> --database <name>` worked. `create_rule` now GETs the KQL database up front, extracts `queryServiceUri` + `displayName`, and sends the reliable `{clusterHostName, databaseName}` shape instead of the `{itemId, itemType:"KustoDatabase"}` shape (a `resolved` override threaded through `build_source`; unit-tested + live-verified `--eventhouse-id` now creates the rule). Server limitations: KQL sources only (ADX cluster or Fabric eventhouse), email/Teams actions only, single-eventstream, no aggregation. See `.agents/API-BEHAVIORS-DISCOVERED.md` "Reflex (Activator) API Behaviors Discovered".
 - **Digital Twin Builder data access (`show-lakehouse` + `query`)**: The public DTB REST API is CRUD+definition only (7 Items ops, all already in fabio); ontology modeling/mapping/contextualization/Explorer are portal-only with no API. But a DTB auto-provisions a linked `<name>dtdm` lakehouse (`{"LakehouseId":"<uuid>"}` in `definition.json`, present even on a fresh item) whose SQL analytics endpoint exposes the twin's data — the `dom` schema (domain views, recommended) and `dbo` schema (base-layer tables). fabio surfaces this: **`digital-twin-builder show-lakehouse --id <DTB>`** resolves the `LakehouseId` (via getDefinition → pure `lakehouse_id_from_definition`) and returns the dtdm lakehouse + SQL endpoint; **`digital-twin-builder query --id <DTB> --sql "…"`** resolves the lakehouse and runs T-SQL via the shared TDS path (`tds_utils::resolve_lakehouse_sql`/`execute_and_render_sql`), so an agent can query the twin without manually decoding the definition. Querying needs a SQL-scoped token (the `az`/credential chain), not a Fabric-scoped static `FABIO_ACCESS_TOKEN`. Deleting a DTB ORPHANS its `dtdm` lakehouse, so **`delete --delete-lakehouse`** resolves + cascades it (default delete emits an orphan `note`). Flow execution stays portal-gated (no reachable jobs job-type). `context schema DigitalTwinBuilder`/`DigitalTwinBuilderFlow` document the definition shapes; the `fabio-ontology` skill carries the data-access gotchas. Pure `lakehouse_id_from_definition` is unit-tested; the create→show-lakehouse→query→delete-cascade loop is live-validated. See `.agents/API-BEHAVIORS-DISCOVERED.md` "Digital Twin Builder API Behaviors Discovered".
 - **Data-agent NL2SQL/NL2KQL/NL2DAX all live-validated + a KQL `select-tables` bug fixed**: A Fabric data agent's query language is chosen by the data-source `fabricItemType` and the agent generates the query itself (`analyze.database.nl2code`): NL2SQL (`Lakehouse`/`Warehouse`/`SQLDatabase`/`MirroredDatabase`), NL2KQL (`KQLDatabase`), NL2DAX (`SemanticModel`), NL2GQL (`GraphModel`/`Ontology` — graph node-types auto-select). NL2SQL was already covered (Lakehouse lifecycle); added live e2e for NL2DAX (`dataagent_semantic_model_nl2dax_lifecycle`, gated on `FABIO_TEST_SEMANTIC_MODEL`), NL2KQL (`dataagent_kql_database_nl2kql_lifecycle`, self-seeding an eventhouse + KQL DB + table), and NL2GQL generation (`dataagent_graph_model_nl2gql_generation_lifecycle`, gated on `FABIO_TEST_LOADED_GRAPH_ID`). NL2SQL/DAX/KQL verified real query generation AND execution (e.g. `EVALUATE SUMMARIZECOLUMNS(...)` / `Sales | summarize ... by Region`) returning grounded results; NL2GQL verified real GQL GENERATION (`MATCH (n:\`DimStore\`) RETURN n.\`StoreName\``) — but the data-agent's server-side GQL EXECUTION currently fails ("Unable to process the request") even though the identical query runs fine via `graph-model execute-query`, a server-side gap (NOT Fabric IQ, NOT fabio). Warehouse/SQLDatabase/MirroredDatabase use the `FabricItem` `add-datasource` body; a **Lakehouse is the exception** — it MUST use `{type:"LakehouseTables", lakehouseReference:{...}}`, NOT `FabricItem` (a lakehouse item is not itself the SQL DB), else schema discovery fails `BadRequest: Failed to fetch schema` (`build_add_datasource_body`, unit-tested for all types). All four NL2SQL sources share one fixture-gated e2e (`run_nl2sql_source_lifecycle` helper + per-type wrappers `dataagent_{lakehouse,warehouse,sql_database,mirrored_database}_nl2sql_lifecycle`, gated on `FABIO_TEST_LOADED_{LAKEHOUSE,WAREHOUSE,SQLDATABASE,MIRRORED_DATABASE}`). Lakehouse + Warehouse + SQLDatabase are live-validated end-to-end (add-datasource → select-tables --all-tables → publish → grounded query); MirroredDatabase's test requires a **connection-based** mirror source (Snowflake/CosmosDB/Azure SQL) and still skips. **An OPEN (push/GenericMirror) mirror does NOT satisfy it (live-proven Aug 2026)**: even though `create --open-mirroring` now lets us stand up a populated MirroredDatabase via pure fabio (fully T-SQL-queryable — `warehouse query --id <mirroredDb>` returns rows and `INFORMATION_SCHEMA.COLUMNS` lists the schema to the caller), the data-agent NL2SQL `add-datasource` fails `Failed to fetch schema for the data source` DETERMINISTICALLY for an open mirror (mirror `status:Running`, SQL endpoint `provisioningStatus:Success`, az Fabric token) — isolation-proven mirror-specific because a **Warehouse** datasource added to the SAME agent/session/token SUCCEEDS. This is a Fabric data-agent SERVER limitation with open mirrors (fabio sends the correct byte-identical `FabricItem` body), NOT a fabio bug. **CRITICAL cross-cutting gotcha**: `add-datasource` does NOT auto-select a source's TABLES (only columns; graph node-types are the exception — auto-selected), so you MUST `select-tables` + re-`publish` or the agent HALLUCINATES instead of grounding. **Two select-tables bugs fixed**: (1) it only recognized `Schema`/`Schemas` containers, but KQL databases nest tables under `Tables`/`Functions`/`Shortcuts`/`MaterializedViews` (`is_grouping_container` now recognizes them); (2) it drilled only ONE level, but Lakehouse/Warehouse tables sit THREE containers deep (`Schemas → Schema → Tables → Table`) — `select_tables` now walks the tree breadth-first through every level (both unit-tested in `dataagent/datasources.rs`). **NL2SQL auth gap (NOT a fabio code bug)**: the data-agent server fetches a SQL source's schema on-behalf-of the caller's token; fabio's OWN device-code login token (`fabio_cache`) is rejected for the SQL-endpoint OBO (`Failed to fetch schema`), while an Azure-CLI Fabric token works — so NL2SQL add-datasource needs `FABIO_ACCESS_TOKEN=$(az account get-access-token --resource https://api.fabric.microsoft.com --query accessToken -o tsv)` (non-SQL KQL/DAX/GQL sources are unaffected). See API-BEHAVIORS "Data-agent SQL-source schema fetch is on-behalf-of the caller". The uniform query convention (`--sql`/`--kql`/`--dax`) is about direct query commands; the data agent, by contrast, is NL-in and picks the language from the source.
 - User's tenant: set locally via secure environment configuration (redacted)- Active capacity: set locally via secure environment configuration (redacted)
- Inactive capacity: set locally via secure environment configuration (redacted)
- Source workspace/lakehouse: set locally via secure environment configuration (redacted)
- Destination workspace/lakehouse: set locally via secure environment configuration (redacted)
- Notebook ID: set locally via secure environment configuration (redacted)
- Fabric REST base URL: `https://api.fabric.microsoft.com/v1`
- OneLake DFS base URL: `https://onelake.dfs.fabric.microsoft.com`
- OneLake Blob base URL: `https://onelake.blob.fabric.microsoft.com`
- Fabric scope: `https://api.fabric.microsoft.com/.default`
- Storage scope: `https://storage.azure.com/.default`
- Spark rate limit on small capacity: LRO reports 430 `TooManyRequestsForCapacity` (non-standard code)
- Test env vars: `FABIO_TEST_SOURCE_WORKSPACE`, `FABIO_TEST_SOURCE_LAKEHOUSE`, `FABIO_TEST_DEST_WORKSPACE`, `FABIO_TEST_DEST_LAKEHOUSE`, `FABIO_TEST_NOTEBOOK_ID`, `FABIO_TEST_CAPACITY_ID`. Optional (tests skip when unset): `FABIO_TEST_SEMANTIC_MODEL` (a semantic model WITH data, for the data-agent NL2DAX validation), `FABIO_TEST_LOADED_GRAPH_ID` (a portal-loaded graph model, for GQL query validation), `FABIO_TEST_LOADED_LAKEHOUSE`/`FABIO_TEST_LOADED_WAREHOUSE`/`FABIO_TEST_LOADED_SQLDATABASE`/`FABIO_TEST_LOADED_MIRRORED_DATABASE` (pre-populated, schema-enabled SQL sources for the data-agent NL2SQL validations — one per source type; each test also needs `az` for an OBO-capable Fabric token), `FABIO_TEST_ML_MODEL_ID` (an ML model with a registered MLflow version, for the registry-version read e2e), `FABIO_TEST_CONNECTION_ID` (an existing connection, for the in-place `connection update` connectivityType e2e), `FABIO_TEST_REPORT` (an existing Power BI report, for the `report copilot-metadata` Power BI MCP e2e), `FABIO_LLM_*` (bring-your-own judge model).
 - Fabric REST API specs (OpenAPI): `https://github.com/Azure/azure-rest-api-specs/` (look under `specification/fabric/`)
 - Power BI Desktop project (PBIP) format docs (authoritative, coding-agent-facing): `https://learn.microsoft.com/en-us/power-bi/developer/projects/projects-overview` (+ `projects-report` and `projects-dataset`). Defines the plain-text folder layout Power BI Desktop / Fabric Git Integration produce and that coding agents can generate/edit: a PBIP root has `<name>.Report/`, `<name>.SemanticModel/`, and a `<name>.pbip` pointer file. **Report** (`.Report/`): required `definition.pbir` (`$schema`+`version`+`datasetReference` with `byPath` XOR `byConnection`) plus EITHER `report.json` (PBIR-Legacy, version 1.0) OR a `definition/` folder (PBIR enhanced, version 4.0+: `definition/report.json`, `definition/version.json`, `definition/pages/pages.json`, `definition/pages/<page>/page.json`, `definition/pages/<page>/visuals/<visual>/visual.json`, optional `bookmarks/`, `reportExtensions.json` for report-level measures). Every PBIR file carries its own `$schema` (schemas under `microsoft/json-schemas/fabric/item/report/definition/**`). **Semantic model** (`.SemanticModel/`): required `definition.pbism` plus EITHER `model.bim` (TMSL, version 1.0) OR a `definition/` folder (TMDL, version 4.0+). Deploy into Fabric requires `byConnection` (a report's `byPath` must be rewired to the deployed model id); `.pbi/` files (localSettings/cache.abf) are git-ignored user state. PBIR is the format DESIGNED for programmatic generation by agents (per-file JSON with schemas); when PBIR reaches GA it becomes the only report format. fabio uses this to power `report validate` (offline PBIR/PBIR-Legacy structural + `$schema` checks) and `report create --definition <folder>` (create a full PBIR report from a generated folder).
 - Analysis Services references (authoritative — the specs Fabric semantic models inherit): `https://learn.microsoft.com/en-us/analysis-services/analysis-services-references`. Fabric/Power BI Premium tabular models are Analysis Services tabular models, so they share: **TMSL** (Tabular Model Scripting Language — the JSON `model.bim` object/command syntax, compat level 1200+; fabio's `semantic-model create --file model.bim`), **TMDL** (Tabular Model Definition Language — the newer per-object folder format; `definition/` folder, `--file *.tmdl`), **TOM/AMO** (.NET client libraries — NOT usable from fabio's Rust/REST surface), **XMLA** (the SOAP protocol under all AS clients; Fabric exposes XMLA endpoints but fabio uses the Power BI `executeQueries` REST API instead), **DAX** (`semantic-model query --dax`), **MDX** (multidimensional — not Fabric tabular), **Power Query M** (`dataflow execute-query --mashup`), and **Schema Rowsets** (the `TMSCHEMA_*`/`DISCOVER_*` DMVs — exposed as DAX `INFO.VIEW.*` functions and surfaced by `semantic-model list-tables/list-columns/list-measures/list-relationships`). What is REST-accessible to fabio: DAX (incl. `INFO.VIEW.*` introspection) and enhanced refresh via the Power BI API; TMSL command execution and TOM programmatic editing require an XMLA/AS client (out of scope for a REST CLI). See `.agents/API-BEHAVIORS-DISCOVERED.md` "Analysis Services specs → fabio surface".
 - Fabric item-definition JSON schemas (authoritative): `https://github.com/microsoft/json-schemas/tree/main/fabric` — Microsoft's published JSON Schemas for Fabric item definitions and the git-integration `.platform` file. Structure: `fabric/item/<type>/definition/**/<version>/schema.json` (per-item, versioned, e.g. `report/definitionProperties/{1.0.0,2.0.0}`, `semanticModel/definitionProperties/1.0.0`, `map/definition/2.1.0`, `ontology/{entityType,relationshipType,dataBinding}/1.0.0`, `variableLibrary/definition/{variables,settings,valueSet}/1.0.0`, `operationsAgents/definition/1.0.0`), `fabric/gitIntegration/platformProperties/{2.0.0,2.1.0}/schema.json` (the `.platform` file), and `fabric/common/`. When fabio generates or emits a `$schema`-bearing definition part, it MUST reference the LATEST matching published version and conform to that schema's `required`/`properties`/`enum`. Served publicly at `https://developer.microsoft.com/json-schemas/fabric/...` (the URL fabio puts in `$schema`). Item types WITHOUT a published schema here (Notebook, DataPipeline, Eventstream, Reflex, KQL queryset/dashboard, SparkJobDefinition, Dataflow, MirroredDatabase, etc.) have no conformance obligation.
 - **JSON-schema conformance (definition `$schema` fields)**: Every definition part fabio synthesizes that MS marks `$schema`-required MUST include it. Verified/fixed: report `definition.pbir` (`--dataset` binding) emits `report/definitionProperties/2.0.0` with the SIMPLE `byConnection` shape `{connectionString:"semanticmodelid=<id>"}` — **the old 6-field XMLA shape (`connectionString:null`, `pbiServiceModelId`, `pbiModelVirtualServerName`, `pbiModelDatabaseName`, `name`, `connectionType`) is REJECTED by the current 2.0.0 schema** (`Workload_FailedToParseFile: … does not allow additional properties` / `Expected String but got Null`), which completely broke `report create --dataset` and the `report scaffold`→`create` flow (bug fixed Aug 2026: both `build_dataset_pbir` and `rebind_pbir_part` now emit the simple `semanticmodelid=` byConnection, live-verified a scaffolded card+columnChart report creates and its pages/visuals round-trip); semantic-model `definition.pbism` emits `semanticModel/definitionProperties/1.0.0` + `version`; ontology `entityType`/`relationshipType` and operations-agent already emit `1.0.0`. The `.platform` file is emitted at `platformProperties/2.1.0` and now round-trips `metadata.sensitivityLabelId` (2.1.0's added field): `parse_platform_file` reads it and `deploy apply` applies it on create as a fallback when no `governance.metadata.json` sidecar label is present (closes the interop gap where Git-integration/fabric-cicd repos carry the label in `.platform`, which fabio previously dropped). Pure builders `build_dataset_pbir` (report.rs), `build_pbism` (semantic_model/crud.rs), `build_platform_json` (deploy/platform.rs) are unit-tested against the MS `required`/version constraints; all three fixes are live-validated (report + semantic-model create round-trip the `$schema`; `deploy export` emits 2.1.0 `.platform` and `deploy plan` re-parses it cleanly). See `.agents/API-BEHAVIORS-DISCOVERED.md` "Fabric JSON Schema Conformance".
 - **Power BI Project (PBIP/PBIR) report authoring for coding agents**: fabio treats the documented PBIP/PBIR plain-text format (`https://learn.microsoft.com/power-bi/developer/projects/projects-overview`) as the agent-native way to author Power BI reports. Two commands close the biggest gaps: **`report validate --source <path>`** (OFFLINE structural + `$schema` validation of a `.Report` folder, a `definition.pbir`, or a PBIP root — required PBIR files, JSON validity, byPath-vs-byConnection, version/format compatibility; machine-readable `code`s; non-zero exit on invalid), and **`report create --definition <folder>`** (creates a FULL multi-page PBIR report from a generated folder — gathers all files recursively, validates first, optional `--dataset` rebinds `definition.pbir` to a concrete model by connection). PBIR logic lives in `src/commands/report_pbir.rs` (sibling module of `report.rs` via `#[path]`, since report.rs is near the 1500-line limit); pure helpers `validate_report_folder`/`validate`/`gather_report_parts`/`rebind_pbir_part` are unit-tested and the full export→validate→create-from-folder→render loop is live-validated. **`deploy` .platform synthesis (raw Desktop PBIP)**: `deploy plan/apply/validate` now discover a raw Power BI Desktop PBIP folder that has NO Git-integration `.platform` sidecar — `synthesize_platform_metadata` (in `deploy/platform.rs`) infers the item from the folder-name suffix (`<name>.Report` → Report + required `definition.pbir`; `<name>.SemanticModel` → SemanticModel + required `definition.pbism`), but ONLY when the suffix maps to one of those two PBIP types AND the type's entry-point definition file exists (so an arbitrary folder is never misclassified — it recurses as a plain folder instead). The synthesized item has `logical_id: None`, so rename tracking is unavailable (the plan warns "no logicalId"); items match deployed items by `(type, name)`. Report→model binding still works because it goes through the existing byConnection name-resolver (`resolve_report_byconnection_model_id`): the model deploys first and is registered in the name→id map, and the report's `initial catalog=<model_name>` (v2 PBIR) rebinds to the newly created model's id. Live-validated: export SalesReport + sales_semantic_model → strip every `.platform` → `deploy plan` discovers/types both → `deploy apply` to a fresh workspace creates both AND the report's `semanticmodelid` rebinds to the newly created model. `.pbi/` Desktop user state is excluded from parts (existing `read_parts_recursive` skip). Roadmap: PBIR scaffolding from a compact page/visual spec. **Full JSON-Schema (per-property) conformance validation was investigated and closed as "won't implement"**: real Fabric reports declare body-schema `$schema` versions that are NOT published in `microsoft/json-schemas` (e.g. a live-exported `visual.json` declares `visualContainer/2.11.0` while upstream maxes at `2.9.0` — a 404), so offline body conformance can never match a real file, and only the stable `definitionProperties`/`semanticModel` schemas (which `definition.pbir`/`definition.pbism` declare and which real files DO conform to) could be validated — adding only strictness over the existing structural checks, not worth promoting `jsonschema` to a runtime dependency. See `.agents/API-BEHAVIORS-DISCOVERED.md` "PBIR body-schema conformance is INFEASIBLE offline". (Folder-based `semantic-model` TMDL ingestion outside deploy is now DONE — `semantic-model create --definition <folder>`.) See `.agents/API-BEHAVIORS-DISCOVERED.md` "Power BI Project (PBIP) / PBIR Report Support".
   - **PBIR report page/visual authoring + scaffold (`report list-pages`/`list-visuals`/`add-page`/`delete-page`/`rename-page`/`set-active-page`/`add-visual`/`delete-visual`/`scaffold`, definition read-modify-write)**: Reports have no XMLA/TOM surface, so fabio authors them like semantic models — a definition read-modify-write over the item API (`getDefinition` → edit the PBIR parts → `updateDefinition`), plus a from-scratch `scaffold`. Ground-truthed against a real 2-page portal PBIR report. Module: `src/commands/report_def.rs` (wired into `report.rs` via `#[path]`, alias `authoring`). Exact live shapes: `version.json` `version:"2.0.0"`; `pages.json` `{pageOrder[],activePageName}` (pagesMetadata/1.1.0); `page.json` (page/2.1.0) `{name,displayName,displayOption:"FitToPage",height:720,width:1280}`; `visual.json` (visualContainer/2.11.0) `{name,position{x,y,z,h,w},visual{visualType,query?}}` where a binding is `visual.query.queryState.<role>.projections[].{field,queryRef}` (field = Column `Table.Col` / Aggregation `Sum(Table.Col)` Function 0..5 / Measure `Table.Name`). `add-visual` builds a conformant `visual.json` from a compact spec (`--type/--title/--text/--category/--measure`; `parse_field_spec` auto-Sums a bare column in a measure role) — **LIVE-PROVEN to render** (a scaffolded report exports to a valid PDF). `scaffold --name --spec <json> --dataset <id> [--out]` generates a COMPLETE report from `{"pages":[{"displayName","visuals":[…]}]}`; the from-scratch `report.json` MUST carry `layoutOptimization:"None"` + a resolvable `themeCollection.baseTheme` (`type:"SharedResources"`) with a matching `resourcePackages` entry AND a bundled `StaticResources/SharedResources/BaseThemes/*.json` theme file, else the API rejects it; the byConnection `semanticmodelid=` must reference a REAL model (placeholder → `PowerBIEntityNotFound`). PBIR-Legacy (single `report.json`) is rejected for page/visual authoring (convert-first hint). All mutations overwrite the definition → `mutates:true`/`destructive:true`, dry-run guarded; unknown → `NOT_FOUND`, duplicate → `CONFLICT`, last-page delete → `INVALID_INPUT`. `report validate` was also deepened with offline cross-reference/naming/size checks (ACTIVE_PAGE_NOT_FOUND, DANGLING_PAGE_ORDER, INVALID_OBJECT_NAME, DUPLICATE_*_NAME, TOO_MANY_PAGES/VISUALS, BYCONNECTION_MISSING_MODEL_ID, …). Pure helpers unit-tested (18 tests); page/visual/scaffold lifecycles live-validated (`report_page_authoring_lifecycle`, `report_visual_authoring_lifecycle`, `report_scaffold_lifecycle`) with PDF-export render proofs. See `.agents/API-BEHAVIORS-DISCOVERED.md` "Power BI Project (PBIP) / PBIR Report Support".
  - **Semantic-model generation from a data source (`semantic-model generate`, Direct Lake, portal-EXACT TMDL)**: The Fabric portal's "New semantic model" on a lakehouse/warehouse/SQL analytics endpoint (pick tables → Direct Lake model) has NO public REST API, so `fabio semantic-model generate --workspace <ws> (--lakehouse <id> | --warehouse <id>) --name <name> [--tables a,b] [--schema dbo] [--no-refresh]` reproduces it CLIENT-SIDE (same approach as `ontology generate`) and emits **byte-shape-identical TMDL** to the portal: resolve the source's SQL analytics endpoint `(server, database, sqlEndpointId)` → read `INFORMATION_SCHEMA.COLUMNS` for BASE TABLEs over TDS → map each SQL type to a Power BI `dataType`, **dropping unmappable columns** (varbinary/geography/geometry/… — matching Fabric's own sync) → synthesize the portal's exact TMDL folder (`definition.pbism` v4.2; `model.tmdl` with `defaultMode: directLake`/`culture: en-US`/`powerBI_V3`/`ref table` lines; `database.tmdl` `compatibilityLevel: 1604`; per-table `tables/<name>.tmdl` with lean `dataType`+`sourceColumn` columns and a `directLake` entity partition; `expressions.tmdl` `DatabaseQuery = Sql.Database(server, <sqlEndpointId>)`) → create → frame with a `Full` refresh. **Verified byte-for-byte against a real portal Direct Lake model over the same lakehouse** via `scripts/compare-semantic-models.py` (zero column/type/partition/setting gaps). **Key portal conventions discovered**: (1) the `Sql.Database` catalog is the **SQL analytics endpoint item id** (a GUID: `sqlEndpointProperties.id` for a lakehouse, the warehouse id for a warehouse), NOT the display name — rename-stable; (2) columns carry only `dataType`+`sourceColumn` (NO `summarizeBy`); (3) `compatibilityLevel` lives in `database.tmdl`, not `model.tmdl`. Relationships/measures/keys are NOT inferred (the plain portal pick-tables flow creates none either; add via `update-definition`). Reads the schema over TDS → needs a **SQL-scoped token from the ambient credential chain** (az/device-code), NOT a Fabric-only static `FABIO_ACCESS_TOKEN`. Also relevant: Fabric **sunset auto-created default semantic models** (2025-09-05) and decoupled existing ones (2025-11-30). Pure helpers (`map_sql_type_to_powerbi`, `plan_tables`, `build_tmdl_parts`/`tmdl_*`, `build_schema_query`) in `src/commands/semantic_model/generate.rs` are unit-tested against the captured portal shape; the generate→frame→DAX loop is live-validated (`semantic_model_generate_direct_lake_lifecycle`). `scripts/compare-semantic-models.py` is a reusable parity checker (portal vs fabio definition diff). See `.agents/API-BEHAVIORS-DISCOVERED.md` "Semantic Model API Behaviors Discovered".
 - **Semantic-model perspectives (`add-perspective`/`delete-perspective`/`add-perspective-member`/`remove-perspective-member`/`list-perspectives`, definition read-modify-write)**: Perspectives (filtered model views) live in `definition/perspectives/<name>.tmdl`, `ref`-ed from `model.tmdl` as `ref perspective <name>`. The file is a nested tree (`perspective` → `perspectiveTable` → `perspectiveColumn`/`perspectiveMeasure`/`perspectiveHierarchy`); fabio parses/edits/re-renders it (like translations). `add-perspective-member --perspective --table [--column|--measure|--hierarchy]` adds a whole table or one member; `remove-perspective-member` removes a member or the whole table. `list-perspectives` is read-only (`{name, tableCount}`). In `model.bim` it's `model.perspectives[].tables[].{columns,measures,hierarchies}`. Mutations overwrite the definition → `mutates:true`/`destructive:true`, dry-run guarded; duplicate → `CONFLICT`, missing → `NOT_FOUND`, >1 member flag → `INVALID_INPUT`. Pure helpers + `model.bim` variants unit-tested; the add → add-member → list → verify → remove → delete loop is live-validated (`semantic_model_perspective_lifecycle`). See `.agents/API-BEHAVIORS-DISCOVERED.md` "Semantic Model API Behaviors Discovered".
 - **Semantic-model DAX user-defined functions (`add-function`/`update-function`/`delete-function`/`list-functions`, definition read-modify-write)**: DAX UDFs (preview) live in `definition/functions.tmdl` as top-level `function <name> = <DAX>` blocks (the new `(x: INT64) => RETURN x + 1` lambda syntax) — like `expressions.tmdl`, NOT `ref`-ed in `model.tmdl`, created on first add / removed when empty. **UDFs require model `compatibilityLevel` ≥ 1702** (default models are 1604), so `add-function` bumps `database.tmdl`'s level to 1702 when lower (`ensure_compat_level`; live-verified `updateDefinition` accepts it). `list-functions` is read-only. In `model.bim` it's `model.functions[].{name,expression}`. `decl_name` learned `function`. Mutations overwrite the definition → `mutates:true`/`destructive:true`, dry-run guarded. Pure helpers + `model.bim` variants unit-tested; the create(1702) → add → list → verify → delete loop is live-validated (`semantic_model_function_lifecycle`). See `.agents/API-BEHAVIORS-DISCOVERED.md` "Semantic Model API Behaviors Discovered".
 - **Semantic-model named expressions / Power Query parameters (`add-expression`/`update-expression`/`delete-expression`/`list-expressions`, definition read-modify-write)**: Named expressions (shared M queries + PQ parameters) live in `definition/expressions.tmdl` as top-level `expression <name> = <M>` blocks — like `relationships.tmdl`, NOT `ref`-ed in `model.tmdl` (auto-discovered), created on first add / removed when empty. A PARAMETER is a named expression whose M is `"<val>" meta [IsParameterQuery=true, Type="…", …]`; `add-`/`update-expression` accept `--expression <raw M>` OR `--parameter-value <val> [--parameter-type Text|Number|Logical|DateTime]` (built by `build_parameter_m`). `list-expressions` (read-only) flags `isParameter` by the `IsParameterQuery=true` marker. In `model.bim` it's `model.expressions[].{name,kind:"m",expression}`. `decl_name` learned `expression`. Mutations overwrite the definition → `mutates:true`/`destructive:true`, dry-run guarded. DISTINCT from `update-parameters` (a Power BI API call that sets a deployed model's datasource parameter VALUES at runtime) — `add-expression` authors the parameter DEFINITION. Pure helpers + `model.bim` variants unit-tested; the add(parameter) → list → update → verify → delete loop is live-validated (`semantic_model_expression_lifecycle`). See `.agents/API-BEHAVIORS-DISCOVERED.md` "Semantic Model API Behaviors Discovered".
 - **Semantic-model calculation groups (`add-calculation-group`/`delete-calculation-group`/`add-calculation-item`/`update-calculation-item`/`delete-calculation-item`/`list-calculation-groups`, definition read-modify-write)**: A calc group is a special table — a `calculationGroup` block (indent 1, unnamed; the group name IS the table name) of `calculationItem <name> = <DAX>` (indent 2), a string column, and a `partition <name> = calculationGroup`. THREE live-ground-truthed prerequisites: the table needs a `calculationGroup`-type partition, the model needs `discourageImplicitMeasures` (inserted by `ensure_model_flag`), and the table is `ref`-ed. **The calc-group partition MUST be `mode: import` (bug fix)**: a `generate`-created (or any) **Direct Lake** model has `defaultMode: directLake`, which the calc-group table inherits — and Direct Lake validation then rejects it with `Dataset_Import_FailedToImportDataset … Direct Lake Table '<name>' must have an exactly one partition of source entity` (a `= calculationGroup` partition is NOT a source entity). `render_calc_group_table` now emits `partition <name> = calculationGroup` + `mode: import`, overriding the model default so the metadata-only calc-group table is accepted on Direct Lake AND import models (import is the default there, so the explicit mode is a harmless no-op). Before the fix, `add-calculation-group` was completely broken on every Direct Lake model. `add-calculation-group --name [--column-name]` synthesizes all three; `add-calculation-item --group --name --expression [--ordinal]` inserts inside the `calculationGroup` block (via `calc_group_span`, items are grandchildren at indent 2); `update-calculation-item` replaces an item's DAX in place. In `model.bim` it's `table.calculationGroup.calculationItems[]` + `model.discourageImplicitMeasures`. `decl_name` in `tmdl.rs` learned `calculationItem` (inline `= <DAX>`). Mutations overwrite the definition → `mutates:true`/`destructive:true`, dry-run guarded; duplicate → `CONFLICT`, unknown → `NOT_FOUND`. Pure helpers + `model.bim` variants unit-tested; the add-group → add-item×2 → list → verify → delete loop is live-validated (`semantic_model_calculation_group_lifecycle`). See `.agents/API-BEHAVIORS-DISCOVERED.md` "Semantic Model API Behaviors Discovered".
 - **Semantic-model partitions (`add-partition`/`update-partition`/`delete-partition`/`list-partitions`, definition read-modify-write)**: Partitions define a table's data source — `partition <name> = m` (`source = <M>`) / `= calculated` (`mode: import` + `source = <DAX>`) inside `definition/tables/<T>.tmdl`; a table can carry several (incremental-refresh ranges). `add-partition --table --name (--m|--dax)` inserts via `insert_table_child_lines`; `update-partition` replaces the `source` in place (preserving other props); `delete-partition` refuses to remove the LAST partition (`INVALID_INPUT` — a table needs ≥1); `list-partitions [--table]` is read-only (`{table, name, mode}`). Single-line source inline, multi-line source as an indented block. In `model.bim` it's `table.partitions[].source.{type,expression}`. Mutations overwrite the definition → `mutates:true`/`destructive:true`, dry-run guarded. Pure helpers + `model.bim` variants unit-tested; the add(2nd) → list → update → verify → delete → last-guard loop is live-validated (`semantic_model_partition_lifecycle`). See `.agents/API-BEHAVIORS-DISCOVERED.md` "Semantic Model API Behaviors Discovered".
 - **Semantic-model hierarchies (`add-hierarchy`/`delete-hierarchy`/`list-hierarchies`, definition read-modify-write)**: User drill-down hierarchies live inside a table's `definition/tables/<T>.tmdl` as a `hierarchy <name>` child block with ordered `level <name>`/`column: <col>` entries. `add-hierarchy --table --name --level ...` (repeatable; each `--level` is `Column`, `Name=Column`, or `Name:Column`) inserts via the shared `insert_table_child_lines`; `delete-hierarchy` uses the shared `child_span`; `list-hierarchies [--table]` is read-only (`{table, name, levelCount, levels}`). In `model.bim` it's `table.hierarchies[].levels[]` with `ordinal`. Mutations overwrite the definition → `mutates:true`/`destructive:true`, dry-run guarded; duplicate → `CONFLICT`, unknown → `NOT_FOUND`. Adding these pushed the `semantic-model` dispatch over clippy's `large_stack_frames` limit, so the granular-authoring arms were split into a second `execute_authoring` fn that `execute` delegates to via a catch-all. Pure helpers + `model.bim` variants unit-tested; the add → list → verify → delete loop is live-validated (`semantic_model_hierarchy_lifecycle`). See `.agents/API-BEHAVIORS-DISCOVERED.md` "Semantic Model API Behaviors Discovered".
 - **Semantic-model translations / cultures (`add-culture`/`delete-culture`/`set-translation`/`list-cultures`, definition read-modify-write)**: Multi-language translations live in `definition/cultures/<culture>.tmdl`, `ref`-ed from `model.tmdl` as `ref cultureInfo <culture>` (the ref kind is `cultureInfo`, and the file root is `cultureInfo <name>` — ground-truthed). The file is a nested tree (`cultureInfo` → `translations` → `model` → `table`/`column`/`measure` → `caption:`); fabio parses it into a small model, edits, and re-renders (robust vs line-editing the deep indentation). `add-culture` creates the file + the `ref cultureInfo` line (model name read from `model.tmdl`); `set-translation --culture --table [--column|--measure] --caption` sets a translated caption; `list-cultures` is read-only (`{culture, translationCount}`). In `model.bim` the same data is `model.cultures[].translations.model.tables[].{translatedCaption,columns,measures}`. Mutations overwrite the definition → `mutates:true`/`destructive:true`, dry-run guarded; duplicate → `CONFLICT`, missing culture → `NOT_FOUND`. Realizes the MCP's "translate my model" scenario; `model_name` added to the shared `tmdl.rs`. Pure helpers unit-tested (incl. round-trip); the add-culture → set-translation → list → verify → delete loop is live-validated (`semantic_model_translation_lifecycle`). See `.agents/API-BEHAVIORS-DISCOVERED.md` "Semantic Model API Behaviors Discovered".
 - **Semantic-model table lifecycle (`add-table`/`delete-table`/`rename-table`/`update-table`, definition read-modify-write, with cascade)**: Tables are `definition/tables/<name>.tmdl` files `ref`-ed from `model.tmdl`. `add-table --name --expression <DAX>` creates a CALCULATED table (partition `= calculated`, `source = <DAX>`) + the `ref table` line — no explicit columns needed (Fabric auto-infers them, `isNameInferred`). `rename-table` rewrites the decl, MOVES the part file, and updates the `ref table` (references NOT rewritten). `update-table --name [--hidden|--data-category|--description]` sets a table's scalar properties (`isHidden`, `dataCategory:` e.g. `Time` for a date table, `///` description). `delete-table` removes the file + ref AND **CASCADES**: strips relationships referencing the table (`remove_relationships_referencing_table`) + role RLS filters on it (`cascade_remove_table_from_roles`) — required because `updateDefinition` rejects a dangling relationship; the removed relationship ids + affected roles are reported (dry-run previewable). All overwrite the definition → `mutates:true`/`destructive:true`, dry-run guarded; duplicate → `CONFLICT`, unknown → `NOT_FOUND`. Pure helpers + `model.bim` variants unit-tested; the add(calculated) → rename → update → verify → delete(+cascade) loop is live-validated (`semantic_model_table_lifecycle` + `semantic_model_update_table_lifecycle`). See `.agents/API-BEHAVIORS-DISCOVERED.md` "Semantic Model API Behaviors Discovered".
 - **Semantic-model column authoring (`add-calculated-column`/`delete-`/`rename-`/`update-column`, definition read-modify-write)**: Columns are `column <name>` blocks in `definition/tables/<T>.tmdl` (data column = `dataType:`/`sourceColumn:`; calculated column = `= <DAX>` on the decl + `dataType:`). `add-calculated-column` inserts via the shared `insert_table_child_lines` (after the table's scalar props, same indentation-safety as measures); `delete-column` uses the shared `child_span`; `rename-column` rewrites the decl only (preserving a calculated column's `= expr`; references NOT rewritten); `update-column` sets `dataType`/`formatString`/`summarizeBy`/`displayFolder`/`description`/`isHidden` in place. All overwrite the definition → `mutates:true`/`destructive:true`, dry-run guarded; duplicate → `CONFLICT`, missing column → typed `NOT_FOUND` (NOT a generic `bail!`, since the table exists). The reusable TMDL helpers (`is_child_object_decl`/`insert_table_child_lines`/`child_span`/`join_preserving_trailing_newline`) were promoted to the shared `tmdl.rs`. Pure editors + `model.bim` variants unit-tested; the add → update → rename → verify → delete loop is live-validated (`semantic_model_column_lifecycle`). See `.agents/API-BEHAVIORS-DISCOVERED.md` "Semantic Model API Behaviors Discovered".
 - **Semantic-model security roles / RLS (`add-role`/`delete-role`/`set-rls`/`delete-rls`/`list-roles`, definition read-modify-write)**: Roles live in `definition/roles/<name>.tmdl` (one file per role) and MUST be `ref`-ed from `model.tmdl` (`ref role <name>`) — unlike relationships (auto-discovered). A role = `modelPermission:` + `tablePermission <Table> = <DAX predicate>` lines (the DAX predicate is the RLS filter). `add-role` creates the file + the ref; `delete-role` removes both; `set-rls`/`delete-rls` add/replace/remove a table's filter; `list-roles` is read-only. DISTINCT from `add-user`/`list-users` (dataset *permissions* for a principal, a Power BI API concept) — RLS roles are a MODEL concept (definition parts). Mutations overwrite the definition → `mutates:true`/`destructive:true`, dry-run guarded; duplicate role → `CONFLICT`, unknown → `NOT_FOUND`. `add_model_ref`/`remove_model_ref` live in the shared `tmdl.rs`. Pure helpers unit-tested; the add-role → set-rls → list → delete-rls → delete-role loop is live-validated (`semantic_model_role_lifecycle`). See `.agents/API-BEHAVIORS-DISCOVERED.md` "Semantic Model API Behaviors Discovered".
 - **Semantic-model measure lifecycle (`delete-`/`rename-`/`move-measure`, definition read-modify-write)**: Completes the measure CRUD. `delete-measure` removes the whole block (leading `///` comments + decl + indent≥2 body via `measure_span`); `rename-measure --new-name` rewrites only the declaration (DAX references are NOT rewritten; collision → `CONFLICT`); `move-measure --to-table` extracts the block and re-inserts it into the destination table (shared `insert_measure_lines`, canonical measures-first; same-table → `INVALID_INPUT`). All overwrite the definition → `mutates:true`/`destructive:true`, dry-run guarded. Pure helpers + `model.bim` variants unit-tested; the create → rename → move → verify → delete loop is live-validated (`semantic_model_measure_lifecycle`). See `.agents/API-BEHAVIORS-DISCOVERED.md` "Semantic Model API Behaviors Discovered".
 - **Semantic-model relationship authoring (`add-`/`delete-`/`update-relationship`, definition read-modify-write)**: Relationships live in `definition/relationships.tmdl` as top-level `relationship <guid>` blocks (`fromColumn: Table.Column`/`toColumn: Table.Column`, non-default properties only); the file is NOT `ref`-ed in `model.tmdl` (auto-discovered) and is created on the first add / removed when the last relationship is deleted. `add-relationship` mints a v4 GUID; `delete-`/`update-relationship` match by `--relationship-id` OR the full four-column tuple (partial tuple → `INVALID_INPUT` offline). All three overwrite the definition → `mutates:true`/`destructive:true`, dry-run guarded; referenced tables are existence-checked first. The shared definition plumbing lives in `src/commands/semantic_model/tmdl.rs` (fetch/push/upsert/remove parts, `quote_tmdl_name`, `column_ref`, `find_table_file`), reused by all the granular-authoring modules. Pure helpers in `relationships.rs` are unit-tested (8 tests); the add×2 → update → delete loop is live-validated (`semantic_model_relationship_lifecycle`). See `.agents/API-BEHAVIORS-DISCOVERED.md` "Semantic Model API Behaviors Discovered".
 - **Semantic-model granular object authoring (`set-description` / `add-measure` / `update-measure`, definition read-modify-write, no XMLA/TOM)**: Microsoft's `powerbi-modeling-mcp` edits model objects live over XMLA/TOM; fabio is a REST CLI (no XMLA/TOM), so it does the SAME authoring via a definition read-modify-write — `getDefinition` → edit the TMDL `definition/tables/*.tmdl` (or `model.bim`) in place → `updateDefinition` (LRO), the same machinery as `analyze --fix`/`generate`. `set-description --table T [--column C] [--measure M] --description` sets an object's `///` comment (table indent 0, column/measure indent 1, replacing any existing block); `add-measure --table T --name N --expression DAX [--format-string] [--display-folder] [--description]` inserts a measure; `update-measure --measure N [--expression] [--format-string] [--display-folder] [--description]` edits one in place (new expression, properties preserved). All three OVERWRITE the definition (irreversible) → `mutates:true`/`destructive:true`, `--dry-run` guarded; `updateDefinition` validates server-side (a bad TMDL edit is rejected, never silently corrupts). **TMDL-ordering gotcha (live-caught)**: a table child object must follow the table's scalar properties (`lineageTag:`) — a measure inserted between `table X` and its `lineageTag` fails with `Workload_FailedToParseFile … Invalid indentation`, so `add_measure_tmdl` inserts before the first child-object decl (or its `///` comment) at indent 1 (canonical "measures first"). Measure DAX is read from the definition (not `INFO.VIEW.MEASURES().Expression`, which is null over `executeQueries`). Errors: duplicate name → `CONFLICT`, unknown table/measure → `NOT_FOUND`, no `set-description` target → `INVALID_INPUT` (offline). Pure editors in `src/commands/semantic_model/authoring.rs` are unit-tested (11 tests incl. the lineageTag regression); the create → set-description → add-measure → update-measure → verify → delete loop is live-validated (`semantic_model_authoring_lifecycle`). See `.agents/API-BEHAVIORS-DISCOVERED.md` "Semantic Model API Behaviors Discovered".
 - **Semantic-model best-practice analysis (`analyze` + `measure-dependencies`, read-only, over DAX introspection)**: Inspired by Microsoft's "Semantic model best practices for data agent" guidance, two read-only model-quality commands built on the `INFO.VIEW.*`/`executeQueries` surface fabio already uses. **`semantic-model analyze`** is a Best Practice Analyzer / Memory Analyzer: it runs pure rules over `INFO.VIEW.{TABLES,COLUMNS,MEASURES,RELATIONSHIPS}` → `{issueCount, summary, issues:[{rule,severity,object,message,fix}]}`. Rules: missing descriptions, cryptic names (`TR_AMT`/`DIM_GEO_01`), implicit-aggregation (numeric identifier columns with `SummarizeBy != None`), duplicate measures, ambiguous dates, relationship hygiene (inactive/bidirectional/many-to-many), non-star (`flat-schema`/`no-relationships`), calculated columns, and high-cardinality (opt-in `--with-cardinality`, a single batched `DISTINCTCOUNT` probe). `--severity` filters; `--strict` exits non-zero (CI gate). **`analyze --fix` auto-fixes ONLY the safe, mechanical class** — `implicit-aggregation` → set the column's default summarization to `None` via a read-modify-write on the definition (`getDefinition` → edit `summarizeBy` in the TMDL `tables/*.tmdl` or `model.bim` → `updateDefinition`), `--dry-run` guarded (shows `wouldFix`), reporting `{fixApplied, fixed}`. Because it overwrites the definition (irreversible), `analyze` is `mutates:true`/`destructive:true`. Renaming/dedup/schema-restructuring/descriptions are deliberately NOT auto-fixed (they break DAX/relationships or need judgment/content). **`semantic-model measure-dependencies`** lists each measure's dependent measures/columns/tables (the `get_measure_dependencies` equivalent, for scoping AI data schemas). **Key limitation**: `INFO.VIEW.MEASURES().Expression` is null over `executeQueries` and raw `INFO.MEASURES()` is rejected (HTTP 400), so measure DAX is read from the model **definition** (`getDefinition`) — parsing both TMDL `tables/*.tmdl` and `model.bim`. The three "Prep for AI" components (AI data schema, verified answers, AI instructions) are portal/Desktop-only (no public REST API); documented in `context best-practices semantic-model-optimization` + the `semantic-model-ai-readiness` workflow, not implemented. Pure helpers (`is_non_descriptive`, `run_rules`, `parse_measure_refs`, `extract_measures`/`_tmdl`/`_bim`) in `src/commands/semantic_model/analyze.rs` are unit-tested; the create→analyze→measure-dependencies loop is live-validated (`semantic_model_analyze_and_measure_dependencies`). See `.agents/API-BEHAVIORS-DISCOVERED.md` "Semantic Model API Behaviors Discovered".
 - **Semantic-model `evaluate-measure` (semantic-link `evaluate_measure` equivalent)**: `semantic-model evaluate-measure --workspace <ws> --id <model> --measure <name> [--measure <name2>…] [--group-by Table.Column…] [--top <n>]` evaluates one or more measures without the caller hand-writing DAX — the fabio equivalent of SemPy's `fabric.evaluate_measure` (one of the most-used semantic-link functions). It builds the DAX and runs it through the same Power BI `executeQueries` path as `query`: no `--group-by` → `EVALUATE ROW("<m>", [<m>])` (a single-row total); with `--group-by` → `EVALUATE SUMMARIZECOLUMNS('Table'[Column]…, "<m>", [<m>]…)`; `--top <n>` wraps it in `TOPN(<n>, …, [<first measure>], DESC)`. `--group-by` accepts `Table.Column` (→ `'Table'[Column]`, single quotes doubled) or an already-bracketed spec (passed through). Read-only (`mutates:false`, `returns:list`). Pure `build_evaluate_measure_dax`/`qualify_column` in `src/commands/semantic_model/operations.rs` are unit-tested; the query-execution path was refactored into a shared `execute_dax_and_render` reused by `query` + `evaluate-measure`; live-validated end-to-end on a Direct Lake model (grouped EU=130/US=50, total=180, `--top 1`→EU).
 - **Semantic-model schema introspection (Analysis Services "Schema Rowsets" over DAX)**: Fabric semantic models are Analysis Services tabular models, so their metadata is queryable via the DAX `INFO.VIEW.*` functions (the modern, readable form of the `TMSCHEMA_*` schema rowsets/DMVs) through the same Power BI `executeQueries` REST endpoint fabio already uses for DAX. New commands `semantic-model list-tables|list-columns|list-measures|list-relationships` run `EVALUATE INFO.VIEW.{TABLES,COLUMNS,MEASURES,RELATIONSHIPS}()` and return readable model metadata (StorageMode incl. Direct Lake, DataType, SummarizeBy, FormatString, Cardinality, CrossFilteringBehavior, LineageTag, …) WITHOUT fetching/parsing the TMDL/TMSL definition — the agent-native way to understand a model before writing DAX or wiring a report. The raw `INFO.TABLES()`/`INFO.COLUMNS()` variants are rejected by `executeQueries` (HTTP 400) — only `INFO.VIEW.*` works. DAX-bracketed keys (`[Name]`) are stripped for agent-friendly output (`strip_bracket_keys`, unit-tested); the create→introspect→delete loop is live-validated (`semantic_model_schema_introspection_lifecycle`). Enhanced/granular refresh: `semantic-model refresh` gained `--objects` (JSON `[{table,partition?}]`), `--commit-mode` (transactional|partialBatch), `--max-parallelism`, `--retry-count` — the TMSL `refresh` command's granular options mapped onto the Power BI enhanced-refresh API (registers as `ViaEnhancedApi`); pure `build_refresh_body`/`parse_refresh_objects`/`normalize_commit_mode` are unit-tested, live-verified on a Direct Lake table. The enhanced-refresh lifecycle is completed by `refresh-details --refresh-id` (object-level status) and `cancel-refresh --refresh-id` (destructive, cancels an in-progress job). Scheduled (automatic) refresh: `get-refresh-schedule`/`update-refresh-schedule` (Power BI `refreshSchedule`) with typed flags (`--enabled/--days/--times/--local-time-zone-id/--notify-option`); two API constraints are enforced client-side — times must be on the full/half hour (`HH:00`/`HH:30`) and disabling must be sent alone (the API rejects changing other settings while disabling). Dataset gateway binding: `get-bound-gateway-datasources` (read-only) + `bind-to-gateway --gateway-id` (mutation, dry-run-guarded) mirror the `bind-connection`/`unbind-connection` pair for on-premises/VNet gateways; `bind-to-gateway`'s happy path needs a real gateway (a no-op on cloud/Direct Lake models). Deliberately NOT added (probed, not cleanly reachable/valuable here): `Default.DiscoverGateways` (401, needs gateway-admin), `executeQueries` impersonation (needs RLS roles), `queryScaleOut` (Premium, `StorageModeNotSupported` on Direct Lake) — all reachable via `rest call --api powerbi`. See `.agents/API-BEHAVIORS-DISCOVERED.md` "Analysis Services specs → fabio surface".
- **Warehouse execution plan capture**: `warehouse plan` / `sql-database plan` / `sql-endpoint plan` — uses `SET SHOWPLAN_XML ON` via TDS to capture estimated execution plans without executing the query. Returns plan XML in structured JSON (`{"statementCount": N, "plans": [{"statementIndex": i, "planXml": "<ShowPlanXML...>"}]}`). Safe for DDL/DML (not executed). Works on Warehouse, Lakehouse SQL Endpoint, and SQL Database.
- **Warehouse query insights**: `warehouse queries-running|queries-frequent|queries-long-running|queries-history|queries-kill` — TDS queries against `sys.dm_exec_requests` and `queryinsights.*` schema views. `queries-kill` executes `KILL <session_id>` (mutating, guarded by `dry_run_guard`). Note: `sys.dm_exec_requests` on Fabric does NOT have `login_name` column (it's in `sys.dm_exec_sessions`).
- **`queries-history` performance columns + `--label` filter (shared across warehouse/sql-endpoint/lakehouse/sql-database)**: `queries-history` now selects the query `label` and the performance columns `allocated_cpu_time_ms`, `data_scanned_remote_storage_mb`, `data_scanned_memory_mb`, `data_scanned_disk_mb` (in addition to the original command/status/elapsed/row_count), and takes an optional `--label <name>` filter (`WHERE label = N'...'`, single quotes doubled against injection). This lets an agent compare labeled query executions — e.g. the Data Warehouse "data clustering" tutorial tags a query on the regular table `OPTION (LABEL = 'Regular')` vs the `CLUSTER BY` table `OPTION (LABEL = 'Clustered')`, then compares CPU time + data scanned to assess clustering effectiveness. All four groups' `queries-history` route through the single pure `tds_utils::queries_history_sql(top, label)` (unit-tested: column presence, label filter, quote-escaping), replacing four duplicated hard-coded SELECTs. Read-only. NOTE `queryinsights.*` views populate asynchronously (up to ~15 min after the first queries on a fresh warehouse), so a brand-new warehouse briefly returns `Invalid object name 'queryinsights.exec_requests_history'` for BOTH the old and new command — this is initialization lag, not a fabio bug.
- **Warehouse restore-point request-body bugs (fixed)**: Bugs found reproducing the warehouse restore-point flow, verified against `fabric-rest-api-specs` + live. (1) `create-restore-point` AND `update-restore-point` sent `{restorePointLabel: <name>}`, but the API's `Create`/`UpdateRestorePointRequest` is `{displayName, description}` — the label field was silently ignored, so `--name` did nothing (every restore point was named "Restore point"). Both fixed to send `displayName`; added `--description`. The create returns `201`(RestorePoint) OR `202`(empty async body); `create-restore-point` now polls the LRO and, if no `id` came back, re-fetches the list and returns the newest restore point (max epoch id) — previously it printed `{"data":null}` with no id for the agent to reference. (2) `restore-to-point` (`POST /restorePoints/{id}/restore`) takes NO request body (restore is IN-PLACE) — fabio REQUIRED a meaningless `--name` and sent `{restoreToWarehouseName}` the server ignores; removed `--name`, now sends `{}` and reports `{status:"restored"}`. (3) `list-restore-points` rendered nonexistent `restorePointLabel`/`createdDateTime` columns (always empty) → fixed to the real `displayName`/`creationMode`/`description`. e2e dry-run tests assert the corrected bodies. See `.agents/API-BEHAVIORS-DISCOVERED.md` "Warehouse API Behaviors Discovered".
- **Warehouse configurable retention**: `warehouse get-retention` / `warehouse set-retention --days <1-120>` — TDS convenience wrappers for the warehouse time-travel/data-retention window. `set-retention` runs `ALTER DATABASE CURRENT SET TIME_TRAVEL_RETENTION_PERIOD = <N> DAYS` (validated 1–120), `get-retention` reads `time_travel_retention_period_days` from `sys.databases`. This window governs time travel, table clones, restore points, and snapshots. `set-retention` is `destructive: true` (DECREASING it is irreversible — the background GC permanently removes history older than the new window) and dry-run-guarded; it uses the non-rendering `execute_insights_statement` helper (DDL returns no result set) so the output is a single `{id, retentionDays, status}` envelope. Pure `validate_retention_days`/`build_set_retention_sql` in `src/commands/warehouse/retention.rs` are unit-tested; the get→set→restore loop is live-validated (`warehouse_retention_get_set_roundtrip`). Warehouse-only (the SQL analytics endpoint is read-only; SQL Database is a different engine).
- **SQL Pool Insights**: `warehouse pool-insights` / `sql-endpoint pool-insights` / `lakehouse pool-insights` — read-only TDS query against the `queryinsights.sql_pool_insights` system view (SQL pool state changes + sustained pressure events for the built-in SELECT / NON-SELECT pools). Columns: `sql_pool_name`, `timestamp`, `is_optimized_for_reads`, `is_pool_under_pressure`, `max_resource_percentage`, `cache_cooldown_minutes`, `current_workspace_capacity`. Complements `queries-*` with pool-level telemetry (correlate pool pressure with query performance, validate read/write isolation). The SQL is built by the shared pure `tds_utils::pool_insights_sql(top)` (unit-tested); the three groups reuse their existing insights connection resolvers. Live-validated on warehouse + lakehouse SQL endpoint + SQL endpoint.
- **Warehouse statistics management**: `warehouse statistics-list|statistics-show|statistics-create|statistics-update|statistics-delete` — TDS-based CRUD for user-defined statistics. `statistics-list` queries `sys.stats` + `sys.stats_columns` + `sys.tables` (works on both Warehouse and SQL endpoints). `statistics-show` uses `DBCC SHOW_STATISTICS` with auto-lookup of owning table via `sys.stats`. Note: `sys.dm_db_stats_properties` is NOT supported on Lakehouse SQL endpoints — removed from list query.
- **`statistics-update`/`statistics-delete` owning-table resolution (bug fix)**: `warehouse` AND `sql-database` `statistics-update`/`statistics-delete` emitted `UPDATE STATISTICS [<name>]` / `DROP STATISTICS [<name>]`, treating the STATISTIC name as a TABLE name — so both were completely broken (`Table '<name>' does not exist` / DROP syntax error). A statistic is per-table and both commands take only `--name`, so the fix resolves the owning table at runtime via `sys.stats JOIN sys.tables` (as `statistics-show` already did) and runs the DDL through `sp_executesql`. Shared, unit-tested builders `tds_utils::build_update_statistics_sql`/`build_drop_statistics_sql` back all four commands; live-verified on a warehouse (create→update→delete→gone). See `.agents/API-BEHAVIORS-DISCOVERED.md` "Warehouse API Behaviors Discovered".
- **Warehouse module directory structure**: Refactored from single `warehouse.rs` (1357 lines) into `warehouse/` directory module: `mod.rs` (enum + dispatch + shared helpers), `crud.rs`, `query.rs`, `admin.rs`, `restore_points.rs`, `insights.rs`, `statistics.rs`.
- **SQL Database insights and statistics**: Same query monitoring and statistics CRUD as warehouse — `sql-database queries-running|queries-history|queries-kill|statistics-list|statistics-show|statistics-create|statistics-update|statistics-delete`. Uses `resolve_sql_connection()` for different TDS connection resolution (host+port vs connection string).
- **SQL Endpoint insights**: `sql-endpoint queries-running|queries-frequent|queries-long-running|queries-history` — read-only query monitoring (no kill, endpoints are read-only). Same `queryinsights.*` views as warehouse.
- **Lakehouse plan and insights**: `lakehouse plan|queries-running|queries-frequent|queries-long-running|queries-history` — direct discoverability for lakehouse users (previously only accessible via `fabio warehouse <cmd> --id <lakehouse_id>` workaround). Resolves connection from lakehouse `sqlEndpointProperties.connectionString`.
 - **KQL Database query monitoring**: `kql-database queries-running|journal|queries-completed` — uses `.show running queries`, `.show journal`, `.show queries` management commands via Kusto REST mgmt endpoint (`/v1/rest/mgmt`). Reuses existing `kql_utils::execute_kql` infrastructure which auto-routes `.show` commands to mgmt endpoint.
- **T-SQL over a KQL database (`kql-database query` auto-routes leading-`SELECT`)**: Kusto rejects a T-SQL query as invalid KQL unless the request sets `properties.Options.query_language = "Sql"` on `/v1/rest/query` (result comes back in the v1 `{"Tables":[...]}` shape). `kql_utils::execute_kql` now detects a T-SQL query (pure `is_tsql_query`: the leading alphabetic run equals `select`, case-insensitive — KQL has no `SELECT`, so it's unambiguous, and `SelectedRows` is not misdetected) and routes it to `/v1/rest/query` + SQL option + v1 parsing; `.`-prefixed mgmt commands still hit `/v1/rest/mgmt` and other KQL queries `/v2/rest/query` (no regression). Reproduces the RTI tutorial's "Query using T-SQL" step. Unit-tested (`is_tsql_query`) + live e2e (`kql_database_query_tsql`). Applies to every `execute_kql` caller.
 - **Lakehouse table health check**: `lakehouse table-health --table <schema.table>` runs `sys.sp_get_table_health_metrics` over TDS against the lakehouse SQL analytics endpoint (single-row result: `PotentialAnomalyType`/`PotentialAnomalyDescription` + file-size/row-count histograms), letting agents decide whether a table needs `OPTIMIZE`/`VACUUM` before reactive troubleshooting. Read-only. Table name is embedded as an escaped `N'…'` literal (injection-safe); a newly loaded table takes ~30–60s to appear on the SQL endpoint. Handler + pure `build_table_health_sql`/`validate_table_name` in `src/commands/lakehouse/insights.rs` are unit-tested; the create-table→poll→health→delete loop is live-validated (`lakehouse_table_health_lifecycle`).
 - **Environment Spark runtime version + typed Spark properties (Runtime 2.0 / Spark 4.1, native execution engine, remote shuffle, diagnostic emitter)**: `environment update-staging-spark-compute` gained typed `--runtime-version <1.3|2.0>` and repeatable `--spark-property KEY=VALUE` flags (previously a raw JSON conduit only). They do a **read-merge-write** on `staging/sparkcompute` (GET → set `runtimeVersion`, merge into `sparkProperties` → PATCH) so existing properties/fields survive the PATCH's full-replace of the `sparkProperties` map; they conflict with `--file`/`--content` (raw full-body replace). This is the ergonomic path to opt into Runtime 2.0 and to enable the native execution engine (`spark.native.enabled`), remote shuffle manager (`spark.remote.shuffle.enabled`), and Spark diagnostic emitter (`spark.synapse.diagnostic.emitter.*`) without hand-writing the compute JSON. Pure `parse_spark_property`/`apply_spark_compute_overrides` in `src/commands/environment.rs` are unit-tested; the runtime-2.0 + property merge loop is live-validated (`environment_staging_spark_compute_runtime_and_properties_lifecycle`). The **workspace-level default** runtime counterpart is `spark update-settings --runtime-version <ver>` (previously a raw `--settings` JSON conduit only): it read-merge-writes `/workspaces/{ws}/spark/settings` setting `environment.runtimeVersion` (preserving all other settings), conflicts with `--settings`, and implements the "Enable Runtime 2.0 in Workspace settings" flow. Pure `apply_workspace_runtime_version` in `src/commands/spark.rs` is unit-tested; live-validated (`spark_update_settings_runtime_version_lifecycle`).
   - **Spark monitoring diagnostics (`spark get-advice` / `get-resource-usage` / `get-logs`)**: The Fabric Spark monitoring REST APIs expose per-Spark-application diagnostics that fabio only partly surfaced (it listed/got Livy sessions but had no way to read advisor advice, the resource-usage timeline, or driver/executor/livy logs). Added three read-only commands under `spark`, all built on the shared base path `/workspaces/{ws}/{notebooks|sparkJobDefinitions|lakehouses}/{itemId}/livySessions/{livyId}/applications/{appId}[/{attemptId}]/...`: `get-advice` (`/advice` — Spark advisor real-time recommendations), `get-resource-usage` (`/resourceUsage` — core-efficiency/idle-time timeline), and `get-logs` (`/logs?type=driver|executor|livy&meta=<bool>` — `--meta` returns log-file metadata JSON, otherwise raw text via `get_text`). The `--item-type` flag (`notebook`/`spark-job-definition`/`lakehouse`) maps to the URL segment (`spark_item_segment`); `--app-id`/`--attempt-id` come from `spark get-livy-session` (the session response carries `sparkApplicationId` + `attemptNumber`). Livy logs use application id `none` and no attempt id, so `get-logs --type livy` needs no `--app-id`; driver/executor logs require `--app-id` (clean `INVALID_INPUT` otherwise). Pure `spark_item_segment`/`applications_base` are unit-tested; all three live-validated end-to-end (advice `[]` for a trivial run, real resource-usage timeline, driver `stderr` metadata + content, livy-log metadata) against a notebook Spark application. See `.agents/API-BEHAVIORS-DISCOVERED.md` "Spark API Behaviors Discovered".
  - **Environment external-libraries import/export are RAW FILES (bug fix)**: `environment import-staging-libraries` previously POSTed JSON, but `importExternalLibraries` requires `Content-Type: application/octet-stream` with the RAW `environment.yml` bytes — so the command was broken (it even failed at its own JSON parse when given a YAML file). Fixed to read the file/`--content` verbatim and post it via `post_octet_stream`. Symmetrically, `export-staging-libraries`/`export-libraries` previously JSON-parsed the response, but the endpoint returns the RAW `environment.yml` text — fixed to fetch via `get_text` and wrap it as `{"externalLibraries":"<yaml>"}`. Live-validated round-trip (`environment_external_libraries_roundtrip`). The Maven **pom.xml** import (Spark 4.0+, Full mode) uses a separate, not-yet-identified endpoint and remains unsupported; see `.agents/API-BEHAVIORS-DISCOVERED.md` "External-libraries import/export are RAW FILES".
  - **Environment remove-staging-library requires name + version (bug fix)**: `environment remove-staging-library` sent `{libraryToRemove:<name>}`, but `removeExternalLibrary` (`RemoveExternalLibrariesRequest`) REQUIRES both `name` AND `version` → every call failed (`Invalid request. Provide the name and version of external library`). Fixed to send `{name, version}` via a new required `--library-version` flag. Live-verified end-to-end (import `fuzzywuzzy==0.18.0` → remove → `{status:"removed"}`). Offline dry-run e2e. See `.agents/API-BEHAVIORS-DISCOVERED.md`.
- **apply-tags/unapply-tags used wrong field `tagIds` (bug fix)**: `item apply-tags`/`unapply-tags` AND `workspace apply-tags`/`unapply-tags` sent `{"tagIds":[...]}`, but the `ApplyTagsRequest`/`ApplyWorkspaceTagsRequest` field is `tags` (an array of tag ID UUIDs) → every call failed with `InvalidInput`. This was previously mis-recorded as a mysterious "API_ERROR on some tenants" limitation (an e2e test even skipped on it) — it was a plain field-name bug. Fixed all four commands to send `{"tags":[...]}`. Live-validated end-to-end (create tag → apply → unapply on both item and workspace). Offline dry-run e2e assert `tags` (not `tagIds`); the workspace e2e no longer skips. See `.agents/API-BEHAVIORS-DISCOVERED.md`.
- **Semantic-model bind-connection needs a full connectionBinding object (bug fix)**: `semantic-model bind-connection`/`unbind-connection` sent a flat `{connectionId:<id>}` / `{connectionId:null}`, but the `bindConnection` API requires `{connectionBinding:{id?, connectivityType, connectionDetails:{type,path}}}` → every call failed with `InvalidInput: Semantic model bind request cannot be null`. Fixed: `bind-connection` GETs `/connections/{id}` (its response carries `connectivityType`+`connectionDetails`) and builds the full binding; `unbind-connection` gained a required `--connection-id` and sends `connectivityType:"None"` with the resolved `connectionDetails`. Live-verified the shape end-to-end (OLD → `InvalidInput`; NEW → `BindModelHasNoBindableConnections`, a semantic accept). Pure `build_bind_connection_body` is unit-tested. See `.agents/API-BEHAVIORS-DISCOVERED.md` "Semantic Model API Behaviors Discovered".
- **Job-scheduler create/update-schedule config must be nested under `configuration` (bug fix)**: `job-scheduler create-schedule`/`update-schedule` SPREAD the `--config` object at the top level (`{enabled, type, …}`) with a wrong help example (`expression`/`timezone`), but the API requires `{enabled, configuration:{type, startDateTime, endDateTime, localTimeZoneId, …}}` (per-type: Cron→`interval`, Daily→`times`, Weekly→`times`+`weekdays`) → every schedule failed with `InvalidInput`. Fixed to nest via the shared `schedule_configuration_from` (accepts the bare config or unwraps a `{"configuration":{...}}` wrapper); help documents the real fields. Live-verified create (Cron) + update (Daily). Pure helper unit-tested; offline e2e. See `.agents/API-BEHAVIORS-DISCOVERED.md` "Job Scheduler API Behaviors Discovered".
- **Missing required query-param family (6 broken commands, bug fix)**: A full audit of every REQUIRED query param in `fabric-rest-api-specs` (beyond `?beta`) found 6 read commands that omitted a required param and thus always failed: `mirrored-catalog list-scopes`/`list-tables` (needed `?connectionId`), `mirrored-databricks-catalog discover-catalogs`/`discover-schemas`/`discover-tables` (needed `?databricksWorkspaceConnectionId`), and `item get-invitation` (needed `?providerTenantId`). Each gained the corresponding required flag (`--connection-id` / `--provider-tenant-id`, plus optional `--parent`/`--recursive`/`--scope` on the catalog listers). Live-verified all 6 now reach business logic (semantic errors: connection-not-found / PowerBINotAuthorizedException / TenantSwitchDisabled) instead of generic `InvalidInput`/`NOT_FOUND`. Pure `build_scopes_url`/`build_tables_url` unit-tested; offline clap-required-flag e2e guards for all 6. See `.agents/API-BEHAVIORS-DISCOVERED.md` (Mirrored Catalog / Mirrored Databricks Catalog / External data shares).
- **creationPayload optional flags (audit + enhancement)**: A full audit of every `*CreationPayload` in the specs confirmed the REQUIRED ones are already handled (`lakehouse enableSchemas`, `kql-database databaseType`+`parentEventhouseItemId`, `warehouse-snapshot parentWarehouseId`, `digital-twin-builder-flow digitalTwinBuilderItemReference`, `sql-database creationMode`+`--collation`/`--backup-retention-days`) and that `mirrored-*-catalog` create as empty shells (creationPayload optional — no bug). **`snowflake-database` create is NOT an empty shell (gap fixed)**: despite the swagger marking `creationPayload` optional AND shipping a `CreateSnowflakeDatabaseWithoutPayload` example, the LIVE API rejects a `{displayName}` shell create with `InvalidInput: Invalid payload` — a Snowflake (mirrored) database REQUIRES a source connection. fabio had NO way to supply the `creationPayload`, so a Snowflake DB could never be created; added `snowflake-database create --connection-id <id> [--snowflake-database-name <name>]` (builds `creationPayload:{connectionId, snowflakeDatabaseName}`) plus a teaching hint when a shell create is attempted. Live-verified: a real-shaped payload is accepted (a bogus connection id fails with `ConnectionInaccessible`, NOT `Invalid payload` — proving the shape is valid); shell create now surfaces the connection-required hint. Two OPTIONAL payload fields were unexposed and are now flags: `warehouse create --collation` (`creationPayload.collationType`: `Latin1_General_100_BIN2_UTF8` case-sensitive default, or `Latin1_General_100_CI_AS_KS_WS_SC_UTF8` case-insensitive — create-time only) and `eventhouse create --min-consumption-units` (`creationPayload.minimumConsumptionUnits`, always-on minimum capacity). Both live-verified end-to-end (warehouse collation via `DATABASEPROPERTYEX`; eventhouse via `show` → `minimumConsumptionUnits`). Offline dry-run e2e.
  - **Databricks mirror activation (`mirrored-databricks-catalog update --auto-sync`) + SQL-endpoint query resolution (two gaps fixed)**: Reproducing the Databricks Unity-Catalog mirroring end-to-end surfaced two gaps. (1) **A freshly created mirror never replicates** — `create` (even with the correct creationPayload) produces a mirror with `autoSync: Disabled` and `mirrorStatus: NotMirrored`, and the creationPayload has NO `autoSync` field (verified in the swagger), so there was NO way to start it. The `UpdateMirroredAzureDatabricksCatalog` PATCH DOES take `properties: {autoSync, mirroringMode, storageConnectionId}`, but fabio's `update` only handled name/description. Added `mirrored-databricks-catalog update --auto-sync Enabled|Disabled [--mirroring-mode Full|Partial|Exclude] [--storage-connection-id]`, nesting them under `properties` (the UpdatePayload shape). **The PATCH REQUIRES a `displayName` when the body carries `properties`** (a properties-only body is rejected with `Invalid Display Name: ''`), so `update` GETs the current display name and includes it when `--name` is omitted. Live-verified: `update --auto-sync Enabled` flipped the mirror from `NotMirrored`/`lastSyncDateTime:null` to `mirrorStatus: Mirrored`/`lastSync:<ts>`/`errorInfo:None` — the mirror began actively replicating. Pure `build_update_body` (nests properties, keeps displayName) unit-tested; offline dry-run e2e + live-validated. (2) **`warehouse query` didn't resolve a mirror's SQL endpoint** — a `MirroredAzureDatabricksCatalog` exposes a read-only SQL analytics endpoint at `properties.sqlEndpointProperties.connectionString` (same shape as a lakehouse), but `get_connection_string` only tried warehouse/lakehouse/warehouseSnapshot. Added the mirror fallback (same pattern as the earlier `5f1ce44` WarehouseSnapshot fix), so `warehouse query --id <mirror>` now works — live-verified against the mirror's SQL endpoint (system tables + `INFORMATION_SCHEMA` queryable). **Known Databricks-Fabric interop limitation (NOT a fabio gap)**: a Databricks **MANAGED** UC table cannot be read through the mirror's SQL endpoint when its data lives in the **Databricks-managed storage account** (`DESCRIBE EXTENDED` → `Type: MANAGED`, `Location: abfss://unity-catalog-storage@<dbstorage>.dfs.core.windows.net/…`). The SQL-endpoint metadata sync fails with a storage `BadRequest 400` (`x-ms-remote-data-destination-type`) — Fabric reads the table via a OneLake shortcut to that Databricks-managed ADLS and lacks credentials for it (the account sits in the Databricks-managed resource group, which has a deny-assignment, so you can't just grant Fabric access). **Deletion vectors are a SEPARATE, additional blocker but disabling them is NOT sufficient**: live-verified that after recreating the table with `delta.enableDeletionVectors=false` (→ `minReaderVersion=1`) the mirror re-syncs fine (`mirrorStatus: Mirrored`, fresh `lastSyncDateTime`, `errorInfo: None`) yet the SQL endpoint STILL returns the storage `BadRequest` — the managed-storage access is the real gate. To make a Databricks table queryable through the mirror it must NOT be a managed table in Databricks-managed storage: use an **EXTERNAL table in a customer-controlled ADLS** (then a Fabric ADLS connection + `mirrored-databricks-catalog update --storage-connection-id`), or store it in **OneLake** via the "Azure Databricks Storage" external-location flow (data lands in OneLake, read natively — no credential vending). See `.agents/API-BEHAVIORS-DISCOVERED.md` "Mirrored Databricks Catalog".
  - **Databricks stores Unity Catalog managed tables directly in OneLake (`azure-databricks-storage external-location`)**: The June-2026 interop feature lets Azure Databricks Unity Catalog store MANAGED tables (Delta/Iceberg), views, MVs, and streaming tables directly in OneLake — a single copy, no mirroring/copy. The Fabric side of the setup is: (1) create an **`AzureDatabricksStorage`** item (fabio's `azure-databricks-storage create` — the item is a shell whose `definition.json` is `{}`; the full 7-op CRUD+definition surface is already 1:1 with the API), (2) assign the Databricks Access-Connector Managed Identity / SP an Admin/Member/Contributor **workspace role** (`workspace add-role-assignment`), (3) enable the tenant setting `ArtifactDatabricksStoragePreview` ("Users can create Azure Databricks Storage items") and the workspace-delegated `AllowOneLakeUDK` ("Authenticate with OneLake user-delegated SAS tokens"; portal/admin — enable the tenant setting via `admin update-tenant-setting --setting-name AllowOneLakeUDK`), then (4) in Databricks create a UC storage credential (referencing the Access Connector) + an external location whose URL is the OneLake path. **The one thing that was missing in fabio** is surfacing that OneLake path: the Get response carries NO path, and Databricks REQUIRES the exact **ID-based ABFSS** form `abfss://<WorkspaceID>@onelake.dfs.fabric.microsoft.com/<DatabricksStorageID>/Files/` (name-based paths are rejected; the `/Files/` suffix is mandatory). Added **`azure-databricks-storage external-location --workspace <ws> --id <id>`** (read-only) — it validates the item is an `AzureDatabricksStorage`, then prints `externalLocationUrl` + the setup `prerequisites` + a `CREATE CATALOG … MANAGED LOCATION` example (same agent-native helper pattern as `mirrored-database landing-zone` / `data-agent mcp-url`). Pure `build_external_location_url` (via the new `client::onelake_dfs_host()`, honoring `FABIO_ONELAKE_DFS_ENDPOINT`) is unit-tested; the create→external-location→delete loop is live-validated (`azure_databricks_storage_external_location_lifecycle`). **Live-validated end-to-end against real Azure Databricks**: created the item, generated the path, enabled `AllowOneLakeUDK`, assigned the Access-Connector MI a Contributor workspace role, and created a UC storage credential in Databricks — Databricks then **recognized the fabio path as a valid Fabric OneLake path** (`"the path URL belongs to Fabric OneLake"`), confirming the format is exactly right; the external-location creation itself is gated by the Databricks workspace's Beta support for OneLake external locations (needs DBR 18.1+/serverless), a Databricks-side prerequisite, not a fabio gap. The complementary **"Publish to Fabric"** direction (Databricks Catalog Explorer creates a Mirrored Azure Databricks Catalog) is already covered by fabio's `mirrored-databricks-catalog` group (+ the `update --auto-sync` activation fix). See `.agents/API-BEHAVIORS-DISCOVERED.md` "Azure Databricks Storage".
  - **`warehouse query` resolves a MirroredDatabase SQL endpoint (bug fix)**: A `MirroredDatabase` (open mirroring, SQL/CosmosDB/Snowflake mirroring, …) exposes a read-only SQL analytics endpoint over its replicated Delta tables at `properties.sqlEndpointProperties.connectionString` (same shape as a lakehouse), but `warehouse::get_connection_string` only tried warehouse/lakehouse/warehouseSnapshot/mirroredAzureDatabricksCatalog — so `warehouse query --id <mirroredDb>` failed with `Could not determine SQL connection string`. Added the `mirroredDatabases` fallback (extends the same precedent as the `MirroredAzureDatabricksCatalog` fix `c74dd9f` and the `WarehouseSnapshot` fix `5f1ce44`). Refactored the 5 near-identical fallback blocks into a pure, unit-tested `extract_connection(data, via_sql_endpoint)` helper (direct `properties.connectionString` for Warehouse/WarehouseSnapshot vs `properties.sqlEndpointProperties.connectionString` for Lakehouse/MirroredAzureDatabricksCatalog/MirroredDatabase). Live-verified end-to-end reproducing the open-mirroring tutorial: create `--open-mirroring` → `landing-zone` → push a 20-digit `<n>.parquet` + `_metadata.json` (`{keyColumns:[…]}`) → `start` → `status`/`table-status` (POST) → `warehouse query --id <mirroredDb> --sql "SELECT … FROM Products"` returned the 3 mirrored rows. NOTE `sql-endpoint query --id <mirroredDb>` is by-design NOT the path (it resolves a SQLEndpoint item's OWN id via `/sqlEndpoints/{id}`; use the mirrored DB's `sqlEndpointProperties.id`, or just `warehouse query`). Pure helper unit-tested (3 tests); gated live e2e `warehouse_query_resolves_mirrored_database_sql_endpoint`. See `.agents/API-BEHAVIORS-DISCOVERED.md` "Mirrored Database API Behaviors Discovered".
 - **Documentation website**: `docs/` is an Astro + Starlight site organized with the Diátaxis framework. Its browser-searchable command reference is generated at build time from `src/commands/context/data/agent/commands.json`, and `.github/workflows/docs.yml` publishes it to GitHub Pages.


## Relevant Files

The full list of source files, test files, and config files is maintained in:

**File:** `.agents/RELEVANT-FILES.md`

Reference this file when looking up specific source locations or adding new files to the documentation.

## Documentation Website (MANDATORY)

The user-facing documentation site lives in `docs/` — an [Astro](https://astro.build) + [Starlight](https://starlight.astro.build) static site organized with the [Diátaxis](https://diataxis.fr) framework (Tutorials / How-to guides / Explanation / Reference). It is published to GitHub Pages and served from the custom apex domain at `https://ismaelmejia.com/fabio/`. Full-text search is provided by Pagefind (built in).

### Structure

```
docs/
├── astro.config.mjs        — Starlight config: sidebar, base path, plugins
├── package.json            — npm scripts + pinned deps (exact versions, no ^)
├── scripts/
│   ├── generate-reference.mjs   — generates reference/commands/*.md from commands.json
│   ├── check-links.mjs          — dependency-free internal link validator
│   └── *.test.mjs               — node:test unit tests for the scripts
├── public/                 — static assets (favicon, images) served at site root
└── src/
    ├── pages/index.astro   — hand-authored landing page (own <html>, not Starlight)
    ├── styles/             — landing + docs CSS
    └── content/docs/
        ├── getting-started.md          — Tutorial
        ├── guides/*.md                 — How-to guides
        ├── explanation/*.md            — Explanation
        └── reference/
            ├── index.md, global-flags.md  — hand-authored reference
            └── commands/*.md              — GENERATED (gitignored), never edit
```

### Local development

All commands run from `docs/`:

```bash
npm install                 # first time
npm run dev                 # generate reference + start dev server
npm run build               # generate reference + production build to dist/
npm run check               # generate reference + astro type-check + internal link check
npm run check:links         # internal link validation only (check-links.mjs)
npm test                    # node:test unit tests for the generator + link checker
```

Requires Node 22.12+ (CI uses Node 24). `npm run build`/`dev`/`check` all run `generate:reference` first, so the reference is always fresh.

### Generated vs. authored (critical)

- **Generated (never hand-edit)**: `src/content/docs/reference/commands/*.md` — one page per command group, produced by `generate-reference.mjs` from `src/commands/context/data/agent/commands.json`. The directory is **gitignored** and rebuilt on every build. To change the reference, change the CLI (then regenerate `commands.json`) — see **Auto-Generated Files (MANDATORY)**.
- **Authored (hand-maintained, CAN drift)**: the tutorial, guides, explanation pages, `reference/index.md`, `reference/global-flags.md`, the landing page, and styles. These must be updated by hand when the CLI surface they describe changes (e.g. a new global flag → update `reference/global-flags.md`).

### Build validation

`npm run check` (also run in CI) enforces two gates:
1. `astro check` — TypeScript/Astro diagnostics.
2. `check-links.mjs` — validates every internal link in authored pages and the landing page resolves to a real route (authored page, generated command group from `commands.json`, or public asset). No network calls, no server — deterministic and cross-platform.

### Deployment

`.github/workflows/docs.yml` builds on every push/PR that touches `docs/**`, `commands.json`, or the workflow, and deploys to GitHub Pages **only on push to `main`**:
- **build job** (`docs-build-<ref>`, `cancel-in-progress: true`): `npm ci` → `npm test` → `npm run check` → `npm run build` → upload Pages artifact. Runs on PRs too (validation without deploy).
- **deploy job** (`group: pages`, `cancel-in-progress: false`): serializes deploys and never cancels one mid-flight.
- **Base path & origin**: `astro.config.mjs` reads `SITE_URL`/`BASE_PATH` env vars (set explicitly in `docs.yml` to `https://ismaelmejia.com` + `/fabio`, because the site is served from the custom apex domain under the `/fabio` subpath). These override the `GITHUB_REPOSITORY`-derived fallback (`https://<owner>.github.io/<repo>`) used for forks/ad-hoc builds, and the localhost fallback used for local dev. Keep `SITE_URL`/`BASE_PATH` in the workflow in sync with the deployed domain so canonical URLs and the sitemap are correct.
- **One-time prerequisite**: the repo's **Settings → Pages → Source must be set to "GitHub Actions"** (not "Deploy from a branch") or the deploy job fails.

### When and how to keep it updated

| Change | Action |
|--------|--------|
| Add/modify/remove a command, subcommand, or flag | Regenerate `commands.json` (see Auto-Generated Files). The reference pages follow automatically on the next build — no docs edit needed. |
| Add/change a **global flag** | Update `global_flags()` in `agent.rs` AND `docs/.../reference/global-flags.md` (both are hand-maintained). |
| Change an install method, auth flow, or documented workflow | Update the relevant authored page (`getting-started.md`, a guide, or an explanation page). |
| Add a new authored page | Add it under the correct Diátaxis directory; sidebar auto-generates for `guides/` and `explanation/`. Run `npm run check` to validate links. |

### Best practices (MANDATORY)

- **Never hand-edit generated pages** (`reference/commands/*.md`) — they are gitignored and overwritten.
- **Pin exact dependency versions** in `docs/package.json` (no `^`/`~`), matching the repo-wide freshness policy. Validate against npm before bumping.
- **SHA-pin all GitHub Actions** in `docs.yml` with a trailing version comment (repo-wide rule).
- **Use relative links in authored Markdown** (e.g. `../guides/agents/`), not root-absolute links — Astro does not rebase root-absolute Markdown links, so absolute links break under the `/fabio` base path. `check-links.mjs` validates relative resolution.
- **Run `npm run check` before committing** any `docs/` change; it mirrors CI.
- **Add unit tests** for any new/changed logic in `docs/scripts/*.mjs` (`node:test`, colocated `*.test.mjs`).

A contributor-facing quickstart also lives in `docs/README.md`.

## Docker & Devcontainer

### Production Docker Image

Published to GHCR on every push to `main` and on version tags:

```
ghcr.io/iemejia/fabio:latest       # latest stable release
ghcr.io/iemejia/fabio:0.61.0       # release version
ghcr.io/iemejia/fabio:0.61         # major.minor
```

Multi-arch manifest: `linux/amd64` + `linux/arm64`.

**Dockerfile** (root): Multi-stage build — compiles in Alpine (native musl) builder stage, copies to `FROM scratch` runtime with only CA certs (~8MB). Binary is fully static (zero runtime dependencies).

### Devcontainer

Located in `.devcontainer/` for VS Code and GitHub Codespaces. Provides the full development environment:

**System packages** (in Dockerfile): `build-essential`, `cmake`, `pkg-config`, `libssl-dev`, `musl-tools`, `lld`, `clang`, `zig 0.16.0`

**Devcontainer features**: Rust (with cross targets), Git, GitHub CLI, Azure CLI

**Cargo tools** (installed via `postCreateCommand`): `git-cliff`, `cargo-zigbuild`, `cargo-xwin`, `cargo-audit`

**Cross-compilation targets** (for `./scripts/cross-check.sh`): `x86_64-unknown-linux-musl`, `aarch64-unknown-linux-musl`, `x86_64-apple-darwin`, `aarch64-apple-darwin`, `x86_64-pc-windows-msvc`

**VS Code extensions**: rust-analyzer, Even Better TOML, CodeLLDB debugger, Dependi (crate version checker)

**MANDATORY: Keep devcontainer in sync** — When adding a new Cargo dependency that requires system libraries or build tools (e.g., a `-sys` crate needing `cmake`, `libfoo-dev`, or a new linker), you MUST also update `.devcontainer/Dockerfile` to install the required package. The devcontainer must always be able to fully build fabio from source without additional manual setup.

### Docker CI Workflow (`.github/workflows/docker.yml`)

| Trigger | Build | Push to GHCR |
|---------|-------|--------------|
| `.devcontainer/**` or workflow change | devcontainer image | Yes (on push to `main`) |

`GITHUB_TOKEN` for GHCR auth (no extra secrets).

The release workflow (`.github/workflows/release.yml`) handles tagged version images (`:latest`, `:X.Y.Z`, `:X.Y`) as a separate `docker` job that uses pre-built binaries from the build job (no compilation in Docker).

### Relevant Docker Files

- `Dockerfile`: Production image (copies pre-built static binaries into `FROM scratch`, used by release workflow)
- `.devcontainer/Dockerfile`: Dev environment base image (Ubuntu + system deps + musl-tools + zig)
- `.devcontainer/devcontainer.json`: Features, extensions, cargo tools, cross targets
- `.github/workflows/docker.yml`: Devcontainer build + GHCR publish workflow

## API Behaviors Discovered

Runtime behaviors, quirks, and undocumented API details are documented in a separate file to reduce context size:

**File:** `.agents/API-BEHAVIORS-DISCOVERED.md` (2019 lines)

Reference this file when working on specific command groups. Do NOT load the entire file into context — search for the relevant section by command group name (e.g., "Lakehouse API Behaviors Discovered", "Deploy Command Design & Behaviors").

When discovering new API behaviors during implementation, append them to `.agents/API-BEHAVIORS-DISCOVERED.md` under the appropriate section heading.
