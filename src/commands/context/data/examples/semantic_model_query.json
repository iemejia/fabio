{
  "command": "fabio semantic-model query --workspace $WS --id $MODEL_ID --dax \"EVALUATE TOPN(5, 'Products', [Revenue], DESC)\"",
  "description": "Execute a DAX query against a semantic model. Returns tabular results.",
  "response": {
    "data": {
      "columns": ["Products[ProductName]", "Products[Revenue]"],
      "rows": [
        ["Widget Pro", "45000"],
        ["Gadget X", "38000"],
        ["Sensor Y", "29500"]
      ],
      "rowCount": 3
    }
  },
  "notes": "Requires the semantic model to be refreshed (framed). Direct Lake models need at least one POST /refreshes call after creation. DAX query must use EVALUATE for table results or DEFINE + EVALUATE for measures. Use double quotes for DAX strings inside the query.",
  "query_examples": [
    {"query": "rows", "description": "All result rows."},
    {"query": "columns", "description": "Column names from the DAX result."},
    {"query": "rowCount", "description": "Count of returned rows."}
  ]
}
