{
  "command": "fabio workspace get-onelake-settings --workspace $WS",
  "description": "Inspect OneLake lifecycle settings, including file access-time tracking.",
  "response": {
    "data": {
      "lifecycle": {
        "defaultTier": "Cool",
        "policy": "Active",
        "accessTimeTracking": "Enabled"
      }
    }
  },
  "notes": "When accessTimeTracking is Enabled, reads and writes update LastAccessTime. Metadata-only property, metadata, and tag operations do not.",
  "query_examples": [
    {
      "query": "lifecycle.accessTimeTracking",
      "description": "Read the current access-time tracking status."
    }
  ]
}
