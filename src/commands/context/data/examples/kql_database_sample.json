{
  "command": "fabio kql-database sample --workspace $WS_ID --id $KDB_ID --entity-name StormEvents --count 3",
  "description": "Samples N rows from a KQL table (or materialized view, external table, function). Default count is 10.",
  "response": {
    "data": [
      {"StartTime": "2023-01-15T08:00:00Z", "State": "TEXAS", "EventType": "Thunderstorm Wind", "DamageProperty": 15000},
      {"StartTime": "2023-01-15T09:30:00Z", "State": "ILLINOIS", "EventType": "Flood", "DamageProperty": 250000},
      {"StartTime": "2023-01-15T11:00:00Z", "State": "FLORIDA", "EventType": "Tornado", "DamageProperty": 1000000}
    ],
    "count": 3
  },
  "notes": "Output columns are dynamic — they match the table schema. Use --entity-type materialized-view|external-table|function for non-table entities. Results are always returned as a JSON array of objects keyed by column name.",
  "query_examples": [
    {"query": "[0]", "description": "Get the first row."},
    {"query": "[].State", "description": "Extract a single column from all rows."},
    {"query": "[?EventType=='Flood']", "description": "Filter rows by column value."},
    {"query": "length(@)", "description": "Get the number of rows returned."}
  ]
}
