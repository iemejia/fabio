{
  "command": "fabio item exists --workspace $WS_ID --id d4e5f6a7-b8c9-0123-def0-234567890abc",
  "description": "Checks whether an item exists without erroring on 404. Returns a simple boolean result.",
  "response": {
    "data": {
      "exists": true,
      "id": "d4e5f6a7-b8c9-0123-def0-234567890abc",
      "type": "Lakehouse",
      "displayName": "SalesLakehouse"
    }
  },
  "notes": "Never errors on 404 (unlike 'item show' which returns NOT_FOUND). When item does not exist, returns {\"exists\": false}. Useful for conditional logic in agent workflows (check before create, verify after delete).",
  "query_examples": [
    {"query": "exists", "description": "Boolean existence check."},
    {"query": "type", "description": "Item type (only present when exists=true)."},
    {"query": "displayName", "description": "Item name (only present when exists=true)."}
  ]
}
