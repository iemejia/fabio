{
  "name": "Direct Lake Semantic Model + Report",
  "description": "Create a Direct Lake semantic model from lakehouse Delta tables and bind a report for zero-copy analytics.",
  "prerequisites": ["Authenticated via fabio auth login", "Workspace with F2+ capacity", "Lakehouse with at least one Delta table loaded"],
  "steps": [
    {"step_number": 1, "command": "fabio lakehouse create --workspace $WS_ID --name \"SalesLakehouse\"", "description": "Create lakehouse for Delta tables."},
    {"step_number": 2, "command": "fabio lakehouse upload --workspace $WS_ID --id $LH_ID --source ./sales.csv --dest Files/staging/sales.csv", "description": "Upload source data."},
    {"step_number": 3, "command": "fabio lakehouse load-table --workspace $WS_ID --id $LH_ID --table sales --source-path Files/staging/sales.csv --mode Overwrite --format Csv --header true", "description": "Load CSV into Delta table."},
    {"step_number": 4, "command": "fabio lakehouse show --workspace $WS_ID --id $LH_ID --query 'properties.sqlEndpointProperties.id'", "description": "Get the SQL endpoint ID (needed for the model connection)."},
    {"step_number": 5, "command": "fabio semantic-model create --workspace $WS_ID --name \"SalesModel\" --file /tmp/model.bim --connection $SQL_ENDPOINT_ID", "description": "Create Direct Lake semantic model from model.bim with connection to SQL endpoint."},
    {"step_number": 6, "command": "fabio semantic-model refresh --workspace $WS_ID --id $SM_ID", "description": "Frame the model (required before DAX queries work). Wait 15-30s after."},
    {"step_number": 7, "command": "fabio semantic-model query --workspace $WS_ID --id $SM_ID --dax \"EVALUATE TOPN(5, 'sales', [amount], DESC)\"", "description": "Verify model works with a DAX query."},
    {"step_number": 8, "command": "fabio report create --workspace $WS_ID --name \"Sales Report\" --dataset $SM_ID", "description": "Create report bound to the semantic model."}
  ],
  "model_bim_template": {
    "description": "A minimal model.bim for a Direct Lake semantic model. Replace PLACEHOLDERS with actual values.",
    "key_fields": {
      "compatibilityLevel": "Must be 1604 for Direct Lake",
      "defaultPowerBIDataSourceVersion": "Must be powerBI_V3",
      "defaultMode": "Must be directLake",
      "partition.mode": "Must be directLake",
      "partition.source.type": "Must be entity",
      "partition.source.entityName": "Must match the Delta table name",
      "partition.source.schemaName": "Must be dbo (default lakehouse schema)"
    },
    "template": {
      "compatibilityLevel": 1604,
      "model": {
        "defaultPowerBIDataSourceVersion": "powerBI_V3",
        "defaultMode": "directLake",
        "tables": [{
          "name": "TABLE_NAME",
          "columns": [
            {"name": "col1", "dataType": "string", "sourceColumn": "col1"},
            {"name": "col2", "dataType": "double", "sourceColumn": "col2"},
            {"name": "col3", "dataType": "int64", "sourceColumn": "col3"}
          ],
          "partitions": [{
            "name": "part0",
            "mode": "directLake",
            "source": {"type": "entity", "entityName": "TABLE_NAME", "schemaName": "dbo"}
          }]
        }],
        "expressions": [{
          "name": "DatabaseQuery",
          "kind": "m",
          "expression": "let\n    database = Sql.Database(\"SQL_ENDPOINT_ID\", \"LAKEHOUSE_ID\")\nin\n    database"
        }]
      }
    },
    "datatype_mapping": {
      "string": "String/varchar columns",
      "int64": "Integer columns (int, bigint)",
      "double": "Floating-point columns (float, real, decimal)",
      "boolean": "Boolean columns",
      "dateTime": "Date/time columns (date, datetime, timestamp)"
    }
  },
  "creation_methods": {
    "model_bim": {
      "command": "fabio semantic-model create --workspace $WS --name NAME --file model.bim --connection $SQL_ENDPOINT_ID",
      "when": "Quick single-table models. Use model.bim (JSON TMSL) with compatibilityLevel 1604.",
      "notes": "The --connection flag binds the model to the lakehouse SQL endpoint. This is required for Direct Lake."
    },
    "tmdl": {
      "command": "fabio semantic-model create --workspace $WS --name NAME --file model.tmdl --file tables/table.tmdl --file definition.pbism --connection $SQL_ENDPOINT_ID",
      "when": "Multi-table models or complex expressions. TMDL is the recommended format for production models.",
      "notes": "TMDL separates concerns (one file per table). definition.pbism must use version 4.2 with Fabric schema URL."
    }
  },
  "tips": [
    "model.bim WORKS for Direct Lake — use compatibilityLevel 1604 and defaultMode: directLake.",
    "TMDL is preferred for multi-table models but model.bim is simpler for single-table cases.",
    "The --connection flag on semantic-model create is required — it links to the SQL endpoint ID (not the lakehouse ID).",
    "Get the SQL endpoint ID from: fabio lakehouse show --workspace $WS --id $LH --query 'properties.sqlEndpointProperties.id' -o plain",
    "After refresh, wait 15-30 seconds before querying with DAX.",
    "Use fabio lakehouse table-schema to discover column names and types for building the model.bim.",
    "Use --query 'id' -o plain to extract created item IDs for chaining commands."
  ]
}
